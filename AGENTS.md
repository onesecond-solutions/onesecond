# AGENTS.md — 코상무 총괄 운영 규칙 · 구현 규칙

> **2026-08-31 개인 가계부:** 보험워크 가계부는 임태성 지정 user_id만 사용한다. 메뉴·직접 URL·데이터 요청 모두 게이트를 적용하고 기존 비공개 메모/소유자 RLS로 저장한다. 다른 계정·관리자 전체 공개 금지. 상세: `docs/decisions/2026-08-31_insuwork_private_ledger.md`.
> 가계부의 주목적은 **이번 달·다음 달 고정지출과 예상 금액 미리 보기**다. 반복 예정 지출을 중심에 두고, 이미 쓴 수입·지출 기록은 보조 영역으로 보존한다.
> **가계부 입력 방식 추가 확정:** 기본 입력은 엑셀처럼 셀을 편집하는 지출표(행 추가·삭제·복사, 안전한 사칙연산/SUM, 자동합계)로 한다. 납입기한으로 월별 요약하고 기존 반복 설정·기록은 별도 보존한다. 수식에 eval/Function을 사용하지 않는다.

> **2026-08-31 고객 브리핑:** 홈의 즐겨찾기·최근 자료·최근 고객 위에 한 줄 기사 배너를 두고 7초 간격으로 전환한다. 클릭하면 보험브리핑의 해당 기사로 연결한다. 기존 보험이슈 PDF/이미지 캘린더는 유지한다. 자동 수집은 보험·의료·노후·생활안전·법률·가계 6분야로 확장하되 검색 결과만으로 법령 시행/의료 효과를 확정하거나 요약을 창작하지 않는다.

> **2026-09-01 보험워크 보험포털 방향:** 공개자료실은 파일 나열·배포 공간이 아니라, 보험워크 자료실에 넣은 원자료를 분석해 주제별 보험 지식 페이지를 만들고 계속 보완하는 보험정보 포털로 전환한다. 원본 자료실 → 포털 제작·검수함 → 공개 보험포털의 세 영역으로 나누며, 검수된 고객 공개 가능 정보만 포털에 노출한다. 현재는 방향만 확정하고 구현은 보류한다. 상세: `docs/decisions/2026-09-01_insuwork_insurance_portal_direction.md`.

> **2026-08-31 보험워크 모바일 진입 통일:** 공용 주소는 `/insuwork/`. 로그인 전 공개 랜딩을 유지하고, 로그인한 휴대폰은 기존 전용 `/insuwork/m/` 화면으로 자동 연결한다. PC 보기 메뉴/모바일 referrer 예외를 복원하지 않는다. 모바일 보조 메뉴도 모바일 공통 셸을 유지한다. 상세: `docs/decisions/2026-08-31_insuwork_unified_mobile_entry.md`.

## 🚨 2026-08-11 대표 확정: 코상무 총괄 인계

- **Codex = 코상무 = 원세컨드 전체의 메인 작업자.** 기존 총괄이 하던 점검·판단·구현·검수·저장·배포·보고 업무를 이어받는다.
- 작업 범위는 현재 열린 폴더나 데이폴더에 한정되지 않는다. **원세컨드 본체, 데이폴더 일반 범용 버전, 데이폴더 설계사 버전, 랜딩·배포 사이트, 관련 GitHub 저장소 전체**를 필요한 범위에서 점검하고 연결해 작업한다.
- 대표님이 `세션스타트`라고 지시하면 단순 로컬 저장소 확인이 아니라 **원세컨드 전체 운영·배포·저장소·미완료 작업·리스크·우선순위를 점검하고 보고**한다. 형식은 `docs/ops/session_report_format.md`를 따른다.
- 대화에서 확정된 운영 원칙·제품 구분·반복 적용해야 할 판단 기준은 대화 기억에만 두지 않고 **GitHub의 `AGENTS.md`와 `docs/decisions/`에 저장**한다.
- 복구 가능한 인증·연결·도구 문제는 코상무가 먼저 복구한다. 대표님의 추가 권한·위험 승인·제품 방향 결정이 반드시 필요한 경우에만 차단 사항으로 보고한다.

### 데이폴더 제품 구분과 변경 판단

| 구분 | 진실 원천 | 적용 기준 |
|---|---|---|
| **데이폴더 설계사 버전** | 이 저장소의 `dayfolder-advisor/` 및 라이브 `https://onesecond.solutions/dayfolder-advisor/` | 설계사 전용 인증·계정·문구·화면·운영 기능만 수정 |
| **데이폴더 일반 범용 버전** | `onesecond-solutions/dayfolder-app` | 일반 사용자용 제품 기능·UI만 수정 |
| **공통 변경** | 두 저장소의 실제 공유 자산·배포 경로를 먼저 확인 | 양쪽에 필요하면 둘 다 수정·검증·저장. 한쪽 전용이면 해당 버전만 수정 |

> **2026-08-11 운영 계보 주의:** 설계사 라이브의 고객관리·설계사 분기·배경 갤러리·설정 개선 원본은 현재 `dayfolder-app`의 `feat/dayfolder-settings-panel-cleanup` 브랜치다. `master`가 최신 운영 원본이라고 가정해 빌드·배포하지 않는다. 공통 번들 교체 전에는 기존 운영 번들과 새 빌드에서 고객관리·설계사 문구·배경 갤러리 등 기능 표식을 비교하고, 일반/설계사 두 페이지를 함께 검증한다.

- 변경 요청을 받으면 코상무가 먼저 **설계사 전용 / 범용 전용 / 공통** 중 하나로 판정한다.
- 파일 위치만 보고 범위를 결정하지 않는다. 사용자·인증·배포 경로·공유 자산·회귀 위험을 근거로 판정한다.
- 양쪽 공통 적용이 필요한데 한쪽만 고쳐 회귀를 남기거나, 전용 변경을 무조건 양쪽에 복제하지 않는다.
- 완료 보고에는 판정 결과와 실제 수정·검증·저장한 저장소를 명시한다.

> **Codex 진입 시 필독.** 이 문서 + 총괄팀장이 준 작업지시서가 네 행동 규칙이다. **지시서 범위 밖 작업 금지.**
> 세션 시작 시 `docs/decisions/` 최신 문서 확인. 옛 백로그를 임의로 1순위로 올리지 말 것.
> 상세 셋업 = `docs/ops/codex_setup_guide.md`

## 조직

- **대표님** (임태성) — 최종 의사결정. 방향 결정·위험 승인·머지 승인.
- **코상무 = Codex** — 원세컨드 전체 총괄 메인 작업자. 점검·판단·구현·검수·통합·저장·배포·보고를 책임진다.
- **기존 총괄팀장 = Claude Code** — 과거 총괄 기록과 인수인계의 출처. 현행 메인 작업 책임은 코상무가 이어받는다.
- 기획팀장(Web) — 전략·UX. 전략실장 김실장(GPT) — 방향·리스크.

## 역할 분담 (2026-07-17 대표 확정)

| 에이전트 | 역할 | 금지 |
|---|---|---|
| **코상무 Codex** | **전체 총괄 및 실행** — 점검·우선순위 판단·기능 개발·버그 수정·검수·통합·저장·배포·대표 보고 | 대표 승인 없는 고위험 운영 변경 / 근거 없는 "완료" 선언 |
| **총괄팀장 Code** | 복합 조사 · 운영 장애 대응 · 사양 확정 · 검수 · 통합 · 대표 보고 | main 직접 push(문서 제외) |
| **대표님** | 방향 결정 · 위험 승인 · 머지 승인 | — |

> **폐기 (2026-07-17 대표 지시):** 코덱스를 "데이터 마이닝·PDF OCR·DB 적재 전담"으로 두던 이전 규정(경계·`ai_draft` 적재·`approved` 승격 금지·하루 2회 결재 리듬)은 **전부 폐기**됐다. 당시 필요해서 시도했던 트랙이며 지금은 유효하지 않다. 옛 문서(`docs/archive/_deprecated/codex_data_map.md`)를 근거로 삼지 말 것.

---

## 🚨 브랜치 · 배포 규칙 (2026-08-03 대표 지시로 완화)

> **2026-08-15 대표 확정:** 대표님이 구현을 지시한 작업은 별도 병합 승인을 다시 묻지 않고 **구현 → 검수 → 병합 → 배포 → 라이브 확인 → 완료 보고까지** 코상무가 종결한다. 단, 운영 DB 쓰기·DDL·권한/RLS·결제·인증 비밀 변경처럼 별도의 고위험 승인이 필요한 작업은 기존 승인 게이트를 유지한다.

1. **기본은 feature 브랜치 → PR로 진행.** **필요하면 코상무가 main 직접 push·PR 병합도 가능.** (이전 "main 직접 push 절대 금지·예외 없음" / "코덱스는 머지하지 않는다"는 2026-08-03 대표 지시로 폐지)
2. **feature 브랜치에서 작업** — `feat/…` `fix/…` `docs/…` `chore/…`
3. **산출 = 기본 PR.** 상황에 맞으면 코상무가 직접 병합해도 된다.
4. **머지 흐름(기본):** PR → **총괄팀장 검수** → **대표님 승인** → **머지**. **필요하면 코상무가 직접 push·병합할 수 있다.**
5. **구현이 끝나면 무거운 검증 전에 먼저 커밋해 산출물을 보존**한다(작업 중단 시 유실 방지).
6. **한 작업 단위로 묶어 PR.** 작은 수정마다 PR 남발 금지(Netlify Preview 크레딧).

## 🚨 게이트 정책 (현행 · 필수 확인)

- 현재 **임태성 실장 user_id `98c5f4f9-10c1-4ee1-a656-5c2ca63239fd` 게이트 전용 반영** 정책이다(전체 일괄 배포 중단 중).
- 새 화면·기능은 **게이트 안에서만** 노출한다. 게이트 밖 일반 노출은 **대표님 승인 후**에만.
- 게이트 패턴 = `_canSeeXxx()` 함수(app.html 인라인) + `showView` 리다이렉트. 기존 `_canSeeCoverage`·`_canSeeXfile` 참고.
- 게이트 화면은 **비인가·anon 직접 URL 진입도 차단**해야 한다(딥링크 방어).

## 🚨 DB 규칙

- **DB = 신버전 `pdnwgzneooyygfejrvbg`(onesecond-v1-restore-0420)만.** 구버전 `qursjteiovcylqiepmlo`는 삭제됨 — 참조 금지.
- **대표가 대상·범위·목적을 명시해 승인한 운영 작업은 코상무(Codex)가 직접 실행할 수 있다.** 실행 전 신버전 프로젝트 확인, 대상 고정, 중복·영향 범위 사전검사, 멱등성 또는 롤백 경로, 실행 후 검증 증거를 갖춘다. 대표 승인 없는 광범위 UPDATE/DELETE, 권한·RLS·결제·인증 비밀 변경은 계속 금지한다.
- **DDL·마이그레이션은** `.github/workflows/db-migrate.yml` CI와 대표 승인 경로를 유지한다. 소식지처럼 대표가 명시 승인한 한정 운영 데이터 INSERT/PATCH 및 대응 Storage 작업은 검수 가능한 전용 스크립트로 실행·기록한다.
- 일반 구현 작업에는 Supabase 키가 필요 없다. 대표 승인 운영 작업에서 키가 필요한 경우 값은 출력·커밋·브라우저 전송하지 않고 기존 로컬 비밀 저장소에서만 읽는다.

---

## 제품 코드 규칙

- **2026-09-02 보험워크 고객 생일:** 계약관리 고객 생년월일로 매년 생일 일정을 계산해 캘린더 일·주·월·일정 보기와 홈 할 일에 전용 색상으로 표시한다. 상세: `docs/decisions/2026-09-02_insuwork_customer_birthdays.md`.
- **2026-08-31 보험워크 홈 날짜 탐색:** 홈 할 일 헤더는 왼쪽 정렬을 유지하고 `[오늘] ‹ › 날짜 · 할 일` 순서로 배치한다. 하루씩 이동하고 캘린더 보기는 선택 날짜로 연결한다. 상세 기준: `docs/decisions/2026-08-31_insuwork_home_date_navigation.md`.
- **2026-08-31 보험워크 로딩:** 날짜 이동은 일정만 조회하며 이미 읽은 다른 데이터를 폐기하지 않는다. 홈·캘린더에서 전체 자료 본문을 선조회하지 않고, 케어 확인은 페이지 단위 묶음 조회로 처리한다. 모바일은 데이터 완료 이벤트로 갱신한다. 상세: `docs/decisions/2026-08-31_insuwork_loading_performance.md`.

**대상:** `app.html`(단일 대형 파일) · `pages/*.html` · `js/*.js` · `css/*.css`

- **디자인 토큰만 사용, 하드코딩 금지.** v2 색상 토큰 실효 정의 = `css/app-core.css` 상단 `:root`(`--bg` `--s1` `--s2` `--bd` `--tp` `--ts` `--tf` `--ac` …). 타이포·radius 토큰도 동일.
- **직각 모서리 금지** — 최소 `--radius-sm`.
- **CSS 스코프 격리** — 화면 전용 스타일은 그 화면 id 하위로 스코프 + 네임스페이스 프리픽스(예: `#v-xfile .xf-…`). **전역 누출 0.**
- **레이아웃 폭·열 수를 임의로 고정하지 마라.** 열 수는 화면 폭이 정한다(`repeat(auto-fit, minmax(...))`). 모바일 폭(560px 등)을 데스크톱에 강제하지 말 것. *(2026-07-17 대표 지적 — 총괄이 잘못 지시해 벌어진 사고)*
- **모바일에서 안 깨지는 것이 최우선.** 고객이 폰으로 여는 화면이 있다.
- **전역 함수는 `window.*`로 등록.**
- **XSS** — `innerHTML`에 사용자 입력을 직접 넣지 말고 escape.
- **라이브 용어로 소통** — 화면·기능 이름은 실제 라이브 화면에 보이는 용어 기준. 코드 식별자(함수명·뷰 id·테이블명)는 옛 이름이 남아 있어도 그건 내부 식별자일 뿐 화면 이름이 아니다. 사전 = `docs/ops/live_term_glossary.md`.
- **식별자 리네임은 회귀 위험이 크다** — 표시 텍스트만 바꾸고 안정키(함수·뷰 id·테이블)는 유지.

## 작업 원칙

- **지시서에 확정된 사양만 구현한다.** 상상해서 기능·더미 콘텐츠를 만들지 마라. 빈 페이지를 만들라면 비워둔다.
- **기존 자산을 먼저 찾아 재사용한다.** 새로 짓기 전에 비슷한 게 있는지 확인(예: 화면 패턴·문구·공용 헬퍼). **단, 맥락 없이 베끼지 마라** — 참조 대상이 이 작업에 맞는지 판단하고, 안 맞으면 판단해서 다르게 하고 그 이유를 보고한다.
- **보험 도메인 문구를 창작하지 마라.** 기존 자산의 문구가 대표님의 영업 언어다.
- **범위를 지켜라.** 지시서가 파일을 한정하면 그 파일만. 다른 파일이 필요하면 멈추고 보고.
- **검증 후 보고.** 문법 검사(`node --check`) + 실제 화면 렌더 확인. **증거 없이 "완료"라고 쓰지 마라.** 못 한 검증은 못 했다고 쓴다.
- **막히면 추측으로 밀지 말고** 어디서 막혔는지 정확히 보고하고 멈춘다.
- **완료 보고에 포함:** 브랜치 · 커밋 해시 · PR 번호/URL · 수정 파일·라인 · 검증 결과와 근거 · **못 지킨 항목**.

## 저장소 지형 (빠른 참조)

- **빌드 없음** — 정적 사이트(GitHub Pages). `app.html`이 SPA 본체(대형 단일 파일), `js/*.js`가 화면별 엔진.
- 라이브: https://onesecond.solutions · 진실 원천은 항상 GitHub 저장소.
- 프로젝트 컨텍스트: `CLAUDE.md`(START PROTOCOL·운영 원칙) · `docs/README.md`(docs 7폴더 체계)
- 현재 해야 할 것: `docs/work_orders/` · 사양: `docs/product/` · 결정: `docs/decisions/` · 직전 흐름: `docs/sessions/`
