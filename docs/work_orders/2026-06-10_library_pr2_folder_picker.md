# [작업지시서] 자료함(library) PR2 — 폴더 선택기 업로드 + UI 깊이 개선

> 발신: 기획실장(Web) / 수신: 총괄팀장(Code) / 승인: 대표(임태성) / 2026-06-10
> 구현: 작업1~6=#523(기반영) / 작업7=PR #525 / 작업8(미리보기)=진단 후 별 처리

## §0 정합
- 신버전 Supabase `pdnwgzneooyygfejrvbg`(Pro)만 참조.
- #519 업로드 기본 라이브 OK(+한글 키 400 수정 #524). #520·#521·#523 합본 충돌 0.
- **DDL/RLS 변경 0.**

## 작업 1~6 (이미 #523 라이브)
- 폴더 올리기 버튼(`webkitdirectory`) + 드래그 재귀(`webkitGetAsEntry`), 계층 보존.
- 빈 폴더 생성 / 중복 폴더명 "… (2)" 자동 / search_text(파일명) / 대량 진행·부분실패 / 빈 CTA.
- → 재구현 아님. 라이브 확인 대상.

## 작업 7 (UI 깊이 — 본 PR #525)
- 3컬럼 **카드화**: `background:--s1` + `border:1px --bd` + `radius-lg` + 약한 깊이(`box-shadow 0 1px 2px rgba(0,0,0,0.04)`), 플랫 톤 유지.
- 컬럼 헤더 구분선(`.mvcol-head` border-bottom 유지).
- 선택 폴더 인디고 악센트(`.mvf-item.on` = `--ac` 채움, 기존 유지).
- 빈 상태 = 원형 아이콘 + 안내문("아직 저장한 자료가 없습니다 / 파일·폴더를 올려보세요"). 텍스트 only 금지.
- 가드: §4 고지 1줄 유지(모달 강제·재확인 추가 금지 — 대표 결정).

## 작업 8 (미리보기 깨진 아이콘 — 진단 후 처리)
- 증상: 파일 선택 시 미리보기 깨진 아이콘. 다운로드 정상 → 저장 OK·렌더 실패.
- **추측 패치 금지** → 콘솔 진단으로 (a)타입 미분기 / (b)Storage 접근(403·404·CORS·content-type) 확정 후 수정.
- 목표 분기: 이미지=`<img>` / PDF=`<iframe>` / 그 외=깨진아이콘 금지→파일 아이콘 + "미리보기 미지원·다운로드". private=signed URL.

## 범위 밖
- 파일/폴더 이동(move) = P1.5.

## 개인정보 로드맵(주석, 미구현)
- 유료화·PRO 격리 시점에 개인정보 3요건 + 데이터 격리 검토.

## AC (라이브 실계정)
- 폴더 올리기→계층 유지(#523). 빈폴더·중복명·검색.
- 3컬럼 카드·깊이·악센트·빈상태 아이콘(밍밍함 해소).
- 미리보기: 이미지·PDF 실제 렌더, 미지원=안내+다운로드, 콘솔 403/404/CORS 0.
