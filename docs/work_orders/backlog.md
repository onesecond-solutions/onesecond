# 작업예정 백로그 (Pending Work Orders)

> 결재는 났으나 설계/우선순위로 뒤로 미룬 항목. 착수 시 이 파일에서 꺼내 진행하고, 완료되면 줄을 지운다.

## 어드민(운영센터) — 엔티티 카드 상세뷰 (2026-05-31 보류)

**배경:** 운영센터 SPA 흡수(PR #207~211) 완료. 대시보드 위젯·버튼 드릴다운까지 라이브. 단, **목록 카드 본체 클릭 = 비활성**(원본 admin부터 없음). "어드민은 이 정도에서 멈춤" 결재(2026-05-31) → 다른 페이지 마무리 후 재개.

**남은 작업 = 엔티티 카드 클릭 시 상세뷰** (설계 결정 선행 필요):
- **게시글 카드** → 현장의 소리 원문 딥링크 vs 어드민 내 상세 모달 (어디로?)
- **사용자 카드** → 상세 관리 모달 (권한 변경·정지·지점 재배정)
- **지점 카드** → 소속 인원 목록 상세
- **댓글 카드** → 원 게시글 딥링크
- **자료실 카드** → 자료 상세/원문

**참고:** 카드 클릭=내부 원본 딥링크 원칙([[home_no_mock_realdata_principle]] 정합). 재개 시 위 5종 목적지 결재부터.

**현 상태(라이브):** 메인테넌스 ON(대개편 검수 중)이라 외부 노출 없음 → 급하지 않음. 대시보드/탭·칩/스크롤/배지/데이터(실DB)는 정상.

## 소식지 검색 — 표·사진 미리보기 (B단계, 2026-06-03 보류)

**A단계 완료:** 검색 결과·AI 답변 → 원문 PDF 링크(`newsletters.source_pdf_url`)로 표·사진 그대로 확인 가능.

**B단계 (후속):** PDF 페이지를 이미지로 렌더해 검색 결과 안에서 **썸네일/미리보기**. PDF.js 렌더 필요(큰 작업). 표·도표·사진을 링크 클릭 없이 화면에서 바로 보게 하는 고급 단계.

## 검수 큐 UX v2 — Phase 3 가동 직전 (1개 PR로 묶음, 2026-06-06 결재)

**배경:** 지식창고 Phase 1(PR #432) = 기본 검수 큐(행별 승인·보류·폐기 + 일괄 승인 + status 필터탭). UX 고도화는 야간 자동 학습(Phase 3) 가동 직전에 **1개 PR로 묶어** 진행. Phase 1 = 그릇·동작 검증까지, v2 = 대량 검수 효율화.

**항목 4 (`pages/knowledge-vault.html` 검수 큐 탭):**
1. **마스터-디테일 전환** — 좌 카드 목록 / 우 본문 전문 + 판정 버튼. 기존 게시판 카드 스타일 재사용. 모바일 = 목록→상세 전환.
2. **판정 후 자동 다음 항목 이동** — 승인/보류/폐기 직후 다음 ai_draft로 포커스 자동 이동(연속 검수 속도).
3. **전체 선택 / 현재 필터 전건 승인 버튼** — 기결재 C안. Phase 1엔 전체 선택 없음(개별 체크만) → 대량 승인 시 SQL 의존 해소.
4. **학습일지 → 검수 큐 바로가기** — 학습일지 탭에서 해당 run이 만든 ai_draft 항목으로 바로 점프(run 단위 검수 동선).

## 대시보드 "지식 검수 대기" 카드 — Phase 3 가동 시 (2026-06-06 기록, 착수 금지)

**배경:** 운영센터 대시보드 "오늘 해야 할 일"(Task Queue, `#ac-task-queue`)에 **"지식 검수 대기 N건"** 카드 추가. 클릭 시 지식창고(`knowledge-vault.html`) 검수 큐로 이동. N = `knowledge_entries where status='ai_draft'` 카운트.

**전제:** Phase 3(야간 자동 학습)가 가동돼 ai_draft가 실제로 쌓이기 시작한 후. 그 전엔 항상 0건이라 의미 없음 → **Phase 3 가동 시점에 착수.** 진입 루트는 이미 운영센터 콘텐츠 > 지식엔진 > 지식창고로 연결됨(PR 본건).
