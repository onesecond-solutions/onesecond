# 보장분석 담보명 너비 — 표시된 대분류 기준

- 담보현황에서 선택한 대분류의 표시 행만 담보명 자동 너비 계산에 포함한다. 전체현황은 전체 행을 사용한다. 복사용 체크박스 선택은 표시 필터와 별개다.
- 최초 렌더와 폰트 로딩 후 실측, 담보명 편집, 자동 맞춤 복귀 모두 같은 표시 기준을 사용한다.
- 직접 입력한 줄바꿈은 각 줄을 따로 측정한다. 마우스로 지정한 너비가 있으면 우선 적용하며 경계 더블클릭으로 현재 표시 담보에 자동 맞춘다.
- 원본 담보 데이터, 기본 양식, 복사 이미지 처리 방식은 변경하지 않는다.
