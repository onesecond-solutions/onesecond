---
세션 종료 시각: 2026-06-08 07:27 KST
주요 마감 작업: 권한/조직/지도 문서 3종 + Google 로그인 근본수정 + 바텀시트 v1·공유/마이채팅·수정버튼 전수 + 첨부 일관화 + 지식엔진 사이클2
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-07 저녁 ~ 06-08 새벽 (Code 단독, 초장시간 마라톤). 팀장님 작업지시서·실시간 지시 다수. **PR #463~#480 머지 17건 전부 라이브 / main 직접 push 0건 / DB 적재 0건.**

## 🔝 다음 세션 1순위 (전부 결재/검수 대기)
1. **지식엔진 사이클2 잔여 채굴 (72→300)** + 하 7건 수정 + 중 출처 보강 + **적재 SQL**(사이클1·2 적재후보 → knowledge_entries, 별도 결재·PR 파일·실행은 팀장님)
2. **라이브 검수 잔여**: 수정 버튼 전수(#479·#480)·Google 로그인(#468)·바텀시트(#469·#470)·채팅 마이채팅(#477) 실장 로그인 눈 검수
3. (미착수) 좋아요·댓글·공유 1단계 — 게시판 반응 기능 (범위 확정됨: 커뮤니티·Q&A·공지 / 추천·공유대상선택은 추후)

## 완료 — 문서 (architecture)
- **#463 role_access_map.md** — 권한 전수조사(9롤×메뉴 매트릭스 + URL우회 + RLS 테이블×CRUD + 가입경로 + 조직매핑 + 차이후보9). 토대=health_check 6/3.
- **#465 organization_policy.md 신설 + role_access_map 크롬 6/7 실측 합류** — 조직 3계층(company→branch→team)·team NULL 규칙(insurer 항상 NULL)·보험사 branch_id 규칙·검색범위. role_access_map §2 미확인 해소(처방B 실재·board_reads 3·push_subscriptions 1·posts 12정책)·§3 동기화 주체=handle_new_user 트리거 확정·§5 실측 합류·§6 신규의심 2건(메리츠 테스트 GA branch_id / dod*** team有branch無).
- **#466 onesecond_map_v1.md** — 전수 인벤토리(화면11·Quick6·테이블~32·EdgeFn9·검색·**휴면자산11**). 소식지 OCR 전례 재발방지.

## 완료 — 인증
- **#464** insurer 계열 현장의 소리 노출·진입 차단(_canSeeVoice + showView/bootView 가드 + applyRoleClass nav-voice).
- **#467** OAuth error 표면화(?error 콜백 콘솔 로깅 + 이메일 로그인 유도).
- **🚨 #468 Google 로그인 근본 수정** — `_handleOAuthCallback`(PKCE ?code 교환)이 `Auth.init()`에서만 호출되는데, app.html 부팅은 `tryInitAuthSafe`가 `loadUser()`만 호출(2026-05-23 hotfix) → ?code 영영 미교환 → 토큰 미생성 → 드롭다운 먹통·"로그인 후 표시됩니다". 처방: tryInitAuthSafe가 부팅 시 OAuth 콜백 파라미터 감지하면 handleOAuthCallback 먼저 await. (이메일 OTP는 verifyOtp가 자체 토큰 저장이라 정상이었음)

## 완료 — 바텀시트 v1
- **#469** 스크립트/채팅/자료 트레이(.main 하단 fixed, 모바일 3단스냅·PC resize, 칩4, 저장규약 OSChatScript encode/decode 캡슐화·scope=global+keywords '유형:채팅').
- **#470** 시각 피드백 5(표면 톤 s2·상단 ac보더·2줄그라버·화살표상태토글·FAB z-index 95·첫방문 모션).
- **#476** 추천→공유 라벨 + 🚨채팅 안보임 수정(renderChat cs.{유형:채팅} 콜론→PostgREST 배열파싱 깨짐 → cs.{"유형:채팅"} 따옴표).
- **#477** 마이 채팅 칩(마이 스크립트 우측, 채팅작성 가능자만 노출).

## 완료 — 첨부 / 스크립트 카드
- **#471** MY SPACE 글쓰기 첨부 삭제 버튼(DataTransfer) + 닫을 때 초기화.
- **#472** 모든 글쓰기(자료실 ivPreviewFiles·함께해요·매니저룸) 첨부 삭제·초기화 일관화.
- **#473** 스크립트 페이지 scope=eq.global — 개인 글 공용카드 누수 차단(#421 stale 계승, #421 닫음). 도입인사 카드 순서 밀림 원인.
- **#475** parseScriptSections 폴백 — 표준 박스(border:1.5px #DCDCDC) 아니어도 평문 본문 렌더(escape+줄바꿈). "보험 점검형 오프닝" 빈 카드 처방.

## 완료 — 수정 버튼 전수 (#479 1단계 + #480 2단계)
본인 글 읽기 화면 전수에 수정 버튼: 보험Q&A·단체방·자료실·MY SPACE 자료(기존) + **스크립트·채팅·바텀시트·스크립트페이지·함께해요(신규)**.
- 기반: `openMyspaceWrite(ctx, edit)` 편집모드 + `_mysPrefillEdit`(콜: 필드 / 채팅: 상황+대안 decode 복원) + `_saveMyspaceEdit`(scripts PATCH) + `scrEditOpen`.
- 진입점: MY SPACE 스크립트 뷰어 / 바텀시트 마이스크립트·마이채팅 / 스크립트페이지(본인 owner만) / 함께해요(openTogetherWrite 편집+PATCH).

## 완료 — 지식엔진 (#478, 무인 2타임)
- **사이클1 마무리**: 보충 채굴 17(01·02·04 각50 정합) + 검수 + **미검수17 착시 규명**(review_batch 요약헤더 과소 오기, 판정표=terms 일치) + 헤더 4건 정정.
- **사이클2**: 채굴 72(계약·보장·인수·판매·민원·제도 각12, 심화+동의어) + **전수 검수 상44·중21·하7, 적재후보 65건(90.3%)**(사이클1 62.7% 대비↑, 심화=법령·공식보고서 출처). 하7=출처/표현 보완 대상(cycle2/review/summary.md).

## 🚨 학습
- **OAuth 콜백 처리 경로 끊김**: 콜백 교환 함수가 init에만 있는데 부팅이 init 우회 → 콜백 미처리. 부팅 초기화가 어떤 함수를 호출하는지 확인.
- **PostgREST `cs` 배열값에 콜론** = 큰따옴표로 감싸야(`cs.{"유형:채팅"}`) 파싱 정상.
- **scripts RLS anon 차단** = 미인증 GET = `[]`. DB 데이터 실측은 로그인 토큰 필요(크롬/콘솔/Dashboard).
- **워커 자가집계 불신** 재확인(batch02·03 오류) → 검수자 표 직접 카운트(node).
- **브랜치 첫 push `-u` 누락** = 조용히 미push(로컬만). 첫 push는 `-u origin <브랜치>`.
- 큰 편집 기능(수정버튼)은 전수 진단(읽기화면×수정유무) 후 단계 구현.

## 미해결 / 이월
- 좋아요·댓글·공유 게시판 반응(범위 확정·미구현).
- 사이클2 잔여 채굴 + 하7 수정 + 적재 SQL(별도 결재).
- 차이후보 #2(가입 branch_id/team_id NULL 조용한 제외)·#10(메리츠 테스트 branch_id) 처방 = v2 이후/실보험사 입점 전.
- stale `feat/report-button-header` 정리.

## 현재 데이터 상태
- **DB 적재 0건 유지** — knowledge_entries 72건·synonyms 7행 변동 없음. 채굴물(사이클1 293+보충17 / 사이클2 72)은 docs/data/terms_draft/ ai_draft(검색 비노출).
- 코드(app.html 등) 변경분 전부 라이브(GitHub Pages).
