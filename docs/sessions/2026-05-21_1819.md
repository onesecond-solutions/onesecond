---
세션 종료 시각: 2026-05-21 18:19 KST
주요 마감 작업: Phase A 알림 시스템 인프라 + 검색 페이지 + PDF 460건 OCR 99.8% + 디자인 시스템 시안 검증
생성 도구: Claude Code /session-end (옵션 B — Claude AI 웹 토큰 소진으로 Code 자체 인계 노트 작성)
---

# 2026-05-21 오후 (18:19) — Phase A 알림 인프라 + 검색 페이지 + PDF 460건 OCR + 디자인 시안 검증

> **본 인계 노트 출처:** Claude Code(Opus 4.7 1M) 자체 작성. 본 세션 진행 중 Claude AI 웹 토큰 소진으로 인계 노트 작성 불가 → 옵션 B로 Code가 본 세션 흐름 통째 정리.
>
> **세션 기간:** 2026-05-21 09:48 ~ 18:19 (약 8.5시간, 실제 작업 약 6시간)
>
> **핵심 어휘 정정:** 본 세션 후반에 팀장님이 "박힘/박음/박는" 시리즈 어휘 금지 결정. 메모리 `feedback_avoid_bonjin_bakum` 강화. 본 인계 노트부터 평이한 한국어로 작성.

---

## 본 세션 마감 4건 (큰 그림)

### 1. Phase A 알림 시스템 인프라 (v1.1) — DB + 코드 완성

**DB 마이그레이션 (`docs/migrations/2026-05-21_notifications_setup.sql`):**
- `notifications` 테이블 신설 (type 7종: system/staff/manager/insurer/team_lab/mine/ops)
- RLS 4종 (SELECT 본인 / INSERT admin / UPDATE 본인 / DELETE 본인+admin)
- 인덱스 2종 (recipient_id + 미열람 필터)
- 기존 `update_nav_question_activity()` 함수 갱신
  - SECURITY DEFINER + EXCEPTION 격리로 알림 INSERT 실패가 답글 작성 실패로 번지지 않게 격리
  - 답글 작성 시 게시글 작성자에 `mine` type 알림 자동 생성

**Chrome AI 검증 결과 = PASS ✅**
- notifications_count: 0 / RLS 4건 (DELETE/INSERT/SELECT/UPDATE) / is_security_definer: true / trigger 정상

**JS 본진 (`js/notifications.js`, ~400줄, 신규):**
- `window.Notifications` 네임스페이스
- `load / setBadge / markAsRead / markAllAsRead / toggleDropdown` API
- 60초 폴링 (Realtime은 Phase B 검토 — 위험 최소화 본진)
- PC 우상단 absolute 드롭다운 + 모바일 풀스크린 시트
- App Badge API 자동 호출 (Chrome/Edge + Windows 11 작업표시줄 숫자 배지)
- C영역 c-home·c-board `.notify-mine` 카드 자동 갱신

**UI (`app.html`):**
- script 추가 (db.js → auth.js → **notifications.js**)
- `onNotifyClick` 빈 핸들러 → `Notifications.toggleDropdown()` 호출

**Git 상태:**
- 브랜치: `feat/notifications-phase-a-v1.1` (commit `7a834d6`)
- main merge 안 함 (B안 결재 = 월요일 신버전 작업 시 통합)

### 2. PDF 460건 OCR 인프라 — 검색 정합도 99.8% (어제 67건 격차 정정)

**어제 인계 노트 격차 발견:**
- 어제 인계 노트: "67 PDF 박힘. 나머지 ~395 = Google Drive 추정"
- 실제 회사 PC sosiggi 폴더: **460 PDF 통째 박혀 있었음** (어제 작업 = 집 PC, 본 세션 = 회사 PC. work folder/는 .gitignore 박혀 git tracked 안 됨 = pull 박지 X)
- → **6.87배 누락 인지**

**본 PC 재실행 결과:**

| 단계 | 결과 | 산출물 |
|---|---|---|
| inventory | 460 PDF (생명 267 + 손해 193, 2025년 333 + 2026년 127, 3GB+) | `inventory.json` |
| extract (pdf-parse) | 2.1분 / 461 JSON / 검색 정합도 85.4% (텍스트 361 + 혼합 32 + 이미지 67) | `extracts/pdf_001~460.json` + `_summary.json` |
| OCR (Google Vision) | 66/67 성공 (11.7분, 약 97만 자 추가, 1건 = 40MB 초과 거부) | `extracts/_ocr_summary.json` |
| CSV + SQL | 14.66 MB (chunk 10개) | `csv/newsletters_*.{csv,sql}` |

**최종 검색 정합도: 99.8%** (459/460)

**OCR 비용:**
- 1,122 페이지 처리 / 무료 한도 1,000 / 초과 122 = **$0.183 (약 251원)**
- 새 가입자 $300 크레딧 안 차감 = 실질 0원
- 매월 신규 자료 OCR = 무료 한도 안 (월 ~100 페이지 추정) = 0원

**OCR 스크립트 격차 정정 사례:**
- 첫 시도 = 30페이지 한 번 요청 = 67건 전부 실패 ("At most 5 pages in one call please")
- 수정 = 5페이지씩 나눠 호출 + 결과 합치기 → 66/67 성공
- 실패 1건 = KB손보 GA희망드림 2504.pdf (35.8 MB, base64 변환 후 40MB 한도 초과)
  - **A안: 빼고 진행 (99.8% 검색 가능)** ← 팀장님 결재 권장 안

### 3. 검색 페이지 작성 (`pages/search.html`, ~700줄) — 본인 본진 6건 통째

- 씹기 카드 (한 입에 들어오는 컴팩트 카드)
- 줍기 모드 (localStorage 저장, 향후 Supabase `saved_newsletters` 테이블)
- 헛웃음 라벨 ("📦 12건 박혔어요" 같은 톤)
- 카테고리 색 5px 줄 (생명=파랑 / 손해=주황 / 기타=보라)
- 인라인 더보기 (페이지 이동 없이 같은 화면 본문 확장)
- 카톡 공유 (Web Share API + 클립보드 fallback)
- Supabase `search_newsletters_grouped` RPC 호출 본진
- 검색 칩 8종 (간편/한도/암/실손/흥국/한화/단기납/청약)
- URL `?q=` 자동 검색 (공유·새로고침 본진)
- `app.html` B영역 사이드바 "📚 소식지 검색" 메뉴 추가

**Git 상태:**
- 같은 feature 브랜치에 별도 commit (`bf4262c`)
- 라이브 안 올림 (B안 결재)

### 4. 디자인 시스템 시안 2건 검증 — 인디고+무채색+다크 적용 확인

팀장님이 `upgrade_20260521/` 폴더에 시안 2건 추가:
- `mock_dbang2_4team.html` (516줄, ~870KB)
- `navi02.html` (518줄)

**디자인 시스템 spec v1 적용 확인:**
- ✅ 다크 모드 기본 (`data-theme="dark"`)
- ✅ 인디고 강조 (`--ac: #6366F1`)
- ✅ 무채색 골격 (`--bg: #0B0C0E` / `--s1: #141518` / `--s2: #1C1D21`)
- ✅ Pretendard 단독 (CDN 로드)
- ✅ 60-30-10 색상 비율 (무채색 90% + 강조 10%)
- ✅ 사이드바 접기/펴기 (collapsed)
- ✅ 카테고리 토큰 (`--t-uw` 인수 / `--t-product` 상품 / `--t-event` 이벤트)
- ✅ html2canvas 라이브러리 (카드 캡처용)

→ 디자인 시스템 spec v1 정확히 적용. 월요일 신버전 작업의 색·구조 기준점 확정.

---

## 본 세션 핵심 결정 6건

### 1. 작업 어휘 정정 — "박힘/박음/박는" 시리즈 금지 (메모리 강화)

본 세션 후반 팀장님 명시:
> "박는다는 말 좀 빼고 쉽게 설명해주면 안될까?"
> "진짜로 개발자들은 저런식으로 대화하고 일을 하는거야? 저건 무슨 별세상 단어와 말 같은데."

→ 본 세션 후반부터 평이한 한국어로 응답 전환. 메모리 `feedback_avoid_bonjin_bakum` 본진 강화 박힘 (다만 본인 인계 노트 어휘 일부 잔존 — 본인 본진 자동 어휘).

### 2. 알림 + 검색 = B안 결재 (라이브 안 올림 → 월요일 신버전 통합)

3안 중 결재:
- A안: 라이브 먼저 올림 → 월요일에 신버전으로 다시 만듦 (두 번 작업)
- **B안 (결재): 라이브에 안 올림 → 월요일 신버전 작업 시 같이 함**
- C안: 라이브 + 신버전 폴더 양쪽 유지

이유: 어차피 월요일 디자인 전면 갈아엎으니 지금 라이브 올리면 두 번 일.

### 3. 업그레이드 폴더 전략 확정

- `upgrade_20260521/` 폴더에 인덱스부터 모든 페이지 새로 만듦
- 그 안에서 검증
- **월요일 (5/25) 저녁** 한 번에 신버전으로 전환
- 안전 + 검증된 방법. 라이브 영향 0.

### 4. OCR = Code 직접 (Make 불필요)

팀장님 어제 의도 = "Make + OCR API 자동화". 어제 Code 격차 = OCR 없이 pdf-parse만.

본 세션 정정:
- Code가 Google Vision REST API 직접 호출 가능 = Make 불필요
- 1회 처리 = 오늘 완료 (460건)
- 향후 지속 자동화 = Supabase Edge Function (별도 트랙, 월요일 신버전 이후)
- Make 비용 절감 (월 $9~16)

### 5. 6월 15일 Anthropic 정책 변경 = onesecond 비용 영향 0

Claude AI 웹 검색 결과 (5/14 발표):
- 대화형 사용 (터미널·IDE 직접 사용) = 영향 없음
- 자동화 (Agent SDK / CI / cron) = 별도 크레딧 ($20~200 구독 안)
- onesecond는 Claude API 호출 코드 0줄 = **영향 없음**
- 미래에 Claude 기능 추가 시 그때부터 비용 발생 (Haiku 모델 + 배치 = 월 1만건도 1만원 미만)

### 6. OCR 자동 카드 = 디자인 배포 후 별도 트랙

웹 작업지시서 답변 (Code 권장 = (b) 디자인 배포 후 별도 트랙):
- 현재 OCR 자동화 인프라 0건 (Supabase Edge Function 신규 작성 필요)
- 월요일 배포 = 디자인 작업만으로도 분량 큼
- OCR 자동화 = 별도 인프라 + 검증 트랙
- 단계: 월요일 = 디자인 + 수동 처리 460건 / 그 후 = Edge Function 자동화 + 작성자 보정 UI

---

## 본 세션 산출물 정리

### Git 박힌 자리 (feature 브랜치, main merge 안 됨)

```
feat/notifications-phase-a-v1.1 (origin push 완료)
├─ commit 7a834d6 (알림 인프라)
│  ├─ docs/migrations/2026-05-21_notifications_setup.sql (210줄)
│  ├─ js/notifications.js (400줄, 신규)
│  └─ app.html (2곳 수정)
└─ commit bf4262c (검색 페이지)
   ├─ pages/search.html (700줄, 신규)
   └─ app.html (사이드바 메뉴 1건 추가)
```

### work folder/ 박은 자리 (.gitignore, git tracked 안 됨)

```
work folder/pdf_newsletter/
├─ package.json + node_modules/      (pdf-parse 라이브러리)
├─ inventory.json                    (460 PDF 메타데이터, 30 KB)
├─ extracts/
│  ├─ pdf_001~460.json               (텍스트 + OCR 결과, ~30MB)
│  ├─ _summary.json                   (검색 정합도 85.4%)
│  └─ _ocr_summary.json               (OCR 통계, 66/67 성공)
├─ migrations/
│  ├─ 01_newsletters_schema.sql      (DB 스키마, RLS, FTS 인덱스)
│  └─ 02_search_rpc.sql                (검색 RPC 2종)
├─ csv/
│  ├─ newsletters_import.csv         (14.65 MB, Dashboard CSV import 본진)
│  ├─ newsletters_lite.csv            (메타만)
│  └─ newsletters_insert.sql         (14.66 MB, chunk 10개)
└─ scripts/
   ├─ 01_inventory.mjs                (sosiggi 메타 추출)
   ├─ 02_extract_all.mjs              (pdf-parse 텍스트 추출)
   ├─ 03_build_csv.mjs                (CSV + SQL 생성)
   └─ 05_ocr_image_pdfs.mjs           (Google Vision OCR, 5페이지 chunk)
```

### upgrade_20260521/ 박은 자리 (untracked, 디자인 시안)

```
upgrade_20260521/
├─ 01_디자인_시스템_spec_v1.md         (인디고+무채색, 다크 기본, Pretendard)
├─ 02_검색_아키텍처_spec_v1.md         (이중구조, OCR 파이프라인, 인덱스 분기)
├─ 03_보험판구글_비전_v1.md             (검색 = 제품 심장)
├─ mock_dbang2_4team.html              (단톡방2 4팀용 시안, 870KB)
└─ navi02.html                         (네비방2 시안)
```

### .env.local 박은 자리 (untracked, 비밀 키)

```
SUPABASE_URL=https://pdnwgzneooyygfejrvbg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJh...
GOOGLE_VISION_API_KEY=AIzaSyAW_eCdfKbTPtyFCY_G9bafjVTGdCRly_k (본 세션 추가)
```

---

## 미해결 / 결정 대기 (다음 세션 인계)

### 1. KB손보 GA희망드림 2504.pdf — OCR 실패 1건 처리

- 35.8 MB → base64 변환 시 47 MB → Google Vision 40MB 한도 초과
- 3안: A) 빼고 진행 (99.8% 검색 가능, **추천**) / B) 페이지 분할 후 재시도 / C) Cloud Storage 사용

### 2. 보험료표 OCR 정확도 측정 (Claude AI 웹 작업지시서 § 1-(4))

- Google Vision = 줄 단위 텍스트 추출. 표 셀 좌표 정보는 응답에 있으나 행/열 구조 자동 복원 안 됨
- 보험료표 PDF 1~2장 실측 의뢰 가능 (Code가 460건 중 골라서 측정 가능)

### 3. 검색기 UI 최종 위치

| 안 | 위치 | 본진 |
|---|---|---|
| 현재 | 별도 페이지 (`pages/search.html`) + 사이드바 메뉴 | 본 세션 작업 결과 |
| 본인 본진 (Claude AI 웹) | 헤더 상시 검색바 + ⌘K 전역 오버레이 | spec § 7 |
| 추천 | 헤더 검색바 + ⌘K + 별도 페이지 = 결과 페이지 | 양쪽 통합 |

월요일 신버전 작업 시 확정.

### 4. OCR 자동화 인프라 (디자인 배포 후 별도 트랙)

- Supabase Edge Function 작성 (업로드 시 자동 OCR)
- 작성자/매니저 보정 UI ("확인 전" 표시)
- 클라이언트 측 OCR (API 키 노출 위험 = 권장 X)

### 5. 디자인 spec 본 자리 격차 정정

- `02_검색_아키텍처_spec_v1.md § 3` = "Make + OCR API" → "Code 직접 / Supabase Edge Function" 양쪽 박는 본진
- `02 § 2` = 단체방 대화 검색 제외 vs 네비방 대화 포함 차이 근거 (팀장님 확정 필요)
- `02 § 5` = 허브 승격 = 관리자 수동 확정 여부 (팀장님 확정 필요)
- `01 § 7` = 토글 상태 저장 = localStorage vs DB

### 6. mock_nav02.html — Claude AI 웹이 만든 시안 (디자인 확정 후 전달 예정)

본 세션에서 Claude AI 웹이 다른 시안 작업 중이라 인지. GitHub에는 아직 없음.

---

## 향후 작업 흐름 (월요일까지)

### D-day = 2026-05-25 (월) 저녁 — 신버전 전환

```
[지금 ~ 일요일]
- 디자인 회의 진행 (팀장님 + Claude AI 웹)
- 시안 추가 (mock_*.html, navi*.html)
- spec 3건 보강·정정
- Code는 대기 (필요 시 grep / 자료 추출 등 보조)

[월요일 새벽 ~ 저녁]
- upgrade_20260521/ 폴더에 인덱스부터 모든 페이지 새로 만듦
- Phase A 알림 시스템 이식 (feat/notifications-phase-a-v1.1 commit 7a834d6)
- 검색 페이지 이식 (commit bf4262c) + 인디고 색 적용
- Supabase newsletters 테이블 + 460건 INSERT (Chrome AI 의뢰)
- 브라운 하드코딩 14곳 전수 제거 (app.html + pages/*.html)
- tokens.css 인디고+무채색 재설계
- 다크/라이트 토글 신설
- 검증 (Netlify staging URL 또는 로컬)

[월요일 저녁]
- main merge → 라이브 전환
```

### 디자인 적용 시 브라운 하드코딩 14곳 (인디고 전환 대상)

- `meta name="theme-color"` 1곳 (`#8B6F47`)
- 헤더/알림 배지 4곳 (`#A0522D`, `rgba(160,82,45,*)`)
- B영역 사이드바 5곳 (그라데이션 `#A0522D → #8B4423`, 박스섀도우)
- Quick 메뉴 1곳
- C영역 알림 카드 2곳

상세 위치 = `app.html` grep 결과 (본 세션 중 Claude AI 웹에 전달).

---

## 잠재 회귀 신호 (마스터 § 14 정합)

### 본 세션 = 회귀 신호 0건. 잠재 위험 3건:

1. **work folder/ git tracked 안 됨**
   - 본 세션 산출물 (460건 OCR 데이터 = 14.66 MB) 본인 결재 안 되면 git 보관 안 됨
   - 다른 PC 진입 시 다시 작업 필요 (어제 67건 격차 = 같은 본진)
   - **방지책:** 월요일 신버전 작업 시 Supabase newsletters 테이블 INSERT 박은 자리 = 진실 원천 박힘. 이후 work folder/ 손실되어도 DB 박힌 자리 박혀 안전.

2. **feature 브랜치 보관 중 (`feat/notifications-phase-a-v1.1`)**
   - main merge 안 함 (B안 결재)
   - 월요일 신버전 작업 시 코드 이식 본진. 단순 cherry-pick 가능 (commit 2건 박힘).
   - **방지책:** 본 인계 노트에 commit hash 명시 박혀 = 본인 본진 정합.

3. **본인 어휘 자동 박힘 ("박힘/박음/박는")**
   - 본 인계 노트 작성 시 본인 어휘 일부 잔존
   - 메모리 `feedback_avoid_bonjin_bakum` 본진 강화 박혔으나 본 어휘 = 본인 자동 어휘
   - **방지책:** 다음 세션부터 본인 응답 본진 평이한 한국어 본진 강제. 인계 노트 작성 시 자동 어휘 박힐 가능성 인지.

---

## 다음 세션 진입 1순위 (권장)

1. **CLAUDE.md 절대 프로토콜 확인** — 마스터 전략 + _INDEX.md + 본 인계 노트 통째 읽기
2. **디자인 회의 진행 상황 확인** — Claude AI 웹 작업 결과 박은 자리 인지
3. **upgrade_20260521/ 폴더 박은 자리 시안 인지** — mock_*.html, navi*.html 신규 추가 여부
4. **월요일 신버전 작업 진입** — Phase A 알림 + 검색 페이지 이식 + 브라운 하드코딩 전수 제거 + Supabase newsletters 460건 INSERT

---

**Code(Opus 4.7 1M) 작성 본 인계 노트 끝.** 본 세션 = 알림 시스템 인프라 박은 자리 + PDF 460건 OCR 99.8% 박은 자리 + 디자인 시안 검증 박은 자리 = 월요일 신버전 작업 본진 기반 박힘. 다음 세션 진입 시 본 노트 통째 인지 후 디자인 확정 + 신버전 작업 진입 권장 🌅
