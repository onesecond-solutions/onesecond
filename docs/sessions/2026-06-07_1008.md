---
세션 종료 시각: 2026-06-07 10:08 KST
주요 마감 작업: 카드 v2 안정화(첨부·생성 복구 + 딸깍 표 디자인 + 둥근 모서리 배포·검증) + 지식엔진 사이클1(채굴 293·검수 276) + 사이클 운영체계 셋업
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-06 저녁 ~ 2026-06-07 오전 (Code 단독, 초장시간 마라톤). 두 트랙 병렬 — **① 카드 v2 안정화·재설계**(PR #451~#462) + **② 지식엔진 사이클1 채굴·검수**(PR #457·#460, 백그라운드 워커 12대). main 직접 push 0건(세션노트 제외), DB 적재 0건.

## 🔝 다음 세션 1순위 후보 (전부 결재 대기)
1. **카드 경로 통합** — 딸깍(사진 라이트박스)·MY SPACE 카드 만들기를 satori 파이프라인 하나로 라우팅 → 옛 `gemini-card`+`.thecard`(html2canvas) 정리. 팀장님이 직접 짚은 "카드 디플로이 1개 공통". (주의: 딸깍은 이미지→카드라 quick-card 이미지 입력 경로 확인 필요)
2. **용어 검수 후속** — 하·오류 30건 수정 / 하·확인불가 73건 웹 2차 검증 / 미검수 17건 보충 / 승인분 적재 SQL 생성(실행=팀장님)
3. 부족분 7건 채굴 (293→300)

## 완료 — 카드 트랙 (PR #451~#462, 전부 머지·라이브)

### 첨부·생성 버그 복구
- **#451** 카드 v2 B/C 디자인 선택 UI 복원 + v2 실패 재시도 버튼 (#449가 제거한 것 부분 복원, html2canvas 폴백은 제거 유지 = 서버렌더 전환 목적).
- **#453** 파일 첨부 무반응 1차 시도 — `<label for>`+중첩 id 중복 제거 (실제 원인 아니었음).
- **#454** 화면 진단 코드(임시) → **#455** **근본 수정**: 일부 브라우저가 PNG `file.type`을 빈값으로 주고 파일명도 확장자 없음 → MIME·확장자 판별 둘 다 실패 → **첫 16바이트 매직넘버(PNG 89504E47 등)로 판별** 폴백 추가. → **#456** 진단 코드 제거. (크롬 PART1 검증: PNG 선택/드롭·TXT 통과, PDF는 더미라 조건부)
- **#452** "AI 카드 만드는 중" 로딩에 물결 점 3개(기존 cardDotWave 재사용).

### 카드 v2 딸깍 표 디자인 재설계
- **#458** quick-card 추출 `lines[]`→`brand`+`rows[{k,v,big}]` / satori-render 레이아웃 재작성(컬러 헤더+항목:값 표+서명+푸터, A=인디고 B=그라데이션 C=다크, rows없으면 lines 폴백) / app.html 빈결과 체크 rows.
- **#459** 🚨 **회귀 복구**(크롬 발견): #458이 app.html(rows 기대)만 머지·Edge Function 미배포 → 라이브 quick-card는 옛 lines 반환 → "추출 못함" 오판으로 **카드 생성 깨짐**. app.html을 rows·lines 둘 다 수용으로 완화 + satori 푸터 ※ 제거.
- **#461** 둥근 모서리 검정 비침 차단 — 카드는 borderRadius인데 PNG는 사각 → 모서리 투명이 어두운 배경서 검게 비침. **외곽 불투명 field 래퍼**(연회색 A#f1f5f9·B#eef2f7 / 다크 C#070b16)로 감싸 투명 0 + 네 모서리 라운드 노출.
- **#462** satori 렌더 폭 360→392 (field 여백 32 정합, 우측 클리핑 해소).

### 배포·검증 (Edge Function)
- quick-card·satori-render **재배포 완료**(임시 PAT 2회 발급·사용·**폐기 완료**). 라이브 = 새 표 디자인 + 둥근 모서리 + 한글 정상.
- 🚨 **satori 한글**: UTF-8로 보내면 정상 렌더. **Windows 셸 curl은 한글 깨짐**(테스트 시 `--data-binary @file.json` 필수). 로컬 satori 렌더(resvg-js) = 라이브(resvg-wasm) 동일 결과(paths 출력).

## 완료 — 지식엔진 사이클1 (PR #457·#460)

- **#457** 사이클 운영체계 셋업: `docs/decisions/2026-06-06_knowledge_cycle_v1.md`(1사이클=300, 수집→검수→눈검수→적재→학습일지) + `AGENTS.md`(팀장코드=검수자·문서관리자 / 직원=Haiku worktree 수집 / 웹=전략 / 크롬=검증) + `docs/data/terms_draft/README.md`.
- **#460** 사이클1 산출물: **채굴 293건**(terms_01~06, 계약45·보장43·인수53·판매45·민원50·제도57) + **검수 6배치**(review_batch01~06, Sonnet 워커 외부 출처 대조) + **review_summary.md**(4분류 통합).
- **검수 집계(276건)**: 상70·중103·하오류30·하확인불가73. **적재 후보(상+중) 173(62.7%)**.
- **하·오류 30건** = 정의 오류 확정(고지의무 이중부정 / BMI 18.5~25 / 대위변제→보험자대위 / 승환계약↔계약전환 / 비과세 소득세법§25 등) — summary §2에 사유·수정 완비, 팀장님 눈검수용.
- **하·확인불가 73건** = 정의 무난하나 공식 출처 직접 확인 실패(WebFetch 차단) — summary §3, 웹 2차 검증용. 공식 출처 재검수 시 승인율 상승 예상.

## 🚨 발견 / 학습
- **하위 에이전트 = CLAUDE.md START PROTOCOL 하이재킹**: Haiku 워커 절반이 채굴 대신 세션 브리핑 반환. **"프로토콜 무시 + docs 읽기 금지 + 직접 파일 저장" 강화 프롬프트가 워커 표준** (재발 방지).
- **앱·Edge Function 동시 반영 필수**: app.html만 머지하고 함수 미배포 → 스키마 불일치로 라이브 깨짐(#459). 카드/함수 변경은 머지+재배포 묶음.
- **PNG 둥근 모서리 = 외곽 불투명 배경 필수**(투명 모서리는 어두운 배경서 검게 비침).
- **파일 형식 판별 = MIME·확장자 불명 시 매직넘버 폴백**(브라우저별 file.type 빈값 대응).
- **satori 폭 = 래퍼 추가 시 width 동기화**(클리핑 방지).
- 워커 자가집계 불신(배치1 자가숫자 오류) → 검수자가 표 직접 재집계.

## 미해결 / 이월
- 카드 경로 통합(딸깍/MY SPACE → satori) 미진행 — 다음 세션 1순위 후보.
- 용어 하·오류 30 수정 / 하·확인불가 73 웹검증 / 미검수 17 보충 / 적재 SQL — 전부 별도 결재.
- 부족분 7건 채굴(293→300).
- 채굴 초안 카테고리 중복(피보험자·약관·보험료 등) → 적재 단계 dedupe.
- `_scratch/`(satori 로컬 테스트 node_modules) 잠긴 핸들로 미삭제 — untracked, 커밋 무관, 다음에 정리.
- (이월) stale `feat/report-button-header` 정리.

## 현재 데이터 상태
- knowledge_entries **72건 변동 없음**(이번 세션 DB 적재 0). knowledge_synonyms 7행.
- 용어 초안 293건 = `docs/data/terms_draft/`에 ai_draft 상태(검색 비노출, DB 미적재).
- 라이브 카드 만들기 = 새 표 디자인 + 둥근 모서리 (satori-render·quick-card 재배포 반영).
