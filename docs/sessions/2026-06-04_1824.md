---
세션 종료 시각: 2026-06-04 18:24 KST
주요 마감 작업: 사이트 오픈(메인터넌스 해제) + 로그인 대수술(Google/가입 복구·PKCE·세션만료 레이스) + 글쓰기 삽입 도구막대(이미지·표·캘린더) + 보험 지식 엔진 v0 설계·준비
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-04 오후~저녁 (직전 12:01 종료 후 12:34 /session-start로 이어서, Code 단독). PR #403~#418 (#407 제외 머지) + 메인터넌스 해제로 4팀 실사용 개시. 장시간 마라톤.

## 🔝 다음 세션 1순위 (팀장님 지정)
**PR #418 준비 상태 확인 → 샘플 30건 채굴 실행 여부 결정.** 실행 승인 시 3단계: ① 테이블 SQL(`docs/migrations/2026-06-04_knowledge_entries.sql`) ② `extract-knowledge` 함수 배포(Verify JWT OFF) ③ 샘플 30 채굴 1회 호출 → 11지표+실패로그 보고. **조건 8개 준수**(사용자/검색 미연결·ai_draft만·service_role 내부만·CRON_SECRET·실패로그·중복방지·샘플후보고·전체확장 별도승인). 이후 전체 492+카톡+네비방 = 별도 승인.

## 완료 (라이브)

### A. 사이트 오픈 + 로그인 대수술 (#406, #408~#416)
- **메인터넌스 해제**(#406): MAINTENANCE_MODE=false → 4팀·일반 개방.
- **로그인 점검 결과**: 공사중 스텁이 남아 Google 로그인·GA 가입 먹통이던 것 발견.
  - **Google 로그인 복구**(#408 스텁 해제 → #410 토큰 조기저장(레이스) → #411 토큰없는401은 세션만료 처리 금지 → #413 홈 "로그인 후 표시됩니다" placeholder 캐싱 수정 → **#415 PKCE 구현**(code_challenge 강제+?code 교환, implicit 폴백) → #416 비로그인 빈 Bearer 미전송(401 Empty JWT 제거)). 진단로그 #409·#414 추가→#412/#415에서 제거.
  - **GA 가입 복구**(#408): doSubmit의 osShowMaintenance()+return 제거 → 실제 가입 로직 부활.
- **결론**: 이메일 OTP 로그인 ✅(서버 200) / Google 일반 사용자 ✅ / **어드민만 Google X** = 어드민 이메일이 이메일계정+Google 동일이메일 **충돌**(Supabase "Allow manual linking" OFF). → **어드민은 이메일 OTP 사용**(또는 manual linking ON 시 Google도). Google 버튼은 켜둠(일반 사용자 정상이라).

### B. 글쓰기 삽입 도구막대 (#403·#404·#405, #407미머지)
- 본문 오른쪽 세로 도구막대 = **이미지·표·캘린더**. MY SPACE(스크립트·자료·단체방)+매니저룸, **메모·보험Q&A 제외**.
- **블록 방식**(본문 textarea 유지): 표·캘린더를 **html2canvas로 이미지화→기존 첨부 주입** = 저장·뷰어 코드 무변경·안전(비교표/카드 패턴). 표 빌더(셀·행·열), 캘린더 빌더(월 달력·날짜·메모). _activeWriteFileId/_injectFileToInput.
- **매니저룸 글쓰기 타이틀**(#405): "MY SPACE에 저장"→진입점별("매니저룸 글쓰기"/"MY SPACE에 저장"). **구조 확인**: 매니저룸 오피스=owner_id 본인 개인 scripts/library = MY SPACE와 같은 개인 저장소 공유(의도). 스크립트/자료=개인MYSPACE, 단체방=팀 저장.
- **전산신고 버튼**(#407 ⚠️**미머지**): 헤더 알림벨 왼쪽, 클릭→함께해요 '전산 이상 신고' 글쓰기. openTogetherWrite(preCat) 사전선택. → **다음 세션에 검토·머지 필요.**

### C. 보험 지식 엔진 v0 — 설계·준비 (#417 설계, #418 준비)
- 팀장님 "코드를 보험 전문가로" → 외부 조언(5단계) + Code 보강: **AI가 머리로 쓰지 말고 실데이터(소식지·카톡·네비방)에서 추출 + 전문가 검증 + 실데이터=진실원천**(환각방지).
- **실데이터 감사(A등급)**: 소식지 **492**(본문490·OCR done 492·에러0·진성중복0·평균15,767자·64개사·텍스트78%/OCR17%/혼합3%/이미지2%) / 카톡 **약 10,274 메시지**(메인 17,786줄, export 3개 중복정리 필요) / 네비방 **6,889** / 검색 FTS+RPC ✅(미연결).
- **4테이블 모델(source_type/source_id 일반화)**: runs(실행1)→run_items(원본1)→errors(실패1)/entries(지식1). 전부 run_id·run_item_id·source_type·source_id로 추적. knowledge_entries 보조메타(source_title/company/date/page·canonical_company_name). **5소스(newsletter/chat_log/navigation/post/admin_note) DB변경 없이 수용**. 6질문(run/원본/유형/ID/메타/실패단계) 전부 답변 검증 완료.
- **extract-knowledge Edge Function**: newsletters full_text 읽기→Gemini 추출(용어/상품/보험사/시나리오, 본문근거 강제)→ai_draft 저장. x-cron-secret, service_role 내부, 회사명 canonical, 단계별(fetch/prompt_build/gemini_call/json_parse/validation/insert) 실패로그, run_item 추적. **미배포·미실행(준비만)**.

## 미완 / 다음
1. **(1순위) PR #418 샘플 30 채굴 실행 결정** → 11지표 보고 → 전체 확장 별도승인.
2. **PR #407 전산신고 버튼 미머지** — 검토·머지.
3. 어드민 비밀번호 설정(선택, 콘솔 스니펫 제공·미실행) / Google 어드민 충돌(원하면 Supabase Authentication "Allow manual linking" ON).
4. 카톡 export 3개 중복 정리(전체 채굴 전).
5. **CRON_SECRET 채팅 노출**(=onesecond-cron-2026-newkey-abc123) → 한가할 때 로테이션(send-push·charge-subscriptions·extract-knowledge 영향).
6. 알림 send-push 자동발송=morning 완료 / 본인글 제외(exclude_user) 미적용.

## 격차 / 학습
- **SQL 라벨링 약속**: 앞으로 SQL 줄 때 🟢읽기전용(안전) / 🟠데이터변경(확인) 표시. (팀장님 "읽지도 않고 실행" 우려 — 감사 SQL은 전부 SELECT라 무해였음.)
- **로그인 5~6회 추측수정 후 콘솔 로그로 근본 확정**(레이스→PKCE→어드민 충돌). 추측 강행보다 **진단 후 확정**이 옳음. auth는 민감 → 단계적·검증 후.
- **지식엔진 = 실데이터 grounding + ai_draft 게이트 + 4테이블 일반화 + 실행 전 보고(조건 10)**. 측정 목적이라 실패 데이터가 성공만큼 중요(error 테이블 10필드).
- 모바일 헤더 hbtn은 숨김(벨·전산신고 동일) — 모바일 노출 필요 시 별도.

## DB (이번 세션)
- 신규 테이블 **미생성**(지식엔진 준비만). 감사 = **읽기 전용 SELECT만**. 코드 수정 위주(로그인·도구막대·메인터넌스 플래그).
- (morning 완료분: board_reads/push_subscriptions/notify_new_post 트리거/베타공지 이모지·7661 안내공지.)
