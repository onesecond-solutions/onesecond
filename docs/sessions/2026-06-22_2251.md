---
세션 종료 시각: 2026-06-22 22:51 KST
주요 마감 작업: WAV 코덱 원인확정+MP3 파생본 재생(PoC), 다운로드 버튼, 팀 공유 dual-write+source_id text 정정
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-22 저녁 (22:51)

> Code 단독=총괄팀장. 제품 머지=총괄팀장 실행(대표 결재). DB/Edge/Storage=대표·검수팀 직접. 신버전 `pdnwgzneooyygfejrvbg`.
> ⚠️ 별도 AI 노트 미전달 → 총괄팀장(Code) 직접 작성. 직전 세션(16:34) 이어서 저녁 진행.

## 오늘 마감 (PR 전부 머지·라이브)

### 1. WAV 재생 실패 — 원인 확정 + 진단 종료
- **#917** WAV 진단 인증 경로 정정: `_svSignUrl`을 raw 토큰 직접읽기 → **`window.db.fetch` 경유로 통일**(apikey+Auth 자동주입·401 자동갱신). 진단에 `_wavEnsureToken()`(os_token 없으면 os_refresh_token으로 1회 갱신·세션 없으면 중단). 토큰 원문 비노출. Chrome 1차 A안 통과 후 머지·라이브.
- **원인 확정(라이브 진단)**: `보장분석 건강 1시간도입.wav` = **FORMAT 49(GSM 6.10) / audio.error.code 4 / 0:00** → 브라우저 GSM 디코딩 불가 = **코덱 문제**(URL·CORS·서명 아님). 진단 도구는 admin/임태성 게이트로 유지(파이프라인 안정화 후 제거 재검토).

### 2. WAV→MP3 파생본 재생 — 샘플 1건 PoC (임태성 게이트)
- 로컬 `ffmpeg`(Gyan.FFmpeg 8.1.1) 설치 → 샘플 WAV 1건 변환(`ffmpeg -ac 1 -c:a libmp3lame -b:a 48k`), **GSM→MP3 1:15:07 정상 디코드 확인**(원본 WAV 불변).
- DDL: `myspace_files.playback_path text` nullable 추가(운영).
- MP3를 원본과 같은 Storage 폴더에 `..._1.play.mp3`(ASCII 키)로 업로드(별도 myspace_files 행 X) → 그 1행에 playback_path 연결.
- **#920** 검색·자료실 미리보기에서 `playback_path` 있으면 MP3 우선 재생(단일 게이트 `_wavPlaybackEnabled()`=임태성 user_id `98c5f4f9-…`만). 그 외/없음=기존 WAV. 머지·라이브.
- **라이브 실증 성공**(임태성 계정 재생). 초기 "재생 불러오지 못함"=playback_path 값에 공백 끼임(STEP4 붙여넣기 artifact)→`storage.objects` 실키로 UPDATE 정정 후 정상. ⛔ 전체 개방·일괄 변환·자동변환=별도 결재 전 금지.

### 3. 다운로드 버튼 — #919
- 검색 미리보기 상세 `원본 열기` 옆 **`⬇ 다운로드`**(`_dlSignedFile`: `_svSignUrl`→blob→`<a download>`, original_name 유지, WAV·MP3 동일, 모바일 blob, 실패 안내). iOS 15↓는 공유시트 경유(플랫폼 제약). Chrome 1차 A안 통과 후 머지·라이브.

### 4. 스크립트 팀 공유 — dual-write + source_id 다형 정정
- **#918** `_osShareDo`: shares + team_notices(scope=team_internal) **동시 저장**, 실패숨김 제거(상태/body 노출), `window.db.fetch` 경유, 중복클릭 방지, team_notices 실패 시 shares 롤백, 둘 다 성공 후에만 토스트. **지점 공유 버튼 비활성("추후 제공")** + `scope!=='team'` 가드(branch_internal RLS 정책 미확정). DDL: team_notices `source_type`(신규)·`source_id`(기존 uuid 재사용) 확인. 머지·라이브.
- **실측서 버그 발견**: `invalid input syntax for type uuid: "104#"` → **scripts.id·library.id = bigint**(uuid 아님), myspace_files.id=uuid. team_notices.source_id(uuid)에 bigint 못 들어감.
- **A안 채택**: `team_notices.source_id` **uuid→text**(다형 참조). 사전 점검=비-null 0·FK 0·인덱스 0·정책/함수/뷰 참조 0(안전) → DDL 적용(운영).
- **#921** 프론트 정정: script/memo=표시문자 제거→정수→`scripts`/`library` 재조회→**DB 실제 id(bigint) 문자열** 사용. `_shareValidSourceId`(script/memo=`^\d+$`·file=uuid) 검증·불일치 중단. shares.item_id=team_notices.source_id 동일값(text). 롤백 실패도 console.error. 머지·라이브(반영 확인).

### 5. 운영
- **임태성 실장 전체 로그아웃**: auth.sessions/refresh_tokens 폐기(103개 누적 세션 정리). bylts@naver 1계정만, 어드민·타 사용자 영향 0.

## 진행 중 / 다음 세션 인계
1. **★팀 공유 라이브 실측 미완**(대표 퇴근): 임태성 계정으로 스크립트 팀 공유 1건 → 완료기준 1~9(성공토스트/공유함 1건/팀방 1건/source_type='script'/source_id=실제 scripts.id 정수/새로고침 유지/같은 팀 노출/콘솔 0/지점 비활성). #921 라이브 반영됨 → 다음 접속 시 확인만.
2. **⏰ 댓글 본인 수정·삭제(2026-06-23 예약)**: 본인 댓글에만 수정·삭제 버튼(author 게이트)+UPDATE/DELETE RLS author_id=auth.uid(). posts=post_comments / 개인·팀=entry_comments 둘 다. 메모리 `project_comment_edit_delete_own_pending`.
3. **지점 공유 개방(별도 작업)**: branch_internal INSERT 허용 역할 = **admin + 지점장(ga_branch_manager) + 실장(ga_manager)** 결정됨. RLS 정책 + 지점 버튼 재활성을 묶어 별도 진행.
4. (이월) SMS 인증 운영반영 9단계 / 영업노트 import→화면 / 채굴 재가동.

## DDL 적용 기록 (운영, 이번 세션)
- `myspace_files.playback_path text` 추가
- `team_notices.source_type text` 추가(신규) / `source_id` uuid→text 변경

## 주의·교훈
- **스키마 타입 단정 금지**: scripts.id·library.id가 bigint인 걸 모르고 source_id(uuid) 설계→실측서 깨짐. id 타입은 information_schema로 확인 후 매핑.
- **긴 경로 수기 UPDATE 위험**: playback_path 공백 끼임. 일괄 처리 시 `storage.objects`에서 실키 끌어오기.
- **사고 신호 대응**: "source_id 컬럼 없음(No rows)" 떴을 때 즉시 멈추고 프로젝트 확인(행수 18로 운영 확정, false alarm).
- 머지=총괄팀장 실행(대표 통과 결재). DDL·Storage=대표 직접. Chrome 1차는 PR 정적 검수(라이브 불필요).

## 미머지 브랜치
- 이번 세션 신규 미머지 없음(#917~#921 전부 머지). 이월: feat/sms-auth(#887)·feat/verify-identity-fn·결제 #882~886·#802.

상세 PR: #917~#921. 신버전 DB. 라이브=onesecond.solutions.
