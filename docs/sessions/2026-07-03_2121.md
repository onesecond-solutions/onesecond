---
세션 종료 시각: 2026-07-03 21:21 KST
주요 마감 작업: 7월 소식지 23건 등록·공개 완료 + AI 팀 상시 운영 헌장 확립 + CI 마이그레이션 러너 구축(배포는 secret 인증 차단)
생성 도구: Claude Code /session-end (총괄팀장 Code 직접 작성)
---

# 세션 인계 노트 — 2026-07-03 오후~저녁 (총괄팀장 Code 단독)

> 신버전 `pdnwgzneooyygfejrvbg`. main 직접 push 0(전부 PR). 이 세션의 성격 = **운영 실행 통로 확립 + 7월 소식지 공개 + 운영 헌장**.

## 🎯 이번 세션 마감 (증거)

### (1) ⭐ 7월 소식지 23건 등록·공개 완료
- **원인 규명:** 7월이 화면에 안 보이던 이유 = UI 아님, **적재 누락**. Storage 업로드(23/23)까지만 됐고 **create_draft(김실장·기획팀장 등록)가 한 번도 실행된 적 없어** newsletters 행 0. (메모리 published 532/reviewing 2 교차대조로 확정.)
- **복구:** Storage 원본 23건을 **기존 REST 등록 경로**(`scripts/register_newsletters_2026_07.sh`, verify→register→검수→promote)로 reviewing 등록→전건 검수→published 승격. **표준 회사명**(165 표준화 정합·메트라이프/NH농협생명/DB손해보험 등)·생명11/손해12·category·source_path(PDF 열람 서명URL 200).
- **결과:** published **532→555**, reviewing 2(개인문서 불변). 임태성 게이트 소식지 뷰에 7월 노출.
- **실행 채널:** Code + 로컬 `.env.local` service_role **한시** 세션(작업 직후 `rm .env.local`·부재 확인). git bash 이슈 2건 내부 해소(자격 공백분리·한글 본문 400→`--data-binary @file`). PR #1049.

### (2) ⭐ AI 팀 상시 운영 헌장 확립 (모든 규칙 상위)
- 진실원천 `docs/ops/2026-07-03_ai_team_operating_charter.md` + 메모리 `feedback_ai_team_operating_charter`.
- 핵심: **대표를 작업자로 쓰지 마라.** 대표 역할 3가지뿐(방향·위험승인·계정전용 1회). 금지=SQL실행/결과복사/실행자지정/기술선택 요구·"채널없음"으로 대표전환·문제 하나에 신규시스템 확장·재확인. AI가 조사·실행·검수·완료 내부종결. 대표 질문=사업판단 1개(추천안 형식). 보고 6형식. **모든 하위 에이전트 프롬프트 첫머리에 §7 문구 삽입.**

### (3) 감사센터 회사명 표준화 — 부트스트랩·165 표준화 완료
- INVOKER 모델로 재설계(fn_owner 폐기·독립검수에서 RLS 차단결함 발견 후 전환). 대표 대시보드 1회 실행(`db/bootstrap/2026-07-03_audit_center_bootstrap.sql`). ops 스키마·`ac_nlstd_*` RPC 4·매핑 v1(33).
- **165건 company 표준화 execute 완료**(status=executed, changed=165, 되돌리기 대기). newsletters status 건수 불변.

### (4) 잔여 회사명 정제 — RPC·UI 구축 (배포 대기)
- `db/migrations/2026-07-03_resid_fix_rpc.sql`: `resid_fix_preview/apply/rollback`(감사센터 내 1회 고정·INVOKER·is_admin·작업키 고정·company·구분 2컬럼만·개인문서 제외·잔여값 가드·스냅샷 롤백·DOWN). app.html 감사센터에 **미리보기 버튼** 추가. 독립검수 통과.
- 대상: MG*→MG손해보험/손해 · 미상 IBK→IBK연금보험/생명 · 미상 신한라이프(본문근거시)→신한라이프/생명. **라이나·소식지·파일명값=hold**(대표 승인범위).

### (5) CI 마이그레이션 러너 구축·머지 (PR #1048)
- 대표 조건 전건 충족: `workflow_dispatch` 한정(main 머지 자동배포 금지)·Required reviewer=대표·main 게이트·precheck(DOWN/postverify/금지구문/단일트랜잭션 강제)·연결 **PG* env 분해**(argv 미노출·비번 percent-decode)·secret 스크럽·`select 1` 사전검증·fail-stop·fork 차단. 독립검수 5/5 + 10/12→수정 후 전건 PASS.
- **production-db Environment 설정 완료**: 스캐폴딩(gh API: 생성·main 한정·reviewer=onesecond-solutions) + 대표가 `SUPABASE_DB_URL` secret 등록.

## 🚨 현재 차단 (다음 세션 1순위)

**resid-fix RPC 배포가 SUPABASE_DB_URL 인증 실패로 미완 (3연속).**
- 오류: `psql: FATAL: password authentication failed for user "postgres"` (host=`aws-1-ap-south-1.pooler.supabase.com`).
- **DB 반영 0 확정** (연결 실패로 `select 1` 가드가 마이그레이션 전 STOP·부분적용 없음).
- 안전 검증(비밀번호 미노출)으로 **리전(우리 프로젝트 pooler)·user(`postgres.pdnwgzneooyygfejrvbg` 보정)·ref 정상 확인** → 원인을 **비밀번호 값/처리**로 좁힘.
- **안전 진단 main 반영**(`scripts/ci/apply_and_verify.sh`, `DIAG … pw_len / pw_percent(none/has_raw_percent/percentlike) / pw_state(filled/PLACEHOLDER_OR_EMPTY)` — 비밀번호 값 절대 미출력).
- **다음 = 진단 run 1회**(대표 승인 1클릭)로 확정: pw_state=PLACEHOLDER_OR_EMPTY면 secret 미치환/값오류, pw_percent=percentlike면 percent-decode(`_dec`) 제거로 즉시 코드수정. 그 후 배포→감사센터 미리보기→문서별 실 ID·fix/hold 확정→8항목 보고→대표 데이터 버튼→검증→종료.

## 다음 세션 순서 (합의 확정)
`진단 run으로 인증실패 원인 확정 → 수정 → resid-fix RPC 배포(대표 승인 1) → postverify → 감사센터 라이브 미리보기 → 문서별 ID·fix/hold 확정 → 8항목 보고 → 대표 데이터 수정 버튼 1 → 사후검증 → 종료`
- CI 배포 승인(Environment) = RPC 배포 전용(newsletters 데이터 0). 실제 데이터 수정 = 감사센터 승인 버튼 1회로만(2중 게이트).
- 회사명 잔여 정제 완료 후 `_COMPANY_ALIASES` 보강(MG손해보험 등)=별도 후속.

## 머지/브랜치
- 머지: PR #1048(CI 인프라)·#1049(7월/resid-fix RPC·UI)·#1050(pooler user 보정)·#1051(안전 진단). 감사센터 부트스트랩·165 표준화·7월 등록/승격 = 실행 완료.
- 미머지 브랜치: 없음(전부 main 반영). `chore/audit-center-bootstrap-package`·`docs/ai-team-operating-charter` 등은 이력용.

## 교훈
- **service_role 반복 입력 = 헌장 위반** → 지속가능 채널(CI 러너)로 전환이 정답. 단발 자격은 즉시 삭제.
- SECURITY DEFINER + 커스텀 최소역할 = RLS 대상 미포함으로 런타임 차단 → **INVOKER + is_admin 게이트**가 감사센터 표준.
- 실행 통로 없다고 대표를 작업자로 돌리지 마라 → AI가 기존 경로 조사·최소 통로 설계, 불가피한 계정행위만 1회 상신.
- Supabase pooler 연결 = 사용자명 `postgres.<ref>`·연결문자열은 PG* env 분해(argv 미노출)·인증실패는 리전/user/ref부터 안전검증 후 비밀번호로 좁힘.
- 데이터 수정은 미리보기→대표 버튼 2중 게이트로만.
