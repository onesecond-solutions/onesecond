---
세션 종료 시각: 2026-06-10 19:08 KST
주요 마감 작업: 자료함 뷰어·탐색기·이동(Move)·PDF 통일 + 사이드바/홈 단순화 + 다이어리 개편 DDL 준비(reminders 컬럼)
생성 도구: Claude Code /session-end
---

세션: 2026-06-10 오후~저녁 (Code 단독, 총괄팀장). 직전 16:20 노트 이후 이어서. **PR 머지 9건(#530~#537 + #522) + 다이어리 개편 DDL 준비.** main 직접 push 0 / DB=대표님 직접 실행.

## 🔝 다음 세션 1순위 — 다이어리 페이지 개편 1차 PR (DDL 이미 완료)
작업지시서(기획실장, 대표 승인): 대시보드 + 기능형 달력 + 알람. 경로 B로 다음 세션 신선하게 구축.
- **DDL 완료(대표 실행됨)**: `alter table public.calendar_events add column if not exists reminders int[] default '{}';` — 0=정시/10·30·60=N분전/1440=전날, 빈배열=없음. 기존 일정 데이터 보존(추가만).
- **1차 PR 범위**: ① 상단 대시보드 3카드(오늘 일정 N / 공지 N / 답변대기 Q&A N — 실 COUNT, guide 제외, 클릭 연결) ② 하단 기능형 달력(헤더 [오늘][<] ym [>] + 보기탭 [월][주][일][목록]=월만 활성, **기존 renderCalGrid/calendar_events 재사용·보존**) ③ 날짜 클릭→**우측 슬라이드 패널 즉시입력**(제목·시간·메모·알람, **모달 폐기**·팝업 남발 금지), 일정 클릭→같은 패널 수정/삭제 ④ **인앱 알람**(60초 폴링, reminders 시각 도달 시 종 센터 누적+팝업+클릭 일정이동, localStorage 중복방지).
- **2차 PR(분리)**: 주/일/목록 뷰 + PWA 푸시(권한 플로우+SW/푸시서버, §4·인프라·비용 **착수 전 대표 결재**).
- 현 캘린더 구조: `calendar_events`(id·event_date·event_time·title·description·event_type·source_type·insurer_id·author_id+**reminders 신규**), prefix 'mys' 함수(loadCal·renderCalGrid·openCalAdd·saveCalEvent·updateCalEvent·deleteCalEvent). 다이어리 탭=#v-myspace mys-today(mys-cal-area/grid).
- 가드: §4 메타만(고객정보 푸시 금지) / 데이터 파괴 금지 / 하드코딩 0.

## 완료 — 이번 세션 머지 PR (전부 라이브, main 직접 push 0)

### A. 자료함 뷰어·탐색기 (#530·#531·#532)
- **#530** 미리보기 통일 프레임(헤더 고정+stage 내부 스크롤, object-fit contain로 PNG 오버 해결, HWP=미지원카드) + 리스트/그리드 토글(localStorage 'os_mv_view') + '+폴더' webkitdirectory JS 프로퍼티 확정.
- **#531** 윈도우 탐색기 모델: 중앙=상위이동+하위폴더(📁 더블클릭 진입)+파일, 좌측 트리 동기화, 이미지 실제 썸네일(lazy 서명), 용량(그리드 숨김/리스트 끝).
- **#532** PDF 첫페이지 썸네일(lazy pdf.js) + 파일카드 타입무관 유니폼화(onclick·data·menu 공통, 타입분기=썸네일·미리보기만).

### B. PDF 미리보기 — 진단 3연속(★학습)
- **#533/#534**(되돌림): PDF "작동 안됨"을 **CORS로 오진**→iframe 전환(네이티브 PDF.js 툴바 노출=대표 불만).
- **#535**(최종): **curl로 직접 진단** → Storage가 `Access-Control-Allow-Origin:*` 정상 반환 = CORS 안 막힘 확인 → pdf.js 캔버스 렌더(PNG와 동일 프레임, 네이티브 툴바 0) + 호버 돋보기 + **커스텀 확대 모달**(하단 둥근 −/＋/회전/◀ 1/N ▶/딸깍, ←→키·ESC). docx/xlsx/csv/txt도 fetch 정상.

### C. 이동·드롭타겟·사이드바 (#536·#537)
- **#536** 업로드 타겟=**드롭한 위치**(현재폴더 개념 제거, 드롭존·전체→루트/폴더위→그안/계층보존) + 버튼 저장위치 안내 + **이동(Move)** 드래그(Storage 그대로 DB folder_id/parent_id만, 사이클가드) + 우클릭/⋮ 메뉴(이름변경/이동/삭제) + 대상폴더 선택모달.
- **#537** 사이드바·홈 단순화(감추기>만들기): GA 사이드바=홈/라이브러리/스크립트/요금제/(관리자=admin만), 자료실·Quick·현장의소리·함께해요·보험이슈 숨김(`ga-hide` + `#app:not(.is-admin):not(.is-insurer) .nav.ga-hide{display:none}` → GA만, admin·insurer 회귀0), '어드민'→'관리자' 개명·최하단, 홈 포털 5→3카테고리, 배너카드 QUICK/FIELD VOICE 제거→라이브러리/스크립트. **결재 A/B=권장 채택**.

### D. 지식엔진
- **#522** 사이클2 채굴 핸드오프 머지(채굴=김실장/저장=Code, dedup 베이스라인 235건 SELECT). 채굴 재가동=대표가 SELECT 결과를 김실장 전달 시.
- docx/xlsx/ppt 미리보기 = **A(렌더) 유지** 결정(작업지시서 9번 'B카드'와 상충→대표 A 확정).

## DB 현황 (신버전 pdnwgzneooyygfejrvbg)
- `calendar_events`에 **reminders int[] 추가 완료**(대표 실행, 빈배열 기본, 데이터 보존).
- myspace_folders/files/usage + 버킷 myspace + RLS4 / knowledge_logs 1 / knowledge_entries 235 변동없음.
- Storage CORS = `Access-Control-Allow-Origin:*` 정상(curl 확인).

## 미실행 / 대기
- 다이어리 1차 PR(다음 세션) — reminders 컬럼은 준비됨.
- 자료함 10종 형식별 검수(대표 추후 — CORS 정상이라 docx/xlsx/csv도 떠야 함).
- 사이클2 채굴(김실장, dedup SELECT 전달 시).
- menu_settings_by_role DB 테이블(완전 RBAC, 미구축, 별도 결재 트랙).

## 🚨 학습 (★대표 지적 반영)
- **버그 진단은 총괄팀장이 직접(코드/curl/임시로그), 대표께 콘솔 스니펫·fetch테스트 요청 금지.** PDF CORS를 추측 오진(#533)했다가 **curl로 ACAO 헤더 직접 확인**해 정정(#535). 추측 패치 0, 응답으로 원인 확정.
- `<img>`/iframe=navigation(CORS 불필요) vs `fetch()`=CORS 필요 — 타입별 로드 메커니즘 차이.
- 사이드바 role 게이팅: `ga-hide`+`:not(.is-admin):not(.is-insurer)`로 GA만 숨김(admin·insurer 보존), 하드코딩 0.
- 큰 기능(다이어리+알람)은 무거운 세션보다 신선 세션이 품질 유리(경로 B 선택).

## 운영
- main 직접 push 0 / DB=대표 직접 실행 / 모든 함수 window.* / 미리보기·알람=클라 렌더(§4) / 메모리 신설 누적(jsonb CRLF·Storage ASCII키·서명URL /storage/v1).
