---
세션 종료 시각: 2026-07-23 17:39 KST
주요 마감 작업: PR 9건 라이브·병합 — V3 후속 정리 / 홈 보장분석 축 카드 6개 + 빈 페이지 / 고객관리 서버필터·페이지네이션 + 데이터현황 집계 분리 / 지식 카테고리 스펙·레지스트리 설계안·원장 생성(PR-A)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-23

**총괄팀장 Code · 대표 세션 · 데스크톱 앱(`Desktop\임태성` 기본폴더 → onesecond 루트로 진입)**
**PR #1385~#1393 (9건) 전부 병합·라이브 · main 직접 push 0건 · 병합 전 head 검증 9회 전부 일치**

## 이번 세션 한 줄

V3 타이포 트랙의 de-iframe 후속을 닫고, **홈에 보장분석 축 카드 6개(빈 페이지)** 를 세운 뒤, **고객관리 1000행 절단**을 서버필터+페이지네이션으로 해소했다. 이어 기획팀장 **지식 카테고리 레이아웃 규칙 v2**를 받아 대조·저장하고, **지식 문서 공용 레지스트리** 설계안 확정 → **원장 파일 생성(PR-A)** 까지 진행.

---

## ✅ 완료·라이브 (증거)

### 1) V3 de-iframe 후속 정리 — PR #1385 (main `e301e65`)
- **읽는 폭 710은 iframe 제약이 아니라 가독성 표준값**(한글 40~45자/줄)임을 확정. de-iframe 됐으니 760으로 넓혀도 되냐는 대표 질문에 → 760은 44~47자로 상한 초과 → **710 유지**(대표 확정).
- iframe이 실제로 묶고 있던 값은 **사이드바 분기점**이었음: `1060 → 1120`. 1060은 "뷰포트−좌내비212−패딩56" 역산값이라 독립 페이지에선 2컬럼(1036)이 여백 12px로 켜졌음.
- **좌우 거터 24px 복구** — iframe 시절엔 바깥 앱 `.wrap` 패딩이 여백을 대신 줬으나 독립 페이지엔 없어 768~1119 구간에서 본문이 브라우저 가장자리에 붙던 회귀. 간병·암=`.wrap` 808+패딩24(border-box→콘텐츠 760 유지) / **실비만 구조가 달라 `body` 패딩 24**.
- ⚠️**함정**: 2컬럼 구간에서 `.wrap` 좌우 패딩을 0으로 되돌려야 함(간병·암). 안 하면 표가 760→712로 줄어듦.
- 상단 여백 26→32px은 간병·암만. 실비는 `body{padding:1.5rem}` 4방향 동일값이라 26px 기준 자체가 없어 **미적용**(대표 확정).
- **실측(1120 창) 좌우 여백 34.5px** — 세로 스크롤바 15px 때문. 계산상 42px로 보고했다가 정정. **뷰포트 폭 계산 시 스크롤바 15px 차감 필수.**
- 검수: 머지 전 로컬 실렌더로 768/1119/1120/375 실측 → 승인 → 병합 → 라이브 curl 재검증.

### 2) 홈 보장분석 축 카드 6개 + 빈 페이지 — PR #1386·#1388·#1389·#1390
- 최종 형태: 홈 그리드 **맨 앞 두 줄**(3열)에 **의료실비 · 암 · 뇌 · 심장 · 수술비 · 간병 · 기타** 6개. 전부 `_canSeeCoverage`(임태성·admin) 게이트.
- 각 카드 → 독립 빈 뷰(`#v-axis-medical`/`-cancer`/`-brainheart`/`-surgery`/`-caregiver`/`-etc`), 내용 = 제목 + "준비 중입니다."뿐.
- 기존 '보장분석' 허브 타일(`v:'bojang'`)은 **유지**(대표 "일단 15개, 정리는 후속"). 3줄 아래 순서는 대표가 직접 정리 예정 — **총괄이 임의로 손대지 말 것.**
- ⚠️**총괄 실수 박제**: 처음에 4개 카드를 **기존 보장분석 허브 서브(`js/bojang.js` 발송용 화면)에 연결**했다가 대표 교정("내 의도와 어긋나는 일을 벌렸다") → #1388로 독립 빈 뷰 재배선. **교훈 = 새 카드/화면 지시는 "기존 화면에 링크"가 아니라 "자기 페이지를 갖는다"가 기본.** 허브 콘텐츠는 삭제 않고 보존.
- ⚠️`axis-caregiver`(보장분석 축, 게이트) ≠ `caregiver-history`(간병보험 변천사, 독립 URL 전체공개) — **이름 비슷하나 완전 별개**, 홈에 둘 다 노출(대표 확인).
- '기타'는 처음 `disabled`(연결 없음) → 이후 대표 지시로 빈 페이지 연결. 비활성 타일이 0개가 되어 관련 분기·CSS 3줄 제거.

### 3) 고객관리 1000행 절단 해소 — PR #1387 (main `1daffb2`)
- 구조 = **서버 필터로 좁힌 결과를 200행 페이지로**(대표 확정). 상태·이름(`ilike`)·기간(`created_at`)·채널(`profile->>attr`)을 URL 조건으로 내리고 `Range` 헤더 + `Prefer: count=exact`로 총건수 표시.
- **케어 플로우 필터(상령일·생일)는 `birth_date` 월·일 계산이라 서버화 불가** → `status='청약완료'` 고객만 전량 별도 로드해 클라이언트 계산(계층2).
- ⚠️**부작용까지 같이 처리**: `_ciData`가 200행이 되면서 데이터현황(`#ci-stats`)이 그걸 세어 더 부정확해지는 문제 발견 → **집계 분리**(`_ciStatsLoad`가 `select=created_at,attr:profile->>attr` 두 필드만 1000행씩 전량 수신 → `_ciStatsData()` 단일 분기점). 집계 함수(`_dsBuildMap`·`_dsChanIdx`) 로직 무변경, 입력 배열만 교체. **일별 차트 포함 전체 기준으로 정확해짐**(이전 1000행 기준은 오답 → 숫자가 달라 보이는 게 정상).
- 빌더가 사양에 없던 2곳(`_ciStatsNav`·`_ciStatsApplyCustom`)을 전수 조사로 추가 발견해 분기 — 총괄 재감사에서 타당 판정. `#ci-flow-total`은 salesnote에서 `_cfSalesnoteTop()`이 `#ci-band`를 덮어써 화면에 안 남음을 코드로 확인.
- **실측: 청약완료 0건 / 고객관리(신규+청약완료) 2,572건 / 상담관리 68건.**
- ⏳ **대표 실사용 확인 대기** — 페이지 이동·필터 시 1페이지 리셋·달라진 숫자. 로그인 자격증명이 필요해 총괄 검증 불가분(대표 "추후 내가 확인하고 다시 얘기해줄게").

### 4) 지식 카테고리 레이아웃 규칙 v2 저장 — PR #1391 (main `f974a53`)
기획팀장 원문 343행 **무수정 보존**(diff 검증) + **총괄 대조 부록**. `docs/specs/knowledge_category_layout_v2.md` · 시안 HTML 동봉.

**대조 결과 원문 전제 2건이 사실과 달랐음:**
- §4 "스크립트 페이지의 탭·칩 재사용" → **스크립트 화면은 칩 1줄뿐, 라벨도 다름**(`SCRIPTS_GROUPS` 10개 상담단계). 원문이 적은 라벨이 글자 단위로 일치하는 곳은 `pages/insurance-factory.html:1399`(**SPA 밖 독립 프로토타입**). CSS도 그 파일 내부에만 존재, `alignSubTabs()`는 `offsetLeft` 기반이라 숨겨진 컨테이너에서 정렬 깨짐.
  → **확정 대안: 운영센터(어드민) `#ac-tabs`+`#ac-chips` 패턴 재사용**(`js/admin-console.js:1340~`). `#ac-chips:empty{display:none}`로 "그룹 없으면 칩 줄 감춤"이 이미 구현돼 있음. **"신규 제작 금지" 취지는 유지, 재사용 대상만 교체.**
- §3 "발행등급에서 노출범위 유도" → `customer_ok`/`internal_only`/`customer_blocked` **코드·DB 히트 0건**, `exposure` 컬럼 없음. 라이브 게이트는 `status` 4값.

**폰트(대표 확정):** 읽는 문장 **기본 16px**. 원문 §8-1 "15px 이상"은 하한을 기준선 삼은 서술이라 폐기. 한 줄 설명 15.5→**16**, 탭 15→**16**, 하단 안내 15→**16**, **칩은 14px 유지**(필터 라벨=읽는 문장 아님, 대표 확정). **행간 1.85 추가**(원문 누락). ⚠️시안 HTML에 §13 폐기값 14.5px 2곳 잔존 → 구현 시 제거.

### 5) 지식 문서 공용 레지스트리 — 설계안 PR #1392 (main `20ed51f`) + **원장 생성 PR-A #1393** (main `99ff394`)
- 설계안 `docs/specs/knowledge_registry_v1.md` — 7항목(실측/필드/샘플/공개범위 함수/카운트/화면연결/수정파일) + 리스크 + 미확정 기록.
- **PR-A 완료**: `js/knowledge-registry.js` 신설(레코드 3건 + 함수 6종) + `app.html` head 로드 1줄. **소비처 무접촉 → 화면 무변화**(브라우저 실측: 홈 타일 14개·순서 그대로·콘솔 0). 라이브 검증 완료.

---

## 🔑 다음 세션이 알아야 할 핵심 사실

### 지식 레지스트리 (`js/knowledge-registry.js`)
- **원장은 깔렸으나 아직 아무도 안 읽는다.** `app.html`에서 `knowledgeVisibleDocs`/`knowledgeCount`/`knowledgeById` 호출 **0곳**.
- 레코드 3건: `silson`(의료실비 변천사) · `cancer-treatment`(암주요치료비 변천사) · `caregiver-history`(간병보험 변천사).
- **정본 라벨 = "의료실비 변천사"**(대표 확정). 기존 표기 5종 산재를 통일할 예정 — 아직 화면엔 옛 이름("실비 변천사") 그대로.
- **`id` = 기존 showView 뷰 키와 동일 강제.** `?view=silson` 딥링크·북마크가 묶여 있어 라벨 통일 명목으로 바꾸면 안 됨(의도된 불일치).
- **`url`은 실측값 고정.** 의료실비는 `/pages/silson-generations.html` — `silson-history.html` 등으로 바꾸지 말 것(대표 지시).
- **`updatedAt` = 원장 기준일이지 실제 본문 수정일이 아님**(3건 전부 `2026-07-23`, 대표 확정). git 최종 커밋이 전부 동일 CSS 커밋이라 본문 변경일 판정 불가 → 없는 날짜 지어내지 않고 오늘로 통일. 앞으로 본문 고칠 때마다 수기 갱신. **NEW 배지는 이번 범위 제외**(날짜가 전부 같아 변별력 0).
- **`factGrades`는 현재 실질 무동작**(3건 전부 `customer_ok`). ⚠️**클라이언트 표시 필터일 뿐 접근 통제가 아님** — `internal_only` 문서를 실제로 올릴 땐 페이지 레벨 가드/서버 게이트 선행 필요.
- ⚠️**미해결**: `knowledgeIsAdvisor()`가 "로그인했는가"만 판정. **"로그인=설계사"가 참인지 확인 못 함**(일반 고객 계정 개념 유무 미확인). 설계사 전용 자료 첫 등록 전까지 해결하면 됨.
- **검색 keywords는 원장에 못 넣음**(대표 지정 필드에 없음). 간병 25개 실재 → 유실 시 검색 회귀. `_SHORTCUTS`에 잔류 → **"원장 1곳" 원칙이 검색에 한해 미완결**. 후속 트랙 분리 권고(유실된 `caregiver_search_index_v1.json` 복원과 함께).

### ⭐ 다음 작업 순서 — 대표 지적으로 재조정됨
대표 질문 "새로 만든 카드에 3건 병합시켰어?" → **아직 아님.** 축 카드 안은 여전히 빈 페이지.

원래 계획(PR-B 라벨통일 → PR-C 라우팅 → PR-D 사이드바 → 카테고리 화면)은 **대표 눈에 보이는 결과가 맨 뒤로 밀리는 순서**였음. 총괄이 순서 재조정 권고:

> **① 카테고리 화면 먼저**(카드 누르면 그 안에 자료 목록) → ② PR-B(홈·검색 라벨 통일) → ③ PR-C(`showView` 단독) → ④ PR-D(사이드바 3파일)

대표 응답 = "다음에 순차적으로 이어서 진행하자" — **다음 세션에서 착수 순서 확정 후 진행.**

### 구현 시 위험 지점 (설계안 §11)
- 🔴 `app.html:6225` `showView`는 **한 줄에 게이트 20여 개**. 잘못 자르면 bojang/xfile/salesnote/goji/newsletters 동시 붕괴 → **반드시 단독 PR + 비게이트 계정 게이트 전수 확인.**
- 🔴 registry 로드 순서 — head(현 24행)여야 함. `_SHORTCUTS`(인라인 ~12224)·`js/home.js`(~18281)보다 앞.
- 🔴 3개 지식 페이지는 **외부 JS 참조 0건 = 자체완결 정적 파일**. 사이드바는 문자열 concat, `_relurl` 맵이 4벌. `caregiver-history.html`은 **V3 정본**이라 훼손 시 타이포 표준 전체 영향.
- 🟡 홈 타일 아이콘·색은 원장 필드에 없음 → 상수 고정. **타일 순서(보장분석 축 맨 앞)는 대표 확정값, 되돌리면 회귀.**

---

## governance / 작업 운영 교훈 (이번 세션 확립)

1. **동시 작업자가 같은 파일을 만지면 격리 worktree로 분리**(`git worktree add`). 같은 트리에서 브랜치 갈아타면 서로 덮어씀. 영역이 다르면(app.html 뷰/라우팅 vs `_ci*` 15800~17000행) 각각 머지해도 충돌 없음.
2. **빌더 하니스 문자열 검증 ≠ 렌더 확인.** 총괄이 로컬 서버(`python -m http.server`)+in-app 브라우저로 게이트 ON/OFF·클릭·computed style까지 실측하고 병합. (라이브 `onesecond.solutions`는 origin 정책차단이라 로컬 실렌더 + 라이브 curl 조합)
3. **병합 전 `gh pr view <n> --json headRefOid` 검증** 9회 전부 이행.
4. **총괄이 빌더 보고를 그대로 전달하지 않고 직접 재대조** — 이번에 라벨 5종 산재·keywords 25개·외부 JS 0건을 총괄이 재확인.
5. **"확인 못 한 것"을 추측으로 메우지 않고 그대로 기록**(설계안 §14에 6건).

---

## 상태

- main `99ff394`(#1393 머지). 오늘 PR #1385~#1393 (9건) 병합·라이브. GitHub Pages 전부 배포 success.
- 워킹트리 = `tmp-insurance-factory-*.png` 다수 untracked(이월).
- `C:/tmp/` 방치 worktree 5개(factory-integration·multi-source·parser-reconcile·pr1369-review·yesterday-screen) — 머지 안 된 작업물 가능성이 있어 임의 삭제하지 않음. **각 브랜치 머지 여부 확인 후 정리 대상만 골라내는 별도 작업 필요.**
- 미결 이월: 카테고리 화면 · 레지스트리 PR-B/C/D · 빈 페이지 6개 내용 채우기 · '기타' 카드 최종 성격(새 6번째 축 vs 종합 보장분석) · 홈 타일 3줄 아래 정리(대표 직접) · 고객관리 대표 실사용 확인 · 검색 키워드 원장 후속 트랙 · `Issues Daily Fetch` 워크플로 실패(기존, 원인 미조사).
