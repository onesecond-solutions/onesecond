# 세션 요약 — 2026-04-24 야간

> **생성 시각:** 2026-04-24T23:30:00+09:00
> **생성 방식:** 수동 작성 (`/session-end` 슬래시 커맨드 인식 불가로 인한 임시 대응)
> **작업 디렉토리:** onesecond
> **세션 맥락:** 퇴근 후 집 작업 — auth_user_id 제거 작업(C 작업)의 사전 준비

---

## 📌 진실 원천 (Source of Truth)

- **최신 커밋(세션 시작 시):** `9a017f1 docs: 세션 요약 2026-04-24 저녁`
- **현재 브랜치:** main
- **라이브 사이트:** https://onesecond.solutions
- **이번 세션 로컬 변경:** 이 세션 파일 1건 + `claude_code/_docs/supabase_dumps/pre_c_work_rls_policies.csv` 1건(untracked)

---

## 🎯 이번 세션 목적

`claude_code/_instructions/20260425_auth_user_id_removal_spec.md` 기준 **C 작업(auth_user_id 컬럼 제거)의 사전 준비(Phase 0~3)** 완수. 비가역 DDL(`DROP COLUMN`)은 내일 아침 맑은 정신에 진행하기로 결정, 오늘 밤은 준비·정지 단계까지만.

---

## ✅ 이번 세션에서 한 일

### 이번 세션 코드 커밋
- **코드 커밋 없음** — 이번 세션은 DB·인프라 사전 점검 세션. 로컬 런타임 파일 수정 0건.
- 커밋 대상은 이 세션 요약 MD 파일 하나.

### Phase 0 — 대전제 확인 (완료)
- Supabase 신버전 프로젝트 ID 확인: **`pdnwgzneooyygfejrvbg`** (프로젝트명 `onesecond-v1-restore-0420`)
- DB 상태: Healthy
- 2026-04-24 오후 CLAUDE.md에 박제된 "매 DB 작업 첫 질문" 프로토콜 실동 성공

### Phase 1 — 사전 준비 (완료)
- **Phase 1-1:** 백업 스냅샷 확인
  - Supabase PRO plan 자동 백업 존재 (`2026-04-24 02:14:14 UTC`, 약 19시간 전)
  - PRO plan은 수동 백업 버튼 없음 (PITR 애드온 미구매) — 자동 백업이 안전망 역할
- **Phase 1-2:** RLS 정책 전체 export
  - 30행 반환 → `claude_code/_docs/supabase_dumps/pre_c_work_rls_policies.csv` 저장 (4,684 bytes)
  - `auth_user_id` 참조 정책 **3개** (전부 `activity_logs` 테이블 SELECT 정책)
  - `public.users` RLS 4개는 이미 `id = auth.uid()` 기반으로 정리돼 있음
- **Phase 1-3:** 트리거·함수 본문 export
  - `handle_new_user()` 본문: 이미 id 기반 완성형 (`INSERT INTO public.users(id, email, role, plan, created_at) VALUES (NEW.id, NEW.email, 'member', 'free', now()) ON CONFLICT (id) DO NOTHING;`)
  - `on_auth_user_created` 트리거: `auth.users AFTER INSERT` → 정상
  - `current_user_role` / `is_admin` 등 의심 함수: 존재하지 않음
  - `public` 스키마 전체에서 `auth_user_id` 참조 함수: **0개**
- **Phase 1-4:** 1-3에 포함되어 자동 완료
- **Phase 1-5 + Phase 2 통합 조회:** `public.users` 구조·가입자 전수 조회
  - 컬럼 11개. `auth_user_id` 컬럼 여전히 존재 (`uuid`, default `gen_random_uuid()`, nullable)
  - 인덱스는 `users_pkey`(id)만. `auth_user_id` 관련 인덱스·제약 없음 → `DROP COLUMN` 시 충돌 없을 것
  - 총 가입자 **21명** (admin 1 / branch_manager 2 / manager 2 / member 16)
  - **21명 전원 `id ≠ auth_user_id`** — 컬럼이 무용지물 유령 컬럼임이 실측으로 확정
  - admin 본계정: `pu.id = au.id = true` ✅ (인증 정상 동작) / `auth_user_id` 컬럼값만 별개 UUID (RLS 동작에 영향 없음)
  - `public.users`에만 있는 고아 5명 (auth.users에서 이미 지워진 테스트 흔적)
- **Phase 1-6:** 카톡 오픈채팅(`https://open.kakao.com/o/svu80Moi`)에 점검 공지 게시

### Phase 2 — 계정 정리 Part A (조회)
- Phase 1-5와 통합 실행으로 완료. 결과 동일.

### Phase 3 — 계정 정리 Part B (admin 외 전원 삭제, 완료)
- `public.users` DELETE: admin 본계정(`bylts0428@gmail.com`) 외 **20명 삭제** (SQL Editor에서 `DELETE FROM public.users WHERE email <> '...'`)
- `auth.users` UI 삭제: **15명 삭제** (Dashboard → Authentication → Users, 하나씩. 고아 5명은 auth에 없으므로 대상 아님)
- 삭제 중 본 계정 Delete 다이얼로그 감지 → Cancel 정상 작동 → 안전 프로토콜 실동 검증됨
- 최종 검증: `public.users` 1행 + `auth.users` 1행 + `id_match = true` → **PERFECT 판정**

---

## 🔑 이번 세션의 결정적 발견 3가지

### 1. C 작업 실제 범위가 원 지시서 예상보다 대폭 축소됨
구버전 덤프 기준으로 쓰인 원 지시서 대비, 신버전 실측 결과:

| 항목 | 원 예상 | 실측 |
|---|---|---|
| RLS 수정 대상 | 10+개 | **3개** (activity_logs만) |
| 트리거 수정 | 필요 | **불필요** (이미 완성형) |
| 함수 수정 | 필요 | **불필요** (참조 0건) |
| 예상 소요 시간 | 4시간 10분 | **약 2시간 30분** |

### 2. admin "DEFECT" 판정의 재해석
Chrome이 초기에 `auth_user_id_self_match = false`로 DEFECT 판정했으나, 실제로는:
- `pu.id = au.id = true` → 인증은 id 기반으로 정상 동작 중
- `auth_user_id` 컬럼값이 별개 UUID인 것은 오히려 C 작업(컬럼 제거)의 정당성을 강화하는 증거

### 3. 안전 프로토콜 실동 검증됨
- 매 DB 작업 첫 질문(프로젝트 ID 확인) 프로토콜 → 실동 성공
- 삭제 4단계 안전망(DRY RUN → admin 보호 → DELETE → 즉시 검증) → 정상 작동
- UI 삭제 중 본 계정 다이얼로그 감지 시 Cancel → 정상 작동

---

## 🚧 미해결 이슈

- **Phase 4~7 내일 진행 예정** (`20260425_auth_user_id_removal_spec.md` 기준)
  - Phase 4-1: `activity_logs` RLS 3개 DROP + 2개 CREATE (약 15분)
  - Phase 4-2: `index.html` `doSubmit` 수정 — Step 4 기본 + OTP 전환(`signInWithOtp` / `verifyOtp`) + phone 수집 병합 (약 60분)
  - Phase 4-3: 전역 grep (런타임 코드 `auth_user_id` 참조 0건 예상)
  - Phase 5: 🔴 `ALTER TABLE public.users DROP COLUMN auth_user_id` — 팀장님 직접 SQL Editor 실행 (비가역)
  - Phase 6: admin 로그인 / 신규 가입 / 역할별 RLS 동작 검증 (약 60분)
  - Phase 7: `login.html` 미해결 버그 2건 (myspace "준비 중" + A1 이름/이메일 미표시)

- **`/session-end` 슬래시 커맨드 인식 안 됨**
  - `.claude/commands/session-end.md` 파일 존재 (3,429 bytes)
  - `.gitignore`에서 `.claude/*` 기본 무시하되 `!.claude/commands/` 예외로 추적 허용 상태
  - 원인 미확인. 회사 PC에서 디버깅 필요
  - 오늘 밤은 이 세션 파일을 수동 작성하는 방식으로 우회

- **`pre_c_work_rls_policies.csv` untracked 상태로 보류**
  - `claude_code/_docs/supabase_dumps/` 하위 신규 파일
  - 내일 C 작업의 RLS 정책 복구 원본. 다른 PC에서 작업 재개 시 필요
  - 커밋 여부는 내일 아침 판단

---

## 🎯 다음 세션 시작 시 할 일

1. **Supabase Dashboard 신버전 프로젝트 ID 재확인**
   - 프로젝트명: `onesecond-v1-restore-0420`
   - URL: `https://supabase.com/dashboard/project/pdnwgzneooyygfejrvbg`
2. `claude_code/_instructions/20260425_auth_user_id_removal_spec.md` Phase 4 착수 (Step 2부터 — Step 1 데이터 정합성은 Phase 3 종료로 이미 자동 해결)
3. **기존 role명 유지** (admin / branch_manager / manager / member / staff)
   - `ga_*` 접두어 마이그레이션은 이번 C 작업과 **분리**, 별도 세션에서 처리
   - 이유: 변경 범위 폭증 방지 + 9-role 체계 설계(2026-04-24 오후 `420bf1d` 커밋)는 팀장님 승인 대기 상태
4. `/session-end` 슬래시 커맨드 디버깅 (회사 PC에서)
5. `pre_c_work_rls_policies.csv` 커밋 여부 판단

---

## 💡 Claude AI 메모리에 반영할 핵심

1. **원세컨드 신버전 Supabase: `pdnwgzneooyygfejrvbg` (프로젝트명 `onesecond-v1-restore-0420`)** — 매 DB 작업 첫 질문으로 확인. 구버전 `qursjteiovcylqiepmlo`는 폐기.
2. **2026-04-24 밤 DB 정리 완료 상태: 가입자 1명만 존재 (admin = `bylts0428@gmail.com`)** — 이후 가입 테스트는 새 이메일로 해야 하며, C 작업 중 데이터 정합성 이슈는 없음.
3. **C 작업 실제 범위 축소 확정**: RLS는 `activity_logs` 3개만 교체, 트리거·함수 수정 불필요. 예상 2시간 30분.

---

*수동 작성: Claude Code (2026-04-24 23:30) — `/session-end` 슬래시 커맨드 인식 불가로 인한 임시 대응*
