---
세션 종료 시각: 2026-07-20 낮 (~12:37 KST)
주요 마감 작업: 라이브 완주 3건 — 암호 엑셀 화면 내 복호화(#1329) · X-FILE 내비 먹통 CSS 게이팅 수정(#1330) · 구글 OAuth 로그인 후 비로그인 회귀 수정(#1331). + 보장분석 뱅크샐러드 계약 파서 담당자 진행 중(미완).
생성 도구: 총괄팀장 Code (대표 세션, 데스크톱 앱). 신버전 pdnwgzneooyygfejrvbg.
---

# 세션 인계 노트 — 2026-07-20 낮 (1237)

## ⭐ 성격
데스크톱 앱 세션. START PROTOCOL 부팅 보고 후 대표 지시 연쇄로 **라이브 완주 3건**. 위임(os-builder 대행=general-purpose) + 총괄 검수·머지·배포 체제. 후반 **총괄 검수 위생 사고 1건 박제**(아래).

## ✅ 라이브 완주 (전부 병합·배포·라이브 확인)
- **#1329 암호 보호 엑셀 화면 내 복호화** — 보장분석 투입구(`pages/insurance-factory-claude.html`)에서 암호 엑셀 감지 시 비밀번호 입력칸→그 자리서 복호화. SheetJS 무료판은 암호 복호화 불가라 **`@zurmokeeper/exceljs@4.4.9`**(브라우저 UMD·ECMA-376) CDN으로 복호화→기존 SheetJS 파싱 재사용. officecrypto-tool은 브라우저 crypto 의존으로 불채택(대안 전환). **원본·비번 서버 미저장**. main `0b9c8d6`. (당초 추천 officecrypto-tool 실패→PoC로 zurmokeeper 확정)
- **#1330 X-FILE 내비게이션 먹통 수정** — 진입 후 홈으로·사이드메뉴 클릭 시 화면 안 바뀌던 것. **원인=CSS 특이도**: `js/xfile.js` injectStyleOnce의 `#v-xfile{...display:flex}`가 `.on` 조건 없이 id 특이도(1,0,0,0)로 `.view{display:none}`/`.view.on{display:block}`을 이겨, 진입해 `xf-style` 주입된 뒤로 `.on` 빠져도 v-xfile이 영구 display:flex로 남아 화면 가림(라우팅·URL·.on 토글은 정상=원인 B). **#1319(X-FILE 풀스크린) 회귀**. 수정=선택자 `#v-xfile`→**`#v-xfile.on`** 단일 라인. main `b5f0753`. **12항목 실클릭 검수 통과**(홈으로/홈/고객관리/휴지통/about/재진입/전체폭유지/콘솔0).
  - **⚠️ 검수 중 발견한 별개 버그(미수정)**: 뒤로가기 목적지가 `salesnote`(고객관리)·`trash`(휴지통)일 때 화면 미전환. 원인=`app.html:6194` `VALID_VIEWS` 배열에 이 두 view 누락→popstate가 무시. **이 CSS 수정과 무관·기존 버그**. 해법=배열에 `'salesnote','trash'` 추가(단일). 대표 보류 중.
- **#1331 구글 OAuth 로그인 후 비로그인 회귀** — 대표 "로그인했는데 다시 로그인 초기화면" 증상. **원인=#1317(2026-07-18 비로그인 빠른부팅) 회귀**: startBoot의 `if(!getBootRole() && !token){ bootView(); return; }`가 **OAuth 콜백 순간(URL ?code=는 있으나 PKCE 토큰 교환 전이라 os_token 없음)**을 비로그인으로 오판→즉시 비로그인 홈 부팅+`_booted=true`. 이후 `_handleOAuthCallback`이 토큰 교환·appstate:ready 쏴도 재부팅 무시→비로그인 고정. **이메일 OTP는 무영향**(모달서 토큰 저장 후 /app.html 이동). 수정=startBoot에 `oauthPending` 가드(`?code=`·`#access_token`·`os_pkce_verifier` 있으면 즉시부팅 skip→appstate 대기). main `cdfb1c7`. 담당자 4케이스 검수 통과(비로그인 즉시부팅 유지/OAuth 대기/기존세션/이메일OTP). **구글 로그인 회귀 해결·라이브 반영**(대표 실로그인 육안만 잔여).

## 🟡 진행 중 (미완·다음 세션 회수)
- **보장분석 계약 정보 파서(1단계)** — 담당자가 구현을 끝내고 커밋했으나, **공유 워킹트리 사고로 `main`에 직접 커밋(`2ec285d`, insurance-factory-claude.html +122/-1)** 됨 → 세션엔드 rebase/push에 함께 밀려 라이브 반영 → **총괄이 즉시 `git revert`(`47f61dd`)로 라이브 롤백**(insurance-factory-claude.html은 #1329 복호화 버전으로 복귀). **파서 원본은 커밋 `2ec285d`에 보존.** **⚠️ 사고 원인 = 담당자에 worktree isolation 미부여 + 총괄이 세션노트 쓰려 `git checkout main` 하며 공유 트리의 담당자 브랜치 컨텍스트를 덮음(담당자가 main에 커밋). 재발방지 = 구현 담당자는 반드시 별도 worktree(Agent isolation:worktree), 총괄은 세션 중 공유 트리 브랜치 전환 금지.** 정합 [[feedback_no_concurrent_git_agents_shared_tree]]. 
  - 사양(대표 확정): 뱅크샐러드 엑셀=**가로 다중헤더 담보×상품 매트릭스**(1행 회사·2행 "월X원/N년"·3행 헤더+상품명·4행~ 담보). 현재 세로형 파서라 **계약 0건**(담보 36건은 정상). 수정=뱅크샐러드형 감지 시 각 상품 컬럼→계약 1건(회사=1행/상품명=3행/보험료=2행 "월X원" 파싱). **계약일=엑셀에 없음→설계사 화면 직접 입력 편집필드** / **갱신여부=설계사 select(미지정/갱신형/비갱신형)** / **계약 리스트 하단 합계보험료(월 보험료 합)**. 세로형 회귀0·목업 검증. 담보는 2단계(미착수).
  - 검증=고객 실데이터 대신 목업 엑셀. 배포 후 대표 실파일(김은경) 확인 예정.

## 🔴 총괄 실수 박제 — 검수 위생 사고
- X-FILE 12항목 검수 중 `about`을 클릭했더니(about=`location.href='/pages/landing.html'` 미사용 인트로) **대표가 실시간 보는 인앱 브라우저 창(tab-10)에 미사용 인트로가 남음** + 직후 **로컬 검수 서버(8777) 종료** → 그 landing의 로그인 링크가 죽은 서버 가리켜 **대표가 "어드민 로그인 오류"로 오인**. 대표 "왜 인트로 화면 띄웠냐"로 지적.
- **교훈**: 대표 창(preview pane)을 검수에 쓰지 말 것. 검수는 **별도 탭+별도 포트**로, 끝나면 즉시 원복·서버 정리. 이후 OAuth 검수는 담당자에게 "tab-10 무접촉·별도 포트·정리" 명시해 재발 방지. [[feedback-daepyo-screen-quality]] 정합.
- 단 이 오인 조사가 **진짜 OAuth 회귀(#1317)**를 찾아내는 계기가 됨(대표 "onesecond.solutions에서도 튕긴다"→실장애 확정).

## ⏰ 다음 세션
1. **보장분석 계약 파서 회수 (revert된 것 재작업)** — 커밋 **`2ec285d`를 별도 브랜치로 cherry-pick**(worktree 격리) → 총괄 검수(뱅크샐러드형 계약 추출·합계보험료·**세로형 회귀0** 특히 확인)→**대표 김은경 실파일 확인**→PR·배포. (main엔 현재 `47f61dd`로 롤백돼 파서 없음.)
2. **보장분석 2단계=담보 정보**(상품별 가입금액 매트릭스 펼침) 대표 지시 대기.
3. **X-FILE 뒤로가기 별개 버그** — `VALID_VIEWS`에 `salesnote`·`trash` 추가(단일) 승인 여부.
4. 실호출 육안(대표 몫): 암호 엑셀 실복호화 / 캡처 OCR / 구글 OAuth 실로그인.
5. worktree·로컬 브랜치 적체(210+) 정리.

## 정합
CLAUDE.md START PROTOCOL · [[feedback-pr-report-format]] · [[feedback-daepyo-screen-quality]] · 메모리 `project-onesecond-pending`. 직전 세션: `docs/sessions/2026-07-20_0700.md`.
