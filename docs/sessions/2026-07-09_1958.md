---
세션 종료 시각: 2026-07-09 19:58 KST
주요 마감 작업: 비로그인 공개 대량(소식지·스크립트 anon 정책·딸깍·계산기·상품라인업·실비) + 검색·정렬 실측버그 수정 + 라이브 용어 원칙 박제 + 업무노트 유형 5종
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-09 저녁 (19:58)

> **성격:** 총괄팀장 Code 단독 진행(대량 위임 체제=실행 담당자 위임/총괄 조율·검토·머지·보고). 신버전 `pdnwgzneooyygfejrvbg`. main 직접 push=문서/세션만, 제품 전부 PR. 동시 app.html=worktree 격리 병렬. 오늘 PR #1200~#1212 라이브 반영.

## 오늘 마감 (전부 라이브 반영·실측, PR#)

1. **#1200 홈 푸터 하단 정렬** — `.home-footer{margin:auto auto 0}` + `#v-home` 비검색 flex 컬럼. 라이브 확인.
2. **#1201 계산기·변환기 스크롤 복구** — `#v-tool`이 스크롤 복구 목록 누락 → `.wrap{overflow:hidden}` 상속으로 잘림. 목록 추가 1줄.
3. **라이브 화면 용어 원칙 박제(CLAUDE.md, `e063c4d`·`2a47be4`)** — 대화·질문·보고는 라이브 화면 용어로만. 재발방지 규칙(화면 이름 정본은 라이브에 있음, 대표에게 되묻지 말 것) + 코드 정리 제도(1 화면문구 교체·3 주석·4 매핑주석·2 죽은화면 제거).
4. **용어 사전 `docs/ops/live_term_glossary.md`(`a35f300`)** — 라이브 표시용어↔코드 식별자↔폐기 옛 용어 매핑. §2 화면노출 옛용어 12건 / §3 코드내부(별도 트랙) / §4 정본 미확정.
5. **#1202 + anon 정책 = 비로그인 5개 페이지 개방** — 상품라인업·소식지·실비변천사·스크립트·계산기변환기 비로그인 열람. 프론트(소식지 `status=eq.published` 필터·검색 로그인 전용 봉인·홈 posts skip). **anon 정책 CI 적용**(newsletters `published`·scripts `global`만, 화이트리스트). ⚠️**GitHub 단일계정 self-review 데드락**=required-reviewer 일시조정→배포→**원복**(대표 위험승인 근거). 실측: 소식지 537·스크립트 59만 노출, posts/calendar/myspace/team/sales 전부 anon 0.
6. **#1203·#1208 검색 상세 HTML/첨부 노출 차단** — #1203이 도달불가 `_searchOpen` 고침(실패) → #1208이 **실제 경로 `_searchGoScript`** 수정(SELECT에 search_text·attachments 추가, 본문=`search_text||_svStripHtml`, 썸네일·첨부 분리). 라이브 확인.
7. **#1204·#1206 업무노트 저장 최상단 정렬** — 새 글 sort_order DEFAULT 999 → 하단. #1204(saveScrInline만)→#1206(대표 실제경로 **saveMyspaceWrite=scriptWrite**+채팅 sbody에 `sort_order:null`). **대표 라이브 실측 완료 확인.**
8. **#1205 코드 매핑 주석 + #1207 죽은 화면 '현장의 소리'(#v-voice) 제거(-254줄)** — 공용 헬퍼(`_voiceAttsHtml`·`voiceOpenLb`·`_voiceImgs` 17곳) 보존, '오늘 단체방' 카드 재연결 유지(삭제 안 함).
9. **#1209 딸깍 전체 공개** — FAB/오버레이·외부검색 이미 열림, `quick_contents` anon 정책 CI 적용(9건 노출, 타 테이블 0). 저장류는 `_osLoginGate` 유지.
10. **#1210 로그인 후 푸터-카드 간격 통일** — `#homeDesk{margin-bottom:clamp(56px,9vh,104px)}` → 로그인 후 0px→81px(로그인 전과 동일).
11. **#1212 업무노트 유형 5종 배지·모아보기** — 채팅·스크립트·카드·상담관리·기타(업무노트=카드명). keywords 태그 규약, DB 무변경. `SCRIPT_TYPES` 단일상수·`filterMysType`·유형 배지. 기존 채팅32 자동/콜1+무태그21=미지정(대표 편집 재지정). 새글 기본=기타.

## ⚠️ 미완/후속 (다음 세션 1순위)

- **소식지 비로그인 미리보기 EF 배포(#1211 미완)** — 프론트 폴백(`_nlSignAnon`) 라이브 반영. **Edge Function `newsletter-sign` 배포만 남음**(published만 1시간 서명·버킷 하드코딩·경로주입 차단, deno check 통과). 이 환경에 Supabase `SUPABASE_ACCESS_TOKEN` 없음 → **대표 Supabase 계정 1회 인증 필요**(외부서비스 대표 자리). `supabase functions deploy newsletter-sign --no-verify-jwt`. 배포 전엔 비로그인 소식지 PDF 안 열림.
- **업무노트 유형 재분류** — 무태그21+콜1=22개 '미지정' → 대표가 편집으로 재지정.
- **코드 정리 3번(전체 주석 옛단어 교체)** — 매핑주석·죽은화면 제거로 대부분 흡수, 선택 보류.

## 교훈 (중요)

- ★**함수 단위 검증만으로 "완료" 단정 2회(정렬·검색) → 대표 실측에서 드러남.** 이후 **실제 도달 경로 검증·라이브 반영 확인 후에만 완료 보고**로 교정. #1203이 도달불가 `_searchOpen` 고친 것, #1204가 저장경로 하나만 고친 것이 대표적 사례.
- ★**라이브 화면 용어로만 소통**(대표 재확정, CLAUDE.md 박제). 화면 이름 정본은 라이브에 이미 있음 → 되묻지 말 것. 코드 옛 이름을 화면 이름으로 착각 금지(용어 사전 참조).
- ★**docs 커밋 브랜치 착각 사고** — 로컬 HEAD가 feat 브랜치였는데 main으로 착각, `git push origin main`이 조용히 실패. cherry-pick으로 수습. **push 후 `git log origin/main`로 실제 반영 확인·커밋 전 브랜치 확인 습관.**
- ★**GitHub 단일계정 environment self-review 데드락** — 트리거=reviewer=대표 계정이 같아 승인버튼 미노출. required-reviewer 일시조정→배포→원복(원복 전 완료보고 금지)이 표준.
- 총괄 직접 실행 금지→위임(대표 반복 지적). 대표에게 새 글 작성·클릭·화면검수 노동 요구 금지=AI팀 검수 계정/헤드리스로 종결.

> 정합: `docs/ops/live_term_glossary.md` · `docs/work_orders/2026-07-07_first_public_launch_checklist.md`
