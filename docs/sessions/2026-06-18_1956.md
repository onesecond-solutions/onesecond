---
세션 종료 시각: 2026-06-18 19:56 KST
주요 마감 작업: 검색 홈 전면 구축(3계정 미리보기) + 검색 속도 trgm 인덱스 근본해결 + 스크립트 HTML 정화 + 전략 3대 정정
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-18 (저녁) 검색 홈 본진 + 검색 속도 근본해결 + 전략 정정

작성: 총괄팀장(Code) 단독 종일 세션. main 직접 push 0(제품 PR·머지=총괄팀장 실행) / DB·인덱스 실행=대표님 Dashboard 직접·신버전 `pdnwgzneooyygfejrvbg`.
**전 작업이 3계정 미리보기 게이트(`_searchScopeAllow`: 어드민 bylts0428@gmail · 실장 bylts@naver · 팀장 bylts@kakao)** — 일반 사용자 영향 0. 전체 라이브는 대표님 확정 시 게이트 한 줄 오픈.

## ⭐⭐ 전략 3대 정정 (기획팀장 대화 → 메모리 박제, master_strategy 반영 대기)
AI(나 포함)가 현실과 어긋나게 좁혀 잡던 본질을 대표님이 정정:
1. **중립 = 추천/비교 안 함이 아니라, 어느 원수사도 우선순위에 안 두고 보험료·보장으로 공정 비교해 최선 찾기** = GA 존재이유·원수사 차별점. (메모리 `strategy_neutrality_fair_comparison`)
2. **고객정보(이름·생년월일·전화) = 동의 기반이면 수집·저장 정상.** "절대 저장 X"는 AI 과한 디폴트. 기준=동의+적법처리+보안. 데이터분리(검색=PII0/CRM=동의PII)는 tier 아닌 위치. (메모리 `customer_pii_consent_based_ok`)
3. **요금제(FREE/PLUS/PRO)=성공 이후 미래 개념. 지금 전면 무료.** tier를 오늘 설계 제약으로 끌어쓰기 금지. (메모리 `pricing_tiers_are_future_not_now`)
🚨 **이 셋은 master_strategy_v1.md(중립 정의·tier·§4 PII)와 충돌** → decisions 문서+PR로 정정안 올리는 것 **대표 결재 대기**(미착수).

## 검색 홈 본진 — 전면 구축 (PR #803~#823, 3계정 게이트)
- **#803** 검색 범위 가시화 v2 — 결과를 4범위(내자료/팀/지점/공동)로 묶음 + 범위 칩(이후 #810에서 소스 축으로 재편).
- **#804** Phase 1 검색 홈 — A영역 인사 좌측 이동 + 배너 숨김 + D영역 중앙 "무엇을 찾으세요?" 큰 검색창 + 카드3(상품/인수=검색렌즈, 상담=다이어리) + v2 결과 인라인.
- **#810** 소스 탭 — `[전체·지식·보험Q&A·소식지·공지·스크립트·자료]`(답품질 우선·자료 뒤), 라벨 사용자말("지식엔진"→"지식"), 상품/인수=지식 탭+category 필터+활성칩, 스코프=요약 카운트줄(축 분리).
- **#811** empty-state ①필터0→[필터 끄기] ②전범위0→[질문 남기기](기존 #ask 폼 검색유형 맞춰 오픈) = 플라이휠 입구. #ask placeholder 사명 중립화.
- **#815** 작업 A — 계열 필터를 category 그룹 매핑 파생으로(DDL 0): 상품=상품+상품구조특약 / 인수=인수+인수심사+계약청약고지 / 보장=보장+보장담보+손해사정보험금+보상+청구. 미분류=평검색 노출.
- **#819** 슬라이스 2 — 검색 후 좌측 정렬(검색 전=중앙). 로고 outdent=사이드바 onesecond.
- **#820** 화면 이동을 **제출(엔터·검색버튼) 시에만**(입력 중 이동 X) = 구글/네이버 정합. 입력(_hsInput)/제출(_hsSearch) 분리.
- **#821** type=search 기본 X와 커스텀 X 중복 → 기본 X 숨김.
- **#823** 홈 검색 아래 3항목 = 박스→세로바│+라벨+설명 텍스트(home01, 상담시작=인디고). 클릭·라우팅 불변.

## 스크립트 검색 HTML 정화 (전 구간 마감)
- **#813** stopgap: 스크립트 script_text=HTML 저장 → 검색 스니펫 태그 노출 + 메타 내부 stage 누출 → strip + 메타 '스크립트' 고정.
- **#814** 영구 마이그레이션 SQL(대표 실행 완료): `scripts.search_text` 컬럼 + HTML strip 백필 → **filled 80/81 · still_html 0**.
- **#817** app.html 검색이 search_text 우선 읽기 전환.
- **#818** write-time: 저장/수정 7곳 `_scrFillSearchText`로 search_text 기록(신규 회귀 차단) + **상세 모달이 script_text(HTML) 이스케이프 출력하던 것 → search_text 평문 표시**.

## ⭐ 검색 속도 근본해결 — pg_trgm GIN 인덱스 (대표 실행·검증 완료)
- 진단: 느림=서버 아님. **색인 없는 ILIKE 전체 훑기(Seq Scan).** ([B] posts·team_notices·knowledge content HTML 0 / newsletters full_text 12=미미 → 정화 선행 불요)
- **#822** 마이그레이션 SQL(대표 Dashboard 실행): `CREATE EXTENSION pg_trgm` + 4테이블 9컬럼 `CREATE INDEX CONCURRENTLY ... gin_trgm_ops`(knowledge title·body / newsletters title·company·full_text / posts title·content / myspace_files original_name·search_text).
- **검증**: myspace EXPLAIN ANALYZE → `Bitmap Index Scan on idx_mf_stext_trgm`(색인 실사용) + Execution 31.8ms(색인탐색 0.07ms, 나머지=행 heap 읽기). Seq Scan 제거 = 자료 늘어도 안 느려짐.

## taxonomy v1 (지식 분류 체계)
- **#812** `docs/specs/knowledge_taxonomy_v1.md`(6축: 소스/계열/교차태그/공간/출처/노출상태) + Code 매핑 분석.
- 20건 샘플 매핑 결과: 상품4·인수4·보장4=12/20 깔끔, 8/20 미분류(계약·민원·세제·판매=용어·제도). 교차태그 질병·회사=현 데이터 거의 0(mine-batch 단계 도입).
- **대표 결재 = (가) 채택**: 5계열=선택적 필터 렌즈+미분류 허용. **914 재태깅·DDL 불요**(계열=category 묶음 파생, #815로 구현). 교차태그 컬럼=mine-batch 때.

## DB 작업 (대표님 Dashboard 직접 실행 완료)
- `scripts.search_text` 컬럼 추가 + HTML strip 백필 (#814).
- pg_trgm 확장 + 9개 trgm GIN 인덱스 (#822).
- (참고 파일) `docs/ops/2026-06-18_scripts_search_text.sql` · `2026-06-18_search_trgm_indexes.sql` · `2026-06-18_scripts_migration_chrome_request.md`(미사용=크롬 외부URL 자동실행 불가, 대표 직접 복붙으로 대체).

## 경쟁 분석 (참고)
시그널플로우(B2B 설계사 가설계·3초 비교)·시그널플래너(B2C 슈퍼앱) = 해빗팩토리 수직통합. **차별화=심판 not 선수**(고객 안 가져감·중립). "PII 안 받음" 차별화는 폐기(걔들도 받음·우리도 동의기반 받음). 우리 길=C(무료 지식·검색 인프라로 사용자부터). 원수사 입점·유료화·비교엔진=미래.

## ⏳ 다음 작업 예약 (파일: `docs/work_orders/2026-06-18_search_next_queue.md`)
1. **1순위 = 슬라이스 3a**: 검색 결과 **우측 패널 미리보기**(모달/자동새창 폐기). 시안 search05~07. 재사용 훅=`_searchOpen`(렌더 이사)·`openLb/#lb`(이미지+딸깍)·`_openSignedAtt`(새창→패널)·`_nlDdalggak`. 결과클릭→우측 패널 상세, "원본 열기"=보조 버튼.
2. **2순위 = 슬라이스 3b**: AI 개요(좌, `_aiAnswer` 승격) + 근거 패널(우, 모드전환) = search04 완성. 검색 디테일 이식과 합류.
3. **대기**: mine-batch 자동채굴 / 914 재태깅·교차태그 DDL (taxonomy 결재 후).

## 미머지/대기
- 🔧 **PR #802 미머지** — 어제(6/17)부터 대기 중인 AI 파일명 '정리 중…' 시그널(app.html only). 이번 세션 검색 트랙 집중으로 못 다룸. 다음 세션 머지 필요.
- master_strategy/decisions 전략 3대 정정 반영(대표 결재).

## 학습/메모리
- 신설 메모리: `feedback_small_request_minimal_change`("버튼만/약간만"=딱 그만큼, 재설계·전문가팀은 명시할 때만 — '시작하기 버튼만 삭제+약간이동'을 카드 재설계로 부풀려 원복한 사례) · `strategy_neutrality_fair_comparison` · `customer_pii_consent_based_ok` · `pricing_tiers_are_future_not_now`.
- 검색 화면 이동=제출 시에만(구글/네이버, 입력마다 X). SQL 채팅 전달=코드블록만(한글 프로즈 섞이면 42601). GitHub Pages 일시 빌드장애(.nojekyll라 콘텐츠 무관)=POST 재트리거로 복구. knowledge_entries PK=entry_id(id 아님). 검색 컬럼 정제(search_text)+trgm GIN이 ILIKE 가속 정석(FTS 아님, 쿼리 재작성 0).
