# 2026-08-01 세션 — 인프라·비용·플랫폼 전환 준비 (대표 세션, 데스크톱 앱)

> 총괄팀장 Code. 이번 세션은 제품 코드(app.html 등) 무접촉. Supabase 대시보드·GitHub 설정·docs·별도 저장소 작업 중심. 라이브 사이트 영향 0.

## 1. Supabase 요금 원인 규명 + 유휴 test 프로젝트 삭제

- 대표 "슈퍼베이스 요금이 예상보다 많다" → 대표 Chrome(claude-in-chrome)으로 Usage 실측.
- **원인 = 사용량 아닌 Compute 고정비.** Egress 0.78/250GB·Storage 9.2/100GB·MAU 16/100k = 전부 <10%. 비용은 **프로젝트 2개 상시가동 Compute**: 라이브(onesecond-v1-restore-0420, Small ~$15/월) + **onesecond-test**(Micro ~$10/월, Egress/MAU/Storage 전부 0인 유휴).
- **onesecond-test 삭제 완료**(대표 직접 최종확정, 총괄은 영구삭제 대행 안 함). 삭제 전 스키마 DDL 백업 = `docs/archive/onesecond-test_schema_backup_2026-08-01.sql`(문자인증+결제/구독 프로토타입 12테이블 컬럼+PK/FK/UNIQUE/CHECK, 데이터 미포함). 조직 프로젝트 2→1 확인. **다음 청구주기부터 ~$10/월 절감.**
- 남은 여지(미실행): 라이브 Small→Micro 하향 ~$15/월(부하<1%나 7월 성능튜닝 이력 있어 신중). 메모리 `project-supabase-billing-compute`.

## 2. 플랫폼 전환 준비 — 읽기전용 조사 완료 + 안1 확정

- 대표 「플랫폼 전환 준비 범위 확인서」(docx) → 읽기전용 조사 지시. `onesecond.solutions`=소개홈 / `insu.`=보험 / `dayfolder.`=데이폴더(별도) / `theonespace.`=신규구상.
- **코드 실측 5갈래 병렬(Explore):** 단일 저장소·단일 CNAME·빌드없는 GitHub Pages, 루트 index가 전원 `/app.html` 강제리다이렉트(루트=곧 보험앱). 인증·결제는 `window.location.origin` 자동적응이라 코드 손댈 것 거의 없고, 깨지는 건 외부 대시보드(Supabase Redirect·PG 도메인). SEO/교차링크 하드코딩 다수(canonical/og 21·sitemap9·교차링크21). **데이폴더·더원스페이스는 코드/전략에 실체 0**(3제품 분리 전제와 어긋남 → 김실장 정정: 보험 insu 이동+루트 소개홈만 먼저).
- **김실장 결정 A~E** 반영: A=①만 먼저 · B=보험 공개문서 9종도 insu로(옛URL 301) · C=별도 저장소/Pages 분리 · D=Supabase 신규테이블X(기존유지+insu 허용목록) · E=루트 전체공개·insu 게이트 유지.
- **총괄 최소이동 비교 → 안1 확정**: 기존 저장소=보험 insu 유지 + 루트 소개홈은 새 저장소(앱 이동0·라이브 대공사X·롤백 단순). 안2(앱 통째 복제)는 위험 큼.
- **대시보드 실측 4건(대표 Chrome, 읽기전용):** ①DNS=uhost.co.kr 관리(루트 A에 GitHub Pages IP + 210.220.163.82 잔재, insu 미연결) ②Supabase Auth Site URL=onesecond.solutions·Redirect 5개(insu 미등록=insu서 구글로그인 차단) ③public **66테이블**·거의 RLS ON·**RLS OFF 2건**(`salesimport_staging`~2400행·`_bak_users_team_20260623`)·cron활성 미확정 ④PortOne admin.portone.io=브라우징 차단→대표 직접(단 빌링키라 도메인의존 낮음).
- 산출물 = `docs/work_orders/2026-08-01_platform_transition_prep_review.md`(제출물 6종+7 실측). 메모리 `project-platform-transition-track`. **다음=실제 이동 구현(별도 승인).**

## 3. 데이폴더 GitHub 비공개 백업 (별도 프로젝트)

- 데이폴더 = 원세컨드 저장소 밖 별도 프로젝트(`C:\Users\az\Documents\캘린더 앱 개발`). 로컬 전용(remote 없음)이라 유실 위험 → 대표 지시로 백업.
- 원본 무손상 read-only clone→push(원본 .git 무변경): **onesecond-solutions/dayfolder-app**(private, master, 31커밋 `1022e0c`) + **dayfolder-home**(private, main, 1커밋 `69efbdc`). node_modules/dist 제외, 시크릿 없음. 코상무(Codex) 전달 메시지 제공. 메모리 `project-dayfolder-repos`.

## 4. 홈 "보험이슈" 자동수집 중단 진단 + 워크플로 비활성화(A)

- 화면 자료가 6/23 이후 끊김. 진단: GitHub Actions `Issues Daily Fetch`가 매일 정상 실행되나 **매번 failure** — korea.kr RSS 3곳 전부 **404**.
- 근본 원인 = **korea.kr "정책브리핑 RSS 서비스 제공 중단 안내"(2026-07-01, 저작권 사유)**. 대체 RSS/API 미제공 → **URL 교체로 복구 불가**. 우회 스크래핑은 저작권상 부적절.
- **비용 = 0**(public repo Actions 무료·실패 런도 무료·Supabase 미접속). 대표 걱정(비용) 해소.
- **조치(대표 "A만"):** `gh workflow disable 281952446` → active→disabled_manually. 실패 실행·알림 메일 정지. yml·스크립트 보존. 라이브 무영향. 메모리 `project-boheom-issue-rss-discontinued`.
- 남은 것(B, 대표 판단 대기): 대체 소스(부처 자체 RSS/공공데이터포털 API) 재구축.

## 미결/다음 세션

- **플랫폼 전환:** 안1 실제 이동 구현(별도 승인) — 보험저장소 도메인 문자열 교체·새 소개홈 저장소·uhost insu CNAME·Auth insu redirect·루트 스왑+301.
- **대표 몫:** PortOne/KG이니시스 도메인 확인 · (위생) RLS off `salesimport_staging` 점검 · 라이브 Small→Micro 하향 판단.
- **보험이슈 B:** 대체 소스 재구축 여부.
- **데이폴더:** 코상무에게 원격 연결 안내 전달(대표).

## 커밋
- 백업: `docs/archive/onesecond-test_schema_backup_2026-08-01.sql`
- 문서: `docs/work_orders/2026-08-01_platform_transition_prep_review.md`
- 세션 노트: 본 파일 + `_INDEX.md` 갱신
- (Supabase test 삭제·워크플로 비활성화·데이폴더 백업은 저장소 밖 작업)
