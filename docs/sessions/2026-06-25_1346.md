---
세션 종료 시각: 2026-06-25 13:46 KST
주요 마감 작업: team_notices 스크립트 이식 RUN2 + 스페이스 네비 트리 숨김·팀/지점 다이어리·스크립트 통일
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-25 (스페이스 네비 후속 + DB 이식)

Code 단독(=총괄팀장). main 직접 push 0(전부 PR). DB(이식)=대표 실행. 신버전 pdnwgzneooyygfejrvbg. 전 작업 is-uat-spacenav(임태성+admin) 격리라 일반 사용자 영향 0. 임태성 실장 계정으로 라이브 검수 완료.

## 오늘 마감 (증거)
- **team_notices 스크립트 이식 RUN2** (DB, 대표 실행): 옛 #918 dual-write 잔재 정리. team_notices의 script 12건 → `scripts scope='team'`(scope_id=4팀 5fccd362) 이식 + script·memo team_notices 소프트삭제 COMMIT. RUN1(BEGIN~ROLLBACK 검증: A=team 스크립트 +12, B=순수공지 team14/branch4만) 통과 후 RUN2. 마이그레이션=`docs/migrations/2026-06-24_migrate_team_notices_scripts_to_scope.sql`. 공지게시판 표시분리(#954)는 어제 라이브됨.
- **#955 바텀시트 안 좌측 트리 숨김** (PR 머지): `body.is-uat-spacenav #spTree{display:none}`(UAT 한정, DOM 보존). 글로벌 사이드바 카드 드릴다운과 겹치던 이중 메뉴 해소. 트리 전용 항목을 글로벌 사이드바(sn2-only)로 이동 — 원수사 자료실(`_navInsurerVaultSp`→blib_ins), 보험 Q&A(`_navQnaSp`→qna), 스페이스 설명서(`_navSpGuide`→`_osStartGuide`). 공유함=UAT 진입점 제거(P3 전체 폐지 예정). 사이드 nav가 숨겨진 트리 노드 클릭으로 기존 라우팅 재사용(라우터 재작성 0). 비-UAT 일반 사용자는 트리 유지(영향 0).
- **#956 팀/지점 스페이스 공유 일정 다이어리 캘린더** (PR 머지): `loadCal(p, scopeOpt)` scope 필터 추가 — personal(author_id·기존)/team(team_id+team_internal)/branch(branch_id+branch_internal), scope는 calendar state 보존. `_spDiaShow(host, scope)` `#mys-today` 재사용·팀/지점 모드는 캘린더만(`.sp-shared-cal`로 개인 대시보드·이야깃거리 숨김). 팀/지점 카드에 '다이어리'(공유 일정) 추가. 소속 team_id/branch_id=`_cospLoadMe()`(DB 직접·캐시 stale 방지). **calendar_events `calendar_select` RLS에 team_internal/branch_internal SELECT(my_team_id()/my_branch_id()) 이미 완비** → 팀원 공유분까지 노출, 추가 SQL 0(진단 먼저로 확인).
- **#957 팀/지점 스크립트 레이아웃=마이 스페이스와 동일** (PR 머지): `_snScopeScripts` 아코디언 → 마스터-디테일 split 재작성. 기존 `.split.scr-mode.cosp-split`/`.list-col`/`.item`(`.badge`/`.it`/`.id`)/`.viewer-col`/`.viewer` CSS 그대로 재사용 → 마이 스페이스 스크립트와 동일 레이아웃. `_snScrPick(i,btn,keepList)`(데스크탑 첫 항목 자동선택·모바일 show-viewer 토글+'목록으로'). 공유 스크립트=읽기 전용. 본문 URL 자동링크 유지.

## 라이브 상태
- main 1e6c451. #955·#956·#957 전부 머지·GitHub Pages 라이브. 임태성 계정 검수 완료. 작업 브랜치 3개 머지 후 삭제.

## 미완 / 다음 1순위
- **P2-b-3b 공유 알림 + send-push 팀필터 수정** = 6/23 푸시 broadcast 사고(trg_notify_team_notice 비활성 차단 중, 메모리 project_team_share_push_broadcast_incident)와 묶어 동시 해소. 트리거가 team_id 전달 + send-push가 user_id join으로 같은 팀만 푸시 → 검증 후 트리거 재활성. ⚠️수정·검증 완료 전 트리거 재활성 금지.
- P3 전체 개방(is-uat-spacenav 게이트 해제)·공유함 전체 폐지·③7명 team정합화·고아구독 10a859ec.

## 교훈
- 트리는 비-UAT 유일 네비라 통째 삭제 대신 UAT 한정 숨김(영향 0) + DOM 보존으로 기존 라우팅 재사용.
- DB 작업 전 신버전 확인 + RUN1(ROLLBACK 검증)→RUN2(COMMIT) 2단계, 검증 SELECT는 마지막 단일 그리드로.
- calendar_events 공유 SELECT RLS는 이미 완비 — 진단(pg_policies 읽기) 먼저로 불필요한 정책 SQL 회피.
- 마이/팀/지점 레이아웃 통일 = 새 마크업 만들지 말고 기존 CSS 클래스 재사용.
- 소속 기준(team_id/branch_id)은 os_user 캐시 말고 _cospLoadMe()/DB 직접.

## 메모리 갱신
- `project_space_nav_p1_p2_track` 갱신(RUN2 완료 + #955·#956·#957 + 교훈, 다음 1순위=send-push 팀필터).
