---
세션 종료 시각: 2026-07-28 23:26 KST
주요 마감 작업: 상담관리 다음세션 작업 (A)(B) 완주 + 예약 즉시팝업 교정 (PR #1483~#1485 라이브)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-28 (밤)

**main 최신 `72feb26`. 직전 세션이 "다음 세션 작업"으로 예약해둔 상담관리 (A)(B)를 이번 세션에 완주하고, 예약 팝업 UX 교정까지 라이브 반영.** 총괄=오케스트레이터(위임·검수·머지·라이브 확인 종결), 구현=general-purpose(os-builder 대행, 커스텀 3티어 에이전트 미로드 세션).

## 이번 세션 완료 (PR #1483~#1485)

### (A) 상담내용·비고 카드↔표 양방향 저장 — #1483 `28a650e`
- **착수 전 실측 조사에서 세션노트 가설과 다른 사실 발견**: 상담내용이 카드/표에서 **서로 다른 저장소**를 쓰고 서로를 안 읽었음.
  - 카드뷰 상담내용 = `profile.consult_log`(jsonb 배열, 날짜별 회차 누적)
  - 표뷰 상담내용 = `sales_consultations.memo`(별도 테이블, 최근 1건 덮어쓰기)
  - 비고 = `profile.비고`(표뷰 전용, 카드폼엔 비고란 없었음)
- **설계 확정**: 상담내용 정본을 **`consult_log`(이력)로 일원화**(정보 유실 없는 방향). 기존 표 데이터는 **폴백 읽기로 무손실**(consult_log 없으면 `_ciMemoMap` 폴백). 비고는 카드폼에 란 신설.
- **구현**: `_ciSheetMemo`(표셀·CSV 표시=consult_log 최신 우선·memo 폴백), `_ciPatchProfile`(**서버 재조회→해당 키만 병합→PATCH** 공용 헬퍼, `_cfSave` 원칙), `_ciSaveSheetConsult`(표뷰 편집=최신 회차 text만 수정·나머지 보존), `_ciCellCommit` memo 분기 교체, `_ciNewRowCommit` 새행 memo→consult_log 첫 회차, 카드폼 `#cif-bigo` textarea 신설+로드/저장/임시저장 배선. `_ciSaveProfileKey`도 `_ciPatchProfile` 경유로 통일(비고·인입유형까지 서버 재조회 병합=안전↑).
- **주의**: `_ci*` 코드는 상담관리↔고객관리 공유(#1479 확인)라 **고객관리에도 동일 반영**. `_ciSaveMemo`(옛 sales_consultations 경로)는 미참조 dead code로 정의만 잔존.

### (B) 표뷰 상담상태 '예약' 선택 시 일정 팝업 — #1484 `1f8ff56`
- 기존 `_ciResvOpen`/`_ciResvSave`(calendar_events `source_type='personal'`·customer_id 미저장=단순 개인일정) 재사용.
- `_ciResvOpen(nameArg, dateArg)` 옵션 인자 추가(무인자=카드폼 경로 무접촉·회귀0), 표뷰는 **행 이름·오늘 날짜 명시 주입**(총괄 검수에서 날짜 stale 비대칭 1건 잡아 보강 `09593a3`).

### (B-fix) 예약 팝업을 '선택 즉시'로 교정 — #1485 `72feb26`
- **대표 지적**: '예약'을 선택하는 **순간** 팝업이 떠야 하는데, 총괄이 처음 `_ciSaveCust` **저장 성공 콜백** 안에서 팝업을 띄워 "다음 액션(커밋·서버저장) 대기" 구조가 됨.
- **교정**: 표뷰 status select의 **`change` 이벤트에 훅**(`_ciCellEdit`)—'예약'이면 즉시 `_ciResvOpen(이름, 오늘)`. `_ciCellCommit` 저장 콜백의 팝업 호출은 제거(팝업은 change 한 곳=중복 없음, 상태 저장은 커밋 흐름 유지). 카드폼 `_ciPickStage` 무접촉.

## 다음 세션 첫 작업 — v3 타이포 적용 (조사 완료·확정안 대기, 착수 전 대표 승인 필요)
대표 "지금 작업한 걸 v3 타이포로 수정해봐" 지시 → 총괄이 **추정 착수 금지**(v2토큰·타이포 사고 교훈)로 **실측 조사부터 완료**(세션 종료 중 결과 회수). **구현 미착수** — 다음 세션은 아래 확정안으로 **승인만 받고 바로 os-builder 위임**.

### 조사 결과 (코드 근거 확정)
- **v3 타이포 = 공용 토큰 + 페이지 전용 인라인 혼합.** 공용 토큰 `css/tokens-v3.css`(`--text-body:16px`·`--text-title:18px`·`--text-heading:20px`·`--leading-relaxed:1.7` 등)는 **app.html에 이미 링크됨(`app.html:39`)**. 실제 지식페이지 적용값(`insurance/*/index.html` `html.v3` 인라인)=본문 16px·**행간 1.85**·읽기폭 710(표 760)·keep-all·h1 27/h2 20/h3 19. 공용 `.knw` 클래스는 미채택(iframe 로드라 불필요).
- **상담관리 `.ci-*`는 현재 v3 미적용** — app.html은 tokens-v3만 링크(tokens-v2/tokens.css 미링크), `.ci-*`는 `html.v3` 스코프 밖. 현재값(`app-core.css --ts-*`): 카드폼 섹션라벨 `.ci-sec` 13px·필드라벨 12px·입력 14px / 예약팝업 제목 15px·라벨 12.8px·입력 13.8px / 표뷰 `.ci-sheet` 본문 13px·헤더 12px(`nowrap`·`min-width:1220px`).
- **`.ci-bigo-ta` 전용 CSS 없음(확정)** — `app.html:17081` HTML에만 클래스명 존재, `.ci-bigo-ta{}` 선택자 부재 → `.ci-ffld textarea`(14px) 상속. **v3 본문값 새로 얹기 가장 쉬운 지점.**

### 총괄 권고 확정안 (다음 세션 이대로 승인 요청 예정)
- **적용 방식**: `html.v3` 인라인 복제 X → **이미 링크된 `tokens-v3.css`의 `--text-*` 토큰을 `.ci-*` 규칙(`css/app-views.css`)에서 참조**(공용 토큰 재사용·회귀 최소).
- **카드폼(적용)**: 섹션라벨 `.ci-sec` 13→`--text-title`(18), 입력·비고 `.ci-ffld`/`.ci-bigo-ta` 14→`--text-body`(16)+행간 `--leading-relaxed`(1.7). 필드 라벨은 12~13 유지(폼 라벨 밀도).
- **예약 팝업(적용)**: 제목 15→18, 라벨 13, 입력 14→16(폼과 통일).
- **표뷰 시트(표준 일부 제외)**: 밀도 도구라 **읽기폭710·행간1.85 제외**(스펙 §4가 표에 본문폭 강제 금지). 폰트만 헤더/본문 12·13→13·14 소폭 상향에 그침.
- ⚠️ `.ci-*`는 상담관리↔고객관리 공유라 v3 타이포도 **양쪽 동시 반영**(의도된 일관 적용). 게이트 안 화면=대표 실로그인 육안 필요.
- 원천: `css/tokens-v3.css` · 스펙 `docs/specs/knowledge_page_readability_shell_spec_v1.md`(부록 C=제목위계) · 적용 예 `insurance/caregiver-history/index.html`.

## 대표 실화면 육안 잔여 (게이트 안 화면·총괄 대행 불가)
1. 표↔카드 상담내용 양방향·카드 비고 입력→표 반영
2. 표뷰 '예약' **선택 순간** 팝업 즉시 표시(이번 교정 핵심)
3. 카드폼 비고 textarea 미관(`.ci-bigo-ta` 전용 CSS 미정의 — `.ci-ffld full`로 폭은 잡힘) — 걸리면 v3 타이포 작업과 함께 정리

## 교훈(박제)
- **상태 선택형 트리거는 저장 왕복이 아니라 선택 순간(change)에 즉시 반응이 기본** — 총괄이 "저장 성공 후 팝업"으로 넣어 대표 "이런 걸 ai가 몰라?" 지적(#1485 교정). select change=즉시 UI, 저장은 커밋 흐름에서 별도.
- **profile jsonb 저장은 서버 재조회→키만 병합→PATCH** — 로컬 캐시 통째 덮으면 다른 키 유실. 이번에 `_ciPatchProfile` 공용 헬퍼로 승격.
- **착수 전 실측 조사가 유실 막음** — (A) 조사 없이 "같은 칸 통일"했으면 카드 이력 붕괴. 정본 모델 확정+폴백 무손실이 정답.
- **모호한 지시("v3 타이포")는 추정 금지·코드에서 대상·실효위치 확정 후 착수** — 이번엔 조사부터 위임(다음 세션 회수).

## 상태
- main 최신 `72feb26`·워킹트리 clean(미추적 tmp-insurance-factory-*.png 33개=기존 잔존, 별건). 관련 메모리: `feedback-proactive-perf-not-reactive`·`feedback-keep-it-simple-not-overthink`·`feedback-goal-proven-stop-fighting-tools`.
</content>
</invoke>
