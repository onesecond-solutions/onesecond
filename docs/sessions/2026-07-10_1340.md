---
세션 종료 시각: 2026-07-10 13:40 KST
주요 마감 작업: 팀/지점 공유 업무노트 수정·삭제 문제 완결(수정 DOM버그·매니저 RLS·프론트 버튼) + DB/EF 실행 원칙 START PROTOCOL 박제
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-10 오후 (13:40)

> **성격:** 총괄팀장 Code 단독 + **위임 체제**(조사·구현 담당자 위임 / 총괄=조율·검토·확정·보고). 신버전 `pdnwgzneooyygfejrvbg`. 제품 전부 PR, 문서/세션/CLAUDE.md만 main 직접. **운영 DB 변경 1건 실제 반영**(매니저 RLS, CI 채널 경유·대표 승인). PR #1213·#1214·#1216·#1217 라이브 + CLAUDE.md 대원칙 1건.

## 오늘 마감 (라이브 반영·증거)

### 1) #1213 — 상담관리 유형 = 임태성 실장 게이트 전용 (라이브)
업무노트 유형 5종 중 **상담관리(care)** 를 임태성 게이트(98c5f4f9)에서만 노출. 배지·유형 필터칩·글쓰기 세그먼트 **3자리 전부** 단일 게이트 소스(`_scrTypesVisible()`)로 격리. 게이트 밖 사용자는 상담관리 태그 카드가 '미지정'으로 표시, 칩·글쓰기 선택지 미노출. 안정키·DB 무변경(표시 레이어). 3단계 롤아웃(임태성→4팀→전체) 1단계 정합. 대표 지시(배지+글쓰기 둘 다).

### 2) 팀/지점 공유 업무노트 수정·삭제 문제 = 완결 (진단→3산출물)
대표 신고: 임태성 게이트에서 **팀·지점에 공유한 업무노트 글이 수정·삭제 안 됨.** 담당자 위임 조사로 규명:

- **원인(확정, RLS 아님):** `openScriptInline`이 편집 폼을 마이스페이스 전용 DOM(`#mysViewer`)에 **하드와이어**(app.html:13944) → 팀/지점 화면(`#sn-scr-viewer`)에서 수정 클릭 시 폼이 현재 화면에 안 뜸. **owner_id 불일치 아님**(담당자 2명 독립 확인: owner_id=auth.uid=public.users.id 단일값). 삭제는 본인 공유 사본이면 원래 정상(`_snScrDelete` 자체 완결).
- **#1214** 수정 폼 호스트 라우팅 정정(`#sn-scr-split` 표시 감지→`sn-scr-viewer`/`mysViewer` 분기 + 저장 후 sn 목록 리프레시). **라이브.** → 공유 글 수정 됨.
- **대표 결정:** 팀/지점 공유물 관리 권한 = **본인 + 매니저(실장·지점장)**.
- **#1216 → 워크플로** 매니저 RLS: `scripts_update_own`/`scripts_delete_own`을 `본인 OR is_admin OR (is_manager AND 소속 팀/지점 scope_id 일치)`로 확장. 권한 격리(타 팀/지점 불가) 보장. **CI 채널(db-migrate.yml)로 apply + postverify passed = DB 실반영.**
- **#1217** 프론트 매니저 버튼 게이트: `_snScrPick` 버튼 노출을 `_isOwner || _isMgrShare()`로 확장. 비매니저 회귀 0(서버 RLS가 최종 게이트). **라이브.**

### 3) ★DB/EF 실행 채널 회귀·교정 + START PROTOCOL 박제 (39fc972)
매니저 RLS 실행 방법으로 총괄이 **GitHub Secret에 SUPABASE_ACCESS_TOKEN 저장→Actions 방식을 제안**(지난주 이미 실패한 절차) + 대표에게 SQL 콘솔·`.env.local` 편집·PowerShell 붙여넣기 요구 = **대표 3연속 교정(회귀 판정).**
- **Post-mortem:** 지난주 실패 원인 = URI 통짜 secret(`SUPABASE_DB_URL`) 파싱 percent-decode/@분리가 **비밀번호 오손** → password auth failed. **07-04 분리형 PG* 방식 전환(7d6370e)으로 해소**, 07-09·07-10 반복 성공.
- **기존 작동 채널 발견:** `.github/workflows/db-migrate.yml`("DB Migration (guarded)"). 직접 psql→Session pooler, 비밀=`PGPASSWORD`(production-db Env secret) 하나. `gh workflow run -f file=<db/migrations/파일>` → deploy가 대표 1클릭 대기 → 대표 명시 승인 시 `gh api .../pending_deployments`로 AI팀 실행(gh가 대표 계정 인증됨). 파일규격=단일 begin;commit;+DOWN블록+postverify_<name>.sql 필수.
- **CLAUDE.md START PROTOCOL에 'DB/EF 실행 원칙' 대원칙 박음** + 메모리 2건(`feedback_db_exec_via_ci_not_daepyo_console`·`project_db_migration_ci_channel`).

## ⚠️ 미완/이월 (다음 세션)
- **매니저 role 실계정 라이브 스모크 테스트** — 매니저가 팀원 공유 글 수정·삭제 되는지 실계정 실측(현재 postverify·프론트·정책텍스트 검증까지 완료, 실UX만 남음). 매니저 테스트 계정 필요.
- **소식지 EF `newsletter-sign` 배포** — 이월 지속. ⚠️ psql 마이그레이션 채널(db-migrate.yml)로는 **안 됨**(EF는 별도 채널=supabase functions deploy). EF 배포 채널 별도 규명 필요(대표 콘솔 요구 없이 AI팀 실행 경로 찾기).
- **업무노트 미지정 22건 재분류**(대표 편집).
- **상담관리·실비/라인업 승인 트랙** — 오늘 세션 시작 브리핑에서 1순위로 올렸으나 공유글 트랙에 밀려 미착수. 산출물 4종 커밋 전 검토 대기(07-09 세션).

## 교훈 (중요)
- ★**대표 = 방향·승인·외부 계정 인증 1회까지만.** SQL·콘솔·파일편집·PS·토큰 채팅노출·**실패 절차 반복** 요구 = 즉시 회귀. DB/EF 실행은 AI팀이 기존 CI 채널로. (START PROTOCOL 박제)
- ★**실패한 방식 재사용 전 post-mortem 먼저** — run id·실패지점 분류·성공 근거. 성공 근거 없으면 대안. 이번에 "지난주 실패 채널을 원인 규명 없이 재제안"이 핵심 회귀였음.
- ★**새 요청도 기존 자산·채널 실측 먼저** — 공유글 수정 원인이 RLS일 거란 총괄 심증이 틀렸고(실제=DOM 라우팅), DB 실행 채널도 이미 작동본이 있었음. 추정 착수 대신 조사 위임으로 규명.
- 총괄 직접 실행 금지→위임(대표 재지적). 총괄=조율·검토·확정·보고 계층 유지.

> 정합: CLAUDE.md § DB/EF 실행 원칙 · `feedback_db_exec_via_ci_not_daepyo_console` · `project_db_migration_ci_channel`
