---
세션 종료 시각: 2026-06-29 20:15 KST
주요 마감 작업: 영업노트 데이터 적재 완료(고객2583/상담2621) + 화면 v0~캡처OCR 구축(#1014~#1018) + 회사허브 펼침면 썸네일(#1012·#1013)
생성 도구: Claude Code /session-end
---

## 세션 인계 노트 — 2026-06-29 오전~낮 (사무실 PC, 총괄팀장 Code 단독)

신버전 `pdnwgzneooyygfejrvbg` 이 세션 대표 확인. 제품 머지 다수(전부 임태성 실장 게이트 안 = 일반/어드민 무영향). 배포 게이트 전용 정책 유지(GitHub Pages 자동). main 직접 push 0(전부 PR — 한 번 실수로 main 직접 커밋했으나 즉시 feature 브랜치로 이동·PR로 정정, 라이브 영향 0).

### 1. 회사 검색 허브 소식지 썸네일 (#1012·#1013 머지·라이브)
- 펼침면(가로 1031×728 등) 소식지 표지 = 절반만 잘라 세로 통일. #1012 오른쪽 가정 → 라이브 검수로 #1013 왼쪽 면으로 정정.
- **★미해결**: 대표 "카드 안에서 이미지가 오른쪽 치우침" 재제기 → 헤드리스 Chrome 렌더+CDP 픽셀측정 결과 **삼성·동양 2건은 중앙 정상**(표본 한계). Chrome 검수팀 분석으로 **펼침면 표지가 회사/월호마다 좌/우 다름** 판명 → 현재 왼쪽 고정이라 우측표지 소식지는 내지가 보임. **해결=좌/우 면 컬러풀함 픽셀분석으로 표지 자동선택**(구현 전 헤드리스 검증). 대표 "영업노트 먼저" 결정으로 **보류**. 메모리 `project_thumbnail_spread_cover_autoselect_pending`. (헤드리스 렌더+CDP 도구 scratchpad 구축됨)

### 2. 영업노트 데이터 적재 ✅완료 (DB)
- STEP1(상담 2621행)에 주민번호 평문 ~570건(c_raw) 발견 → Chrome 실행 충돌 → **정규식 마스킹**(`[주민번호]`, 잔존0·내용/이름/전화/생년월일 보존).
- Chrome 적재가 헛돌아(테이블 미생성) **대표가 직접 6청크 SQL Editor 붙여넣기로 적재**(2621). 스테이징 검증 6/6 일치(2621/2583/230/35/빈날짜32/형식이상0).
- 함정: 생년월일 `1951-10-96` 오타 1건 → date 캐스팅 에러 → 정규식 안전캐스팅으로 해결.
- **대표 승인 → STEP2 COMMIT 이관 성공**: sales_customers **2583** / sales_consultations **2621** 영구저장. staging DROP 완료. 마스킹으로 본테이블 memo도 주민번호 평문 0.

### 3. 영업노트 화면 v0~OCR 구축 (#1014~#1018 머지·라이브, 임태성 게이트 `_canSeeSalesNote` 98c5f4f9 3중차단)
- #1014 조회 v0(좌 고객목록/우 상세+상담 타임라인, RLS owner 본인만)
- #1015 CRUD(신규·수정·소프트삭제 deleted_at)
- #1016 모달 폐기 → 우측 인라인 폼(스크립트 게시판 구조) + 영업상태 **노션 8단계**(신규DB·부재·예약·진행중·제안서발송·보류·클로징·청약완료)
- #1017 osRich 공용 리치에디터(상담 메모) + Ctrl+V 이미지 붙여넣기(opts.pasteImage 한정 → 스크립트/공지 무영향)
- #1018 **캡처 OCR 자동이식**: '캡처 자동입력' 영역 → Ctrl+V → [캡처에서 정보 읽기] → Gemini 비전이 6항목(이름·성별·생년월일·나이·연락처·고객유형) 추출 → 폼 자동채움 → 확인·수정 후 저장
- **고객유형(유입플랫폼=뱅크샐러드/카카오페이/보맵/키워, profile.customer_type) ≠ 상담채널(channel) 분리.** 나이·고객유형·note_html=profile jsonb(새 컬럼 0). DB 18컬럼이 설계 그대로 수용(정형9 + profile/health jsonb).
- 캡처 원본 이미지 미저장(휘발) · 자동저장/백그라운드 분석 없음(직접 실행만).

### 4. Edge Function gemini-customer-ocr (작성·머지, 배포 미완)
- `supabase/functions/gemini-customer-ocr/index.ts` 신규. 이미지 base64 본문 수신(버킷·저장 0) → Gemini 비전 → 정형 6항목 JSON. GEMINI_API_KEY(gemini-card 것) 재사용. 상담내용 정리·요약 안 함.
- **⚠️ 배포 미완** → Chrome 검수팀에 **배포+실검수 통째 위임**(작업지시서 전달함). 배포 전 [읽기]=404(폼·붙여넣기는 정상). **다음 세션 1순위 = Chrome 배포·검수 결과 받기.**
- 비노출 검증은 **시크릿창**으로(실 설계사 계정 로그인 금지 — 대표가 멈춤, 옳음).

### 5. 김실장 공식자료 등록 자동화 트랙 (논의·보류)
- 김실장(GPT)이 보험사 공식 상품자료 검색·내용검수·키워드·중복확인→reviewing 초안 등록 / 총괄팀장 시스템검수→발행 / 대표 예외만.
- 전체 Supabase MCP 직결 금지 → onesecond 단일 등록 API(Edge Function) 뒤 service_role 은닉 + MCP 3도구. 서버강제 status=reviewing. 영업노트/users 격리. 검수통과분만 통합검색 "지식" 합류. 테이블=기능만 확정(official_materials 미확정→knowledge_entries 재사용 우선 확인). 등록 API 먼저, 연결방식 나중. **영업노트 후 착수.** 메모리 `project_official_material_submission_pipeline`.

### 다음 세션 우선순위
1. **영업노트 OCR**: Chrome 배포·검수 결과 받기 → 정확도 보고 프롬프트(고객유형 추정·생년월일 정규화) 보정
2. **썸네일 펼침면 표지 자동선택**(좌/우 자동판정, 헤드리스 검증 후)
3. 영업노트 후속: 상담 추가·빠른메모·AI 상담정리·민감정보(병력/계좌/주민) 전송정책·마스킹(실사용 후 별도)
4. 김실장 공식자료 등록 자동화 착수(knowledge_entries 재사용 확인부터)

### 신설/갱신 메모리
- 갱신: `project_sales_note_import_masked_chrome`(적재완료+화면v0~OCR+Edge배포대기)
- 신설: `project_thumbnail_spread_cover_autoselect_pending` · `project_official_material_submission_pipeline`

### 교훈
- 큰 그림(영업노트 입력폼·AI)은 메모리/이전 논의 먼저 확인(추측 폼 만들었다 두 번 재작업: 06-20 세션≠대표가 말한 대화, 영업상태값 임의 지어냄). 대표가 "전에 얘기했다" 하면 메모리·세션 검색부터.
- "화면 그대로/치우침" 신호 = curl 라이브 + 헤드리스 픽셀측정으로 실증(추측 패치 금지). 단 표본 편향 주의(2건이 우연히 같은 케이스).
- 개인정보 외부 AI 전송·민감정보(주민/계좌/병력)는 총괄팀장이 반드시 1회 고지·승인 자리.
- Edge Function 배포는 Chrome 검수팀 위임(작업지시서) — 대표 직접 실행 최소화.
