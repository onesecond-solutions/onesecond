---
세션 종료 시각: 2026-07-08 21:22 KST
주요 마감 작업: 첫 공개 A묶음 + 홈 인덱스 전환(B안) + 라이브 검수 유출·게이트 다수 (#1184~#1197)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-08 저녁 (21:22)

> **성격:** 첫 공개 A묶음 → 홈 인덱스 전환(B안) → 라이브 검수로 나온 유출·게이트 다수 수정. **총괄팀장 위임 체제로 전환**(대표 지시: 실행은 담당자 위임, 총괄=조율·검토·머지·보고). 총괄팀장 Code 조율, 실행은 worktree 격리 담당자 병렬. 신버전 `pdnwgzneooyygfejrvbg`, main 직접 push 0(전부 PR).

## 오늘 마감 (전부 라이브 반영·검증, PR#)

1. **#1184 첫 공개 A묶음**: app.html `home_test1` 시안 iframe 주입 제거(누구나 시안 노출 Blocker)·시안 title 정식화·noindex·description·index.html `og:url` `home_v2.html`(404)→루트. 확정 문구=title "원세컨드 — 보험설계사를 위한 검색·자료·업무노트 플랫폼".
2. **#1185** 타이포 가드 녹색화(`.hs2-recent-x` 11px→`var(--ts-badge)`).
3. **#1186** 공개 description에서 '원수사자료실' 제거(대표 교정).
4. **#1187 ⭐홈 인덱스 전환(B안)**: 비로그인 첫 화면=app.html 검색 홈(옛 랜딩 강제경유 폐지)·로그인/가입=`_osAuth` 지연로드 인앱 모달(auth-modal.js **전역로드 금지**=2026-07-05 로그아웃 사고 회피, 로그인 사용자는 미호출→세션 무영향)·사이드바 'about' nav→landing(=about 소개 전용).
5. **#1188** 비로그인 헤더: A영역 로그인·회원가입·전산신고 순서 + 알림벨·계정 아바타 숨김 + 사이드바 라벨 'about'(영문).
6. **#1189** 비로그인 홈=로그인 홈 동일(`is-uat-homedesk` 전원)+로그인 게이트 모달(`_osLoginGate`)+헤더 버튼 pill(각짐 원인=미정의 `--radius-pill`→`--radius-full`).
7. **#1190** 사이드바 about 안 보임(원인=`app-views.css` 홈데스크 최소 사이드바 화이트리스트 누락)+새로고침 nav svg FOUC(`is-uat-spacenav` 첫 페인트 전 부착).
8. **#1191 ⭐auth-modal.css 자족화**: 홈에서 로그인 모달 CTA 버튼이 투명(실종)=로그인 불가 치명버그. 원인=긴 토큰명(`--color-brand` 등)이 landing 인라인 :root에만 있고 app.html엔 짧은 별칭(`--ac` 등)만. `.auth-overlay` 스코프 토큰 정의(`var(--ac,#6366F1)`)로 자족화(CSS만·JS무수정).
9. **#1192** 게이트 정책: 비로그인 카드 클릭=열람 허용, 쓰기/저장만 게이트.
10. **#1193** 홈 하단 푸터 4항목(© 2026 onesecond·카카오톡 문의하기·이용약관·개인정보처리방침), 바텀시트 겹침0.
11. **#1194** 홈 6카드 클릭 시 상세 상단 정렬(워크스페이스 라벨 top=about 라인, delta 0)+카드↔푸터 간격 확대.
12. **#1195 ⭐비로그인 개인데이터·검색 유출 차단**: `loadCal` 토큰가드 없어 owner 미해결 시 `calendar_events` 필터없이 통째 fetch(임태성 데이터). 로더 토큰가드+검색창 placeholder "로그인 후 사용 가능합니다"+비로그인 검색 실행 게이트.
13. **#1196** 원수사자료실·보험Q&A 홈 카드 임태성 실장 게이트(`98c5f4f9`) 전용 노출(일반/비로그인 숨김, `_canSeeRoadmap` 패턴).
14. **#1197** 비로그인 글쓰기 진입 전수 게이트: 저장함수만 막혀있고 작성 폼 여는 진입점 9곳(`openCalAdd`·`openDiaryPanel`·업무노트·메모·`ivWriteGate`·`cospWrite`·자료실업로드 등) 뚫림→전부 게이트.

## 대표 확정 정책 (검색·게이트)

- 비로그인=검색 차단(placeholder+게이트), 로그인=본인 소속(개인/팀/지점/회사) 격리 결과(RLS 상속, service_role 우회 없음).
- 원수사자료실·보험Q&A: 임태성 실장 게이트(`98c5f4f9`)에서만 홈 카드·입력·데이터 작업 노출. 일반/비로그인은 홈 숨김·검색 결과로만(권한 격리). 원세컨드 씨드 데이터로 활용 방향(검수 게이트 전제).

## 조사 결과 (원수사자료실 권한, read-only)

- **입력(글쓰기)**: admin+insurer_* 4종(화면 직접) / GA 실장·지점장은 cospace 경로 RLS 있으나 라이브 화면 경로 불확실 / ga_member·staff 불가.
- **검색/열람**: 원수사자료실은 **통합검색 소스에 미포함**(안 뜸). 열람=지점 격리(GA작성)/회사 격리(원수사·admin작성), seed만 전체. "모든 사용자 검색" 아님.
- ⚠️ **실측 필요**: insurer_* INSERT/SELECT 기반 RLS가 repo 미보존(폐기 설계문서·주석만)→라이브 pg_policies 실측(`rls_overview` RPC).

## 교훈

- ★**게이트 개방 시 "레이아웃 노출"과 "타인 데이터 0"은 별개** — 열람 허용만 검증하고 데이터 0을 안 봐서 유출 2연속(데이터 로드·글쓰기 진입). 앞으로 열람 개방 건은 타인 데이터 0 + 쓰기 진입점 전수 게이트를 항상 검증.
- ★**총괄팀장이 실행에 직접 투입하면 보고체계 붕괴**(대표 반복 지적)→실행 위임·총괄은 조율/검토/머지/보고 계층 유지. worktree 격리로 담당자 병렬+총괄 순차 머지=충돌 0.
- auth-modal.css 자족화(긴 토큰명 페이지 인라인 의존 제거). 사이드바 노출=홈데스크 화이트리스트+FOUC 첫페인트 전 부착. 각진 버튼=미정의 토큰(`--radius-pill`)→`--radius-full`.

## 다음 세션 1순위 (대표 "수정할 몇 가지"는 다음 세션 첫머리 확인)

1. **대표 미지정 수정 몇 가지**(세션 첫머리 확인).
2. **원수사자료실 리치텍스트 전체개방 결재**(조사 완료=검색 전체노출 리스크 없음, 입력=admin+insurer4). insurer_* RLS 라이브 실측 보강.
3. **원수사자료실 검색 소스 편입**(통합검색, 지점/회사 격리 상속 유지).
4. **씨드 승격 채굴 파이프라인**(원문→ai_draft→검수 approved만 공용, 개인정보·저작권·공개범위 준수).
5. **근본 강화(유출 후속)**: 로그아웃/guest 전환 시 `os_token` 확실 무효화 + `calendar_events` anon SELECT RLS 재점검.
6. **첫 공개 실행 트리거**(검색엔진 등록·더원지점 실사용 전환·피드백 수집 로깅) 타이밍 결재.

> 정합 진실 원천: `docs/strategy/organism_architecture_v1.md` · `docs/work_orders/2026-07-07_first_public_launch_checklist.md` · 메모리 `project_home_index_guest_search`.
