---
세션 종료 시각: 2026-06-23 03:26 KST
주요 마감 작업: 검색 홈 전면 개방 · 댓글 본인 수정·삭제 · AI 글 다듬기 배포·명칭통일 · 채굴 진단 · WAV 조사
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-23 심야 (03:26)

> Code 단독=총괄팀장. 제품 머지=총괄팀장 실행(대표 결재). DB(RLS)·Edge 배포=대표·검수팀 직접. 신버전 `pdnwgzneooyygfejrvbg`.
> ⚠️ 별도 AI 노트 미전달 → 총괄팀장(Code) 직접 작성. 세션 시작 START PROTOCOL 보고 후 진행.

## 오늘 마감 (PR #922~#926 전부 머지·라이브)

### 1. 댓글 본인 수정·삭제 (#922)
- 대상 = 바텀시트 마이/팀/지점 스페이스(`entry_comments`) + 게시판 보험Q&A·원수사자료실(`comments`). ⚠️ 작업지시서 `post_comments`는 코드베이스에 **존재하지 않음** → 실제 게시판 댓글 테이블 = **`comments`** 실측 정정.
- 공용 모듈 `_cmtMine`/`_cmtOwnActs`/`_cmtEditStart`/`_cmtEditSave`/`_cmtEditCancel`/`_cmtDelete`(tbl 인자 일반화). 본인 댓글에만 버튼, 인라인 편집·삭제 확인창·`수정됨` 표시·DOM 즉시 반영·XSS escape(data-raw 원문 보존). 렌더러 3곳 배선.
- **STEP 0 실측**: 기존 정책 대부분 존재(`own comments update`/`own comments delete`·`comments_admin_delete`·`entry_comments_delete`=본인 OR admin) → **유일 공백 = entry_comments UPDATE 정책 없음** → STEP 1을 `entry_comments_update_own`(`author_id=(auth.uid())::text`, USING+WITH CHECK) **1개만 추가**로 축소(중복 방지). author_id = 양 테이블 모두 text.
- 대표 STEP 1 실행("Success. No rows returned"=정상). 라이브 검수: 본인 수정·삭제 성공(임태성 계정). 무인증 anon SELECT/PATCH/DELETE → 두 테이블 전부 `[]`(0행 차단) 실증. **기능 완료 처리**.
- 잔여 = 로그인된 **타 사용자** 격리 실계정 교차검증 1건(backlog "보안 교차검증 잔여"). RLS 정책상 차단 보장되나 실계정 확인만 미완.

### 2. 자동 채굴 진단 — A(재가동 가능) / 인증 차단으로 보류
- 진단 8항목: mine-batch Phase A~F(`v1d`)·DB 배선(`record_mined_entry`/`review_knowledge_entry` RPC·`uq_ke_mine_identity` 인덱스·`knowledge_pipeline_events` 테이블) **완비**. cron mine job 없음(자동 OFF). **`events=0` = 채굴 실행 0회·914 이후 신규 적재 0**. 자동 approved 다겹 차단(코드 status 화이트리스트+RPC invalid_status+검색 approved 게이트).
- ★ **CRON_SECRET 공유 위험 발견**: 6함수 공유(mine-batch·ocr-batch·diary-push·extract-knowledge·send-push·charge-subscriptions) + `ocr-batch-5min`(*/1)·`diary-push-5min`(*/5) cron·`notify_new_post` 트리거가 값 리터럴 사용 → 교체 시 운영 동시 장애. → **mine-batch만 `MINE_SECRET` 전용 인증으로 분리(#923, fallback 없음, 헤더명 x-cron-secret 유지)**. CRON_SECRET 불변 → 운영 영향 0.
- `supabase login`이 집 PC(non-TTY)에서 불가 → dry-run 보류. 재개 경로 backlog 박제.

### 3. WAV→MP3 일괄 변환 1단계 조사 (#924 준비물)
- 실측: WAV **114건**(대표 "70/90"→실측 114) / 미변환 113 / PoC 1 / 0바이트·누락·중복 0 / 원본 100% 존재 / 전부 임태성·branch·`audio/wav` / 크기 <1MB 52·1–5MB 59·5–18MB 3. 포맷 분류(PCM 제외 판단)는 헤더 필요 = 인증 후.
- 집 PC ffmpeg·인증 無 → 변환 미실행(write 0). 회사 PC 재개 준비물 박제: `scripts/wav_mp3/classify.sh`(헤더 64B Range→audioFormat 분류)·`convert.sh`(변환·업로드·playback_path, `--write` 게이트·멱등·연속3실패 중단·원본 불변) + `docs/work_orders/2026-06-23_wav_mp3_batch_handoff.md`(9스텝·전후 스냅샷 SQL·승인 게이트). service_role key는 환경변수만, `_work/` gitignore.

### 4. AI 글 다듬기 (#925 + 검수팀 배포)
- 에러 원인 = **`ai-polish` Edge Function 미배포(404)**(프론트 정상). → 검수팀(Chrome)이 대시보드 에디터로 GitHub raw 코드 그대로 배포 완료(401 정상=verify_jwt·11→12함수·기존 함수/Secrets 무변경). GEMINI_API_KEY 재사용.
- 명칭 통일 '**AI 글 다듬기**'(버튼 2곳+토스트 2곳, #925). 게이트 `is-uat-aipolish`=임태성 유지.
- 대표 제기: 다듬은 문장이 "내 말투가 아님" + 원본 덮어쓰기 → backlog "**A. 말투 보존**(단기 프롬프트 강화→중기 모드 재설계→장기 '내 말투' few-shot 개인화) + **B. 저장 모델**(버전 히스토리, 이원화 전용칸 비권장)". A가 B보다 상위. 골든타임·채굴 데이터품질 연계.
- 잔여 = 대표 실사용 검수(임태성), 전체 개방 별도 결재.

### 5. ★ 검색 홈 전면 개방 (#926)
- `_searchScopeAllow()` → `return true`: 3계정(임태성 naver/kakao·어드민 gmail) 미리보기 게이트 해제 → **전원이 검색 홈을 기본 홈으로**(중앙 검색창+상품/인수/상담 카드+범위 검색). 기존 홈(배너 캐러셀·위젯·'시작하기' CTA)은 검색 홈으로 대체. 하단 철학문구(`is-uat-homephilo`) 게이트도 해제.
- 데이터 격리 = 사용자 토큰 RLS 그대로(게이트는 UI 라우터일 뿐, 보안 경계 아님). `_SEARCH_V2_ALLOW` 배열 롤백 참조용 보존. **롤백 = `_searchScopeAllow` return false 1줄**.
- 머지·빌드 완료(`7c1f46d`, Pages built), CDN 전파 중(강력 새로고침 시 반영). **잔여 = 일반 GA 계정 라이브 검수(홈 전환·검색 누수 0)+미로그인 확인**.

## DDL 적용 기록 (운영, 대표 실행)
- `entry_comments_update_own` 정책 (UPDATE, `author_id=(auth.uid())::text` USING+WITH CHECK)

## 진행 중 / 다음 세션 인계
1. **검색 홈 일반 계정 라이브 검수** — 홈 전환·검색 누수 0·미로그인. 이상 시 `_searchScopeAllow` return false 즉시 revert.
2. **AI 글 다듬기 대표 실사용 검수**(임태성) → 통과 시 전체 개방 별도 결재.
3. **자동 채굴 dry-run 재개** — 회사 PC(TTY) `supabase login` 또는 `SUPABASE_ACCESS_TOKEN` → MINE_SECRET 생성·설정 → mine-batch만 재배포 → 5건 dry-run → DB write 0 확인. backlog "자동 채굴 dry-run 재개".
4. **WAV→MP3 변환** — 회사 PC(ffmpeg+인증) `handoff.md` 9스텝. classify→표본5건→대표 승인→전체.
5. backlog: 댓글 타 사용자 격리 교차검증 · AI 다듬기 말투 보존/저장 모델.
6. 이월: SMS 운영반영 9단계 · 영업노트 import · 결제 #882~886 · #802 · ocr-batch cron `*/1→*/5` 원복(색인 끝났으면).

## 주의·교훈
- **스키마 타입·명칭 단정 금지**: `post_comments`→실제 `comments`, author_id=text 실측 확정. 작업지시서 명칭도 실측으로 검증.
- **CRON_SECRET = 공유 secret**(6함수+cron 2+트리거): 교체 전 영향 전수 확인. dry-run 하나에 운영 전반 흔들지 말 것 → 전용 분리(MINE_SECRET)가 안전.
- **집 PC = ffmpeg·supabase 인증 없음**(non-TTY login 불가). 인증 필요 작업 = 회사 PC 또는 검수팀(Chrome). 환경 정정 시 즉시 보류·박제.
- **큰 변경(전 사용자 홈 전환)** = PR + 머지 결재 + revert 1줄 안전장치. 라이브 검수(Preview 중지).
- SQL 한글 주석 복붙 깨짐(42601) → 주석 제거판 제공.

PR #922~#926 전부 머지. 신버전 DB `pdnwgzneooyygfejrvbg`. 라이브 = onesecond.solutions.
