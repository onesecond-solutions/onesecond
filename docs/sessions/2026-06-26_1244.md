---
세션 종료 시각: 2026-06-26 12:44 KST
주요 마감 작업: 영업노트 import SQL 생성 + RLS 격리 검증 PASS + DB 직접연결 28P01에서 중단
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-26 오후 (영업노트 import 준비 + DB 직접연결 막힘)

Code 단독(=총괄팀장). 제품 코드 변경 0. DB 데이터 변경 0(적재 미실행). 신버전 `pdnwgzneooyygfejrvbg` 확인. main 직접 push 1건(docs/ops SQL 파일, raw URL 확보용·주민번호 0이라 안전).

## 오늘 진행 (영업노트 마무리 트랙 — STEP1 데이터 이관 준비까지)

### ✅ 끝낸 것
- **엑셀 파싱 완료**: `upgrade_20260521/상담리스트_정리.xlsx`(정리_상담리스트 시트) → **2,621 상담행**. 데이터 품질: 전화있음 2,391행·고유전화 2,353·무전화 230·생년월일 표준 2,598·다회상담 35명(최다 3건)·원문 100% 존재(평균 105자·주민번호 평문 포함). 헤더=번호·상담일자·이름·생년월일·성별·나이·연락처·상담채널·상담내용·추가메모·원본행·원문.
- **DB 스키마 확정**(대표 SELECT 회신): `sales_customers`(18컬럼)/`sales_consultations`(10)/`sales_status_history`(7). owner_id=text, RLS `sc_own/scon_own/ssh_own` 전부 `cmd=ALL, (auth.uid())::text=owner_id`. ※ `public.users.auth_user_id` 컬럼 없음 → owner_id엔 **auth.users.id** 사용, import SQL이 `auth.users('bylts@naver.com')`에서 자동 조회.
- **import SQL 2파일 생성**(gitignore 보호 폴더 `upgrade_20260521/`, 커밋 0):
  - `_sales_import_2026-06-26_step1_staging.sql`(1.2MB·2,621행 staging 적재)
  - `_sales_import_2026-06-26_step2_load.sql`(2.9KB·staging→customers/consultations 변환+검증)
  - 설계: 전화 dedup(전화있음 2,353 + 무전화 230행 개별 = **고객 2,583**), 원문 100% 보존(memo에 `상담내용+[추가메모]+[원본]`), 상담일자 빈값 32→`2000-01-01` sentinel, `source_ref='imp:<키>'`로 customer↔상담 매핑. 검증 기대값 2583/2621/230/35/2621.
- **STEP2를 docs/ops에 커밋·push**(`9387b73`, raw URL 확보): `docs/ops/2026-06-26_sales_import_step2_load.sql`. 주민번호 리터럴 0(staging SELECT 기반)이라 커밋 안전. STEP1(주민번호 1.2MB)은 raw 금지·대표 직접 실행.
- **✅ Chrome RLS·개인정보 격리 검증 — 전 항목 PASS**: ①신버전 확인 ②RLS ON 3/3·정책 owner_id=auth.uid() 3/3 ③anon 비로그인 → `[]` 0건(차단) ④노출 뷰/함수 0건. **개인정보 외부 누출 경로 0 확정.** 단 "임태성 본인 보임/타 GA 0건" 실계정 교차는 Chrome 로그인 불가로 미수행 → **화면(STEP2) 완성 후 임태성 로그인 시 자연 마감 예정**.
- **DB 비밀번호 재설정 영향범위 점검**: 저장소에 DB 직접연결 문자열·DB 비번 참조 **0건**. 앱=anon key / Edge Functions 11종·CI(issues-daily)·.env 예시=service_role key(전부 DB 비번과 별개). → **DB 비번 재설정해도 서비스 영향 0**(저장소 밖 외부 도구만 대표 확인 필요).

### 🔴 막힌 곳 (다음 세션 1순위) — DB 직접연결 28P01
- SQL Editor가 STEP1 1.2MB 쿼리를 거부(`Query is too large`) → **Node.js(pg) 직접 연결 방식으로 전환**(psql·supabase CLI 미설치, node·npx만 있음).
- 연결 도구 scratchpad에 구비: `dbwork/`에 pg 설치 + `test_conn2.js`/`inspect_base.js`/`diag.js`/`connect_v3.js`. 접속정보는 대표 PowerShell 창 환경변수에만(`ONESECOND_DB_BASE`+`ONESECOND_DB_PW` 분리, 비번/URI 채팅·로그·git 노출 0).
- **BASE 파싱은 정상 확인**: host=`aws-1-ap-south-1.pooler.supabase.com`·port=5432·user=`postgres.pdnwgzneooyygfejrvbg`(Session pooler 정상 형식·ref 일치).
- **그러나 연결은 계속 `28P01 password authentication failed for user "postgres"`**. 비번 재입력·재설정 후에도 동일. 코드는 connectionString 치환 아닌 **개별 필드 방식**(user 명시 전달)이라 코드 문제 가능성 낮음 → **비번/pooler 인증 자체 문제로 좁혀짐**.
- 마지막 액션(미확인): `connect_v3.js` 실행 → pg에 넘어가는 user가 `postgres.<ref>` ✅인지 + PW 길이/공백을 눈으로 확정하는 출력 대기 중에 세션 종료.

## 라이브 / DB 상태
- 제품 코드(app.html 등) 변경 0. 라이브 무변경.
- main: `9387b73`(docs/ops STEP2 SQL 1건 push). 
- DB: 영업노트 테이블 비어있음(STEP1 적재 미실행). 데이터 변경 0.

## 미완 / 다음 1순위
1. **28P01 규명 → 연결 통과**: `connect_v3.js` 출력으로 user 전달값·PW 메타 확정. user ✅인데도 28P01이면 → ⓐ pooler 비번이 DB 비번과 정말 일치하는지 ⓑ transaction pooler(6543) vs session(5432) ⓒ Supabase pooler 인증 정책. 안 풀리면 IPv4 direct 재시도 또는 Supabase 지원 경로.
2. **연결 통과 후 STEP1 적재**: node로 step1 파일 실행 → `_sales_import_staging` 2,621행 → step2 변환(ROLLBACK 검증 5줄 확인) → 대표 승인 → COMMIT → `DROP TABLE _sales_import_staging`.
3. **영업노트 화면(STEP2 제품·미착수)**: 목록(검색·정렬·체크박스 일괄삭제)+고객카드(기본정보·상담 타임라인·빠른메모·AI버튼 자리 동작없음)+임태성 계정 게이트(`is-uat-salesnote` 류)+Phase1 AI 없음(외부전송 0). feature 브랜치 PR→라이브 검수(임태성: 본인 2,583명 보임 + 타 GA 0건 교차).
4. 이월: 바텀시트 바닥=검색 전용 정리 P1 / send-push 팀필터(trg_notify_team_notice 비활성 차단 중·검증 전 재활성 금지) / 검색·AI 다듬기 검수 / 자동 채굴 재가동 / SMS 운영 반영.

## 교훈
- Supabase **SQL Editor는 ~1MB 넘는 쿼리 거부** → 대용량 적재는 처음부터 직접 연결(node pg) 설계.
- 개인정보(주민번호) SQL은 **gitignore 보호 + Chrome 미전달 + 대표 직접 실행**, 변환/검증 SQL만(데이터 리터럴 0) raw 공개해 Chrome 의뢰.
- DB 접속정보는 **BASE+PW 환경변수 분리 + Read-Host -AsSecureString**로 채팅·로그·git 노출 0. 비번은 pg Client에 **객체 raw 전달**(connectionString 치환 회피→특수문자/인코딩 무관).
- 28P01 디버깅 순서: ①BASE 파싱(host/port/user) 정상 확인 → ②pg 전달 user가 `postgres.<ref>`인지 → ③PW 길이/공백 → 여기까지 정상이면 코드 아닌 **비번/pooler 인증**으로 확정. 로컬 IPv4는 Session pooler(`*.pooler.supabase.com`) 사용.
- DB 비번 재설정 안전성: 저장소가 anon/service_role key만 쓰면(직접연결 0건) 재설정 영향 0 — grep으로 사전 확증.

## 메모리 갱신
- 신설 권장: 영업노트 import/직접연결 트랙(엑셀 2621→고객2583, step1=대표직접·step2=raw, 28P01 미해결, scratchpad/dbwork 도구). ※다음 세션 진입 시 반영.
