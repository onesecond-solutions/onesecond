---
세션 종료 시각: 2026-06-05 18:11 KST
주요 마감 작업: 지식엔진 검색 v1→v1.2 (랭킹·동의어·5단 사용자뷰·핵심요약 이원화·확장 배지) + UI 정리 6건 + canon 정규화
생성 도구: Claude Code 세션 정리 (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-05 (Code 단독, 장시간 마라톤). 오전 UI 정리(배너·스크립트·매니저룸 통합·계정메뉴·전산신고) → 오후~저녁 **지식엔진 "검색 중심 대전환" 1단계**(검색 v1→v1.2). Claude AI가 작업지시서를 단계적으로 내려보냈고, Code가 PR 12건을 단계별 게이트로 진행. **모든 작업 main 직접 push 0건**(전부 feature 브랜치 → PR → 머지). 검색 v1 SQL은 **파일만 작성·미실행**(동결), 동의어 SQL만 팀장님이 직접 실행.

## 🔝 다음 세션 1순위
**BMI 데이터 확정 시 BMI 적재 SQL 채워 실행** — 검색에 "보험사별 기준" 섹션 노출. 해제 조건 2건:
1. **BMI 12개사 본문 심사기준 수치** (회사별 BMI 비교표 원본)
2. **실제 비교 페이지 URL** (현재 `/pages/bmi-compare.html` 플레이스홀더)
→ 둘 확정되면 `docs/migrations/2026-06-05_knowledge_entries_bmi12.sql`(verdict/verdict_detail 포함) + `_knowledge_shortcut_bmi.sql`의 placeholder를 채워서 실행. 그 전까지 BMI·shortcut·추가 채굴 전부 **동결**.

## 완료 (PR 12건, 전부 머지·라이브)

### UI 정리 (오전)
- **#420** 홈 배너 우측 번호 2·3·4 + ✕닫기 = admin 전용. 일반 사용자는 1번 기능만(숫자·박스 비노출). `#app.is-admin` 게이트 재사용.
- **#421** 스크립트 도입인사 복구: 매니저룸 오피스 글쓰기 테스트가 scripts(stage=opening, scope=personal)로 INSERT돼 공용 도입인사에 노출 → 테스트 row `id=66` DELETE(실데이터 손상 0, INSERT였음) + 공용 스크립트 조회에 `scope=eq.global` 추가(개인 스크립트 누수 차단, 분포 global 59/personal 1 실측).
- **#422·#423** **매니저룸 → MY SPACE 통합**(2단계): 실측 결과 두 영역 차이=팀원관리 탭+진입권한뿐(오피스/단체방/Q&A/달력 쿼리 동일). 1단계=팀원관리 골격(mr-main) MY SPACE로 이전(id 유지=함수 0줄 수정)+mgr 권한분기 탭. 2단계=매니저룸 view/nav/라우팅/중복로더 제거(-334줄). **공유 자산(mrWriteModal 단체방 모달, 팀원관리 골격) 보존**. 삭제 심볼 전수 감사=전부 typeof 가드.
- **#424** 계정 드롭다운(#m) 바깥 클릭 닫힘(notifPop 동일 패턴).
- **#425** 헤더 알림벨 왼쪽 "전산신고" 버튼 → 함께해요 전산이상신고 필터. (stale #407 미사용 — 오래된 base라 머지 시 회귀).

### 지식엔진 검색 (오후~저녁)
- **#426** `canon()` 회사명 정규화: 실측 company **64종 → 32 표준명**(별칭/파일명/오타/비회사마커). standalone 검증 통과. (Edge Function 소스, Netlify 무관 / 라이브 반영=재배포 별도, 미실행).
- **#427** 지식엔진 검색 admin 테스트 페이지 신설(`pages/knowledge-search-test.html`, 72건, 클라 검색). app.html 무변경.
- **#428** 검색 v1: 랭킹(제목>카테고리/회사>본문, 동점 최신)·동의어 확장·5단 사용자뷰(핵심요약/상품/보험사별기준/실무/용어/원문)·±60 스니펫·shortcut·**BMI 시드 SQL 4파일(미실행)**. 4컬럼 뷰=admin 토글 보존. tokens-v2.css.
- **#429** 검색 v1.1: 핵심요약 **이원화**(제목정확=정답박스 / 그외=집계박스[N건+카테고리+키워드칩] / 전체보기=숨김)·섹션 상위5+더보기·favicon 억제·추천칩 데이터기반(0건칩 제외)·BMI verdict/verdict_detail 필드. + 메타 버전 v1.1.
- **#430** 루트 `favicon.ico` 실파일(404→200, 라이브 curl 검증). icon-192.png PNG-in-ICO.
- **#431** 검색 v1.2: 확장(동의어) 매치 카드에 **"유사어 ○○" 배지**(직접/확장 시각 구분). 메타 v1.2.

## DB (이번 세션)
- **scripts**: `id=66` 테스트 row DELETE (도입인사 복구). 🟠 실행 완료.
- **knowledge_synonyms**: 테이블 생성 + RLS(select=auth/write=is_admin) + **7행 시드** — 팀장님이 SQL 에디터 직접 실행(유사암=갑상선암·경계성종양·기타피부암·제자리암 4종 고정 등). 🟠 실행 완료. ⚠️ 첫 시도 42601(첫줄 주석 이모지/em대시 줄바꿈 깨짐) → 주석 0줄 순수 SQL 재제공으로 해결.
- **source_type 백필**: 실측 전건 'newsletter' 72건 → **백필 불필요 종결**(옵션A). 파일은 기록용 보존.
- **미실행 동결 SQL**: BMI 12건 / shortcut 1건 (본문수치·URL placeholder, 데이터 확정 후).

## 검수 결과 (크롬)
- 검색 v1.1 5항목 + 0번 버전게이트 + 4컬럼 토글 = 통과.
- 동의어: a(유사암 2→10건 확장) ✅ / b(직접>확장: **동일 섹션 내 기준 통과** 확정 + 확장 배지 보완) ✅ / c(knowledge_synonyms 404→200) ✅.

## 🚨 발견 / 학습
- **PR 미머지 = 라이브 미반영**: v1.1 변경분이 라이브에 없다는 보고 → 원인은 단순 #429 미머지(버그 아님). **재발방지: 메타 버전 표기=코드 버전 연동(v1.1/v1.2), 검수 의뢰 0번=메타 버전 일치+배포 curl 확인 후 의뢰**.
- **favicon은 루트 실파일 필요**: 페이지 내 `<link>`로는 브라우저의 루트 `/favicon.ico` 기본 요청을 못 막음 → 루트 배치 + curl 200 검증.
- **SQL 첫줄 주석 줄바꿈 함정(42601) 재발**: 이모지·em대시·한글 섞인 첫줄 주석이 복사 중 깨짐 → 순수 SQL(주석 0)로 제공이 표준.
- **삭제 작업 = 공유 함수 얽힘 먼저 매핑**: 매니저룸 제거 시 mrWriteModal이 MY SPACE 단체방과 공유 → 삭제 아닌 이전. 삭제 심볼 전수 감사(전부 typeof 가드 = 안전 no-op).
- **canon 실데이터 기반 재설계**: 추측 말고 실측 64종 분포 받아 별칭+부분일치+폴백.

## 미해결 / 이월
- **BMI 데이터 대기** (다음 세션 1순위) — 본문 수치 + 비교 페이지 URL.
- **크롬 v1.2 유사어 배지 재확인** 대기 (라이브 v1.2 배포 검증 완료).
- **stale 브랜치 정리**: `feat/report-button-header`(로컬+원격, #407 obsolete — #425로 대체). 삭제 결재 미응답 → 다음에 정리.
- **canon 라이브 반영**: PR #426 머지됐으나 Edge Function 재배포(PAT) 미실행 → 신규 채굴분부터 정규화 적용(기존 72건은 옛 canon). 채굴 동결 중이라 당장 불필요.
- 동의어 SQL 파일 첫줄 주석 = 다음에 ASCII 한 줄로 정리 예정(줄바꿈 함정 재발 방지).

## 현재 데이터 상태
- knowledge_entries **72건 변동 없음**(ai_draft, run 211bd06e=30·746b7efe=42). 오늘 채굴 0.
- knowledge_synonyms **7행**(신규).
- 검색 페이지 라이브 = v1.2 (onesecond.solutions/pages/knowledge-search-test.html, admin 전용).
