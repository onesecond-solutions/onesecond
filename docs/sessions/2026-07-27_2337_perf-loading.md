---
세션 종료: 2026-07-27 (23:37) — 상담관리 카드↔표 호환 + 업무노트 무한로딩 근본해결 + 성능 진단
주요 마감: PR #1467~#1472 — 표 색상·집계동기 · 모바일 카드자동전환 · 업무노트 무한로딩(select=* 프리즈) · 성능인덱스 · RLS 정밀진단 · Supabase preconnect
생성: Claude Code (총괄팀장, 대표 세션, 데스크톱 앱)
---

# 세션 인계 노트 — 2026-07-27 (23:37) 성능·로딩 트랙

**main 최신 `ad4f61b`. 6개 PR 전부 라이브 반영·검증 완료.** 무한로딩은 대표 로그인 세션에서 재현·해결 검증까지 완료.

## 이번 세션 완료 (PR #1467~#1472)

### 상담관리(care) 표 뷰 — 카드↔표 완벽 호환 (대표 검수 지적)
- **#1467** 표 인입유형 **행 색상**(카드와 동일 팔레트: 뱅크샐러드=노랑·카카오페이=파랑·보맵=회색, `.ci-sheet tbody tr.ci-src-*`) + **추가·상태변경 시 상단 집계 동기**(`_ciSheetSyncTop` 신규: 상담플로우 밴드·전체카운트·오늘의할일·데이터현황 부분 갱신). 기존엔 표 신규추가가 `_ciSheetRerender`만 호출해 카드 추가(`_ciLoad→_ciRender`)와 비대칭이었음. care+표뷰 전용, salesnote·카드·저장로직 무접촉.
- **#1468** 모바일 A안: **≤860px면 표 대신 카드 자동 표시, 넓히면 표 복귀**. `_ciEffView()`/`_ciIsNarrow()` 유효뷰 도입(사용자 선택 `_ciView` 보존, 폭만 반영) + resize 훅(150ms 디바운스). 좁은화면 카드모드=ci-sheetmode 해제로 "+고객 추가" 폼 복귀. 브레이크포인트 860=4분할 세로스택과 동일. 분기 로직 단위테스트 8/8.

### 🔴 업무노트 무한로딩·랙 — 근본 해결 (핵심 성과)
- **증상**: 업무노트(개인) 진입 시 "불러오는 중"에서 영구 멈춤 + 탭 전체 랙.
- **근본원인(대표 로그인 세션 라이브 실측)**: `loadMyspace`가 scripts를 `select=*` 전량(리치 HTML `script_text`+`attachments`)으로 50건 fetch → 응답 수MB → **`.json()` 파싱이 메인스레드를 45초+ 블록(프리즈)** → 스피너 콜백 못 돌아 무한로딩. 네트워크는 200 성공(HTML·Supabase 서버는 빠름). 즉 네트워크·속도가 아니라 **거대 페이로드 파싱 프리즈**.
- **#1469 수정**: scripts fetch를 `select=id,title,keywords,created_at,sort_order`로 경량화(본문·첨부 제외), **본문은 항목 클릭 시 `mysOpenItem` 단건 lazy 로드**. library는 이미 경량이라 무변경. 캐시 본문 소비처 전수점검=뷰어뿐(필터/정렬/검색/수정은 메타 또는 자체fetch). **라이브 재현 검증**: 목록 50건 즉시 렌더 + 본문 lazy 로드 성공, `_myspaceLoaded:true`, 프리즈 0.

### 성능 인덱스 + 정밀 진단
- **#1470** db-migrate CI로 `idx_scripts_owner_active_created(owner_id,is_active,created_at desc)`·`idx_library_owner_created(owner_id,created_at desc)` 운영 적용·postverify 통과. (대표는 승인만, 총괄이 gh api로 pending_deployment 승인·apply·검증 종결)
- **#1471 RLS 정밀 진단(no-op+postverify EXPLAIN ANALYZE, authenticated 롤+임태성 JWT)**: **서버 실행 scripts 1.667ms·library 0.087ms**, 새 인덱스 Index Scan 확인, RLS는 인덱스 스캔 후 소수행 Filter라 무해. → **2.2초 잔여는 DB가 아니라 브라우저↔Supabase 연결 계층**(콜드 pooler·TLS·PostgREST·RTT). DB로 더 짜낼 것 없음(2ms).
- **#1472** 그 결론에 따라 head에 **Supabase 오리진 preconnect+dns-prefetch** 추가(콜드 핸드셰이크 선시작). 잔여의 일부는 Supabase pooler 백엔드 콜드스타트라 프론트로 전부는 못 잡음.

## 교훈·운영 (박제)
- **총괄 사후대응 한계**: 무한로딩을 대표가 겪고서야 잡음. 게이트(로그인) 화면 성능이 총괄 라이브 검증 사각지대였고, `select=*` 대량로드는 코드리뷰로 안 걸림(실데이터 임계 필요). → 메모리 **`feedback-proactive-perf-not-reactive`** 신설(게이트 화면 라이브 실측·안티패턴 grep·무한로딩=먼저 네트워크 관측). 안티패턴 잔여: `calendar_events limit=500`(select 없음, app.html:15290).
- **진단 방식**: "무한 정지"는 일반 성능 체크리스트(preconnect/파일크기)가 아니라 **처음부터 라이브 재현+네트워크 관측**이 정답. 단 라이브 반복 프리즈 시 범주 증명되면 멈추고 코드 전환(도구와 안 싸움).
- **브랜치 스택 실수**: #1467→#1468→#1469를 쌓아 만들어, #1469 머지 때 #1468이 승인 전 함께 배포됨. **앞으로 브랜치마다 main에서 새로 분기.**
- **DB 실행 원칙 준수**: 대표는 승인만, workflow_dispatch·pending_deployment 승인·apply·검증·보고는 총괄이 gh(대표계정 인증)로 내부 종결. 대표에게 SQL·콘솔·1클릭 요구 0.

## 상태·남은 것
- main 최신·추적 clean. **미추적 tmp PNG 33개**(insurance-factory 스크린샷) 잔존 — 다음 세션 gitignore/정리 권고.
- 상담관리 표 3차 다중선택 일괄삭제(미착수, 이전 세션 이월).
- 2.2초 잔여의 Supabase pooler 콜드스타트는 인프라 영역(우리 코드 밖).
- 관련 메모리: `feedback-proactive-perf-not-reactive`(신설) · `project-sangdam-sheet-view`.
