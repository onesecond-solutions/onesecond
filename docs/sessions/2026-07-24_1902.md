---
세션 종료 시각: 2026-07-24 19:02 KST
주요 마감 작업: PR #1394~#1402 (9건) 병합·라이브 — 지식 레지스트리 트랙 사실상 완성(카테고리 화면·라벨 통일·사이드바 데이터구동·상단 검색기) + 카테고리 검색기 파괴 회귀 버그 수정
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-24 (지식 레지스트리 트랙 완주 + 검색기 버그 수정)

**총괄팀장 Code · 대표 세션 · 데스크톱 앱(Desktop\임태성 → onesecond 루트)**
**PR #1394~#1402 (9건) 전부 병합·라이브 · main 직접 push 0 · 병합 전 head 검증 9회 이행**

## 이번 세션 한 줄
어제 깔아둔 지식 문서 원장(#1393)을 실제로 읽는 소비처들을 완성했다 — 카테고리 화면·라벨 통일·페이지 사이드바 데이터구동·카테고리 상단 전체검색기. 마지막에 "의료실비만 검색기가 홈 모양으로 뜨는" 회귀를 재현·확정해 파괴 버그를 수정했다.

## ✅ 완료·라이브 (증거)

### 1) 카테고리 화면 — PR #1394 (main `8fbd5fe`)
홈 보장분석 축 카드 6개(#v-axis-*)의 "준비 중입니다."를 원장(`knowledgeVisibleDocs`)을 읽어 문서 목록 렌더로 교체. 신규 `js/knowledge-category.js`(`_kcShow`)+`css/knowledge-category.css`+app.html 3곳. 탭6+칩(그룹0→감춤)+갈고리+문서목록. 문서 표기=원장 `label`(정본). showView 게이트 체인 word-diff 무접촉 증명.

### 2) 사이드바 로고 → 홈 이동 — PR #1395 (main `8431ad1`)
사이드바 로고·"원세컨드" 글자 클릭=홈(`_navView('home')`). 접기/펴기는 햄버거가 담당(기능손실0). 앱 셸이라 전 사용자 영향 — 햄버거 왕복·접힘 노출까지 실측.

### 3) 카테고리 중앙 정렬 A안 — PR #1396 (main `784d3fd`)
콘텐츠 블록 `max-width:1010px;margin:auto` 중앙(읽는 본문 710 불변, 우측 260 레일 자리). 스펙 v2 §8-4 정렬 규칙 보강 + 기획팀장 정렬 지시서 `docs/specs/knowledge_category_alignment_fix_v1.md` 저장(로고추가·홈버튼제거 불채택 사유 박제).

### 4) 라벨 통일 PR-B — PR #1397 (main `ebdc8af`)
'의료실비 변천사' 통일: 홈타일 '실비 변천사'·홈데스크·검색 '실비 세대별 변천사' → 원장 정본. 뷰키 `silson`·keywords 불변.

### 5) 사이드바 데이터구동 PR-D — PR #1398 (main `68a0a81`)
지식 페이지 3종 '함께 보면 좋은 자료'를 하드코딩 상호링크 → 원장 자동 생성(라벨·부제 원장값=A안). 각 페이지 head에 원장 script + 폴백. caregiver V3 무변화(710/16/행간1.85) 실측.

### 6) 카테고리 상단 홈 전체검색기 재사용 — PR #1399 (main `350bb1b`)
홈 `#homeSearch`를 복제 않고 카테고리 진입 시 슬롯 이동, 이탈 시 홈 복귀(`_kcHomeSearchMove`/`_kcHomeSearchRestore`). id 하나라 검색함수·자동완성·최근검색·권한필터·전체검색(`_runSearchV2`) 100% 재사용.

### 7) 홈 버튼 '홈으로'→'홈' + 검색기 좌측 — PR #1400·#1401 (main `8b77af4`)
텍스트 '홈', 검색박스 바로 왼쪽(`left:calc(50% - 370px)` gap 12px). 모바일 검색박스 위 정적.

### 8) 🔴 카테고리 검색기 파괴 회귀 수정 — PR #1402 (main `5bfe93f`)
**증상**: 카테고리에서 의료실비만 검색기가 홈 모양('무엇을 찾으세요?')으로 뜨고 홈버튼 앱바로 튐(암 등 정상).
**근원**: `_kcShow`의 `root.innerHTML=...` 재생성이 슬롯에 이동돼 있던 `#homeSearch`를 detach 파괴. 부팅/재렌더로 showView가 한 축에 2회+ 불리면(딥링크=URL 직접 진입) 파괴/홈잔존. 딥링크로 연 의료실비에서 관측.
**수정**: ①렌더 직전 `#homeSearch`가 root 안이면 홈 대피(파괴방지) ②렌더 끝 `_kcShow`가 스스로 `_kcHomeSearchMove`(showView 횟수·순서 무관 완결·멱등).
**실측**: 같은축 2·3회/축전환/홈왕복/appstate:ready 이중발화 전부 슬롯유지·파괴0.

## 🔑 다음 세션이 알아야 할 핵심

### ⚠️⚠️ 총괄 반복 실패 교훈 (박제)
이동함수(`_kcHomeSearchMove`)를 **직접 호출** 테스트로 "됐다" 보고하면 안 된다. #1399~#1401 검수에서 이걸 안 해 검색기 파괴 버그를 4번 놓쳤고 대표가 화냄. **반드시 실제 `showView` 전체 경로 + 부팅 이벤트(appstate:ready)까지 재현**해야 파괴/타이밍 버그가 잡힌다. 게이트 화면은 `window._canSeeCoverage=()=>true` override(계정주입·검색실행 없음)로 showView 경로 로컬 재현 가능 — 이번에 이 방법으로 원인 확정.

### 미결/대기
- **홈 버튼 위치 미세조정 보류** — calc(50%-370) 방식이 파괴 버그에 가려 제대로 안 보였을 수 있음. flex로 바꾸는 시도는 `git stash stash@{0}`(브랜치 fix/category-home-button-flex WIP)에 보류. 대표 정상화면 확인 후 필요시. (flex 전환 시 CSS 캐시버스팅으로 app.html css 버전 b→c 올려야 했던 이력 있음)
- **대표 실사용 확인 대기**: 의료실비 URL 직접 열기 / 홈 탭 클릭 둘 다 검색기 정상 뜨는지.
- **대표 직접 결정 대기**: '기타' 카드 내용(총괄 임의 편입 금지·자료0 유지) · 홈 화면 정리(무접촉) · 팀명 rename/merge(홀딩).
- **검색 keywords 원장 편입** — 별도 후속 트랙(간병 25개 `_SHORTCUTS` 잔류, 유실된 `caregiver_search_index_v1.json` 복원).
- **PR-C(showView 라우팅 원장화)** — ROI 낮아 보류(최고위험 지점). 4번째 지식문서 생길 때 폴백유지 최소안.

### 팀명 수정 조사 결과 (홀딩, 참고)
팀 공유 자료는 팀 이름이 아니라 team_id(uuid)로 묶임(scripts/myspace_files scope_id + RLS os_user_team_id). **rename은 자료 영향 0(안전)**, merge(팀 통합)는 자료 scope_id 이관 필요(C등급). users.team 텍스트 잔재는 표시용(권한 무관). 어드민 팀명 수정은 acTeamToggle과 같은 경로에 필드 추가로 DDL 0 가능.

## 상태
- main `5bfe93f`(#1402). 오늘 PR #1394~#1402 (9건) 병합·라이브. GitHub Pages 전부 success.
- stash 3개(flex WIP·이월 2개). `C:/tmp/` 방치 worktree 이월.
