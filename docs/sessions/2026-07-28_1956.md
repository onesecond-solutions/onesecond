---
세션 종료 시각: 2026-07-28 19:56 KST
주요 마감 작업: PR #1474~#1482 (전부 main 머지·GitHub Pages 라이브·검증 완료)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-28

**main 최신 `8f8032c`. 9개 PR 전부 라이브 반영·검증 완료.** 상담관리(care) 화면 집중 세션.

## 이번 세션 완료 (PR #1474~#1482)

### 상담관리 표(시트) 뷰 마감
- **#1474** 생년월일/성별 칸에 보험나이 표시(`1968-01-29 / 남 / 58세`, 오늘 기준 `_ciInsAge` 재사용, 공통 헬퍼 `_ciBgDisplay`) + 기존 행 **입력일자 인라인 편집**(`_ciCellCommit`에 `idate` 분기, created_at PATCH).
- **#1475** 다중선택 **일괄삭제** — 체크박스 열(헤더=현재 페이지 전체선택, `_ciSheetSel`) + "선택 삭제 (N)" + 소프트삭제(`id=in.(...)` PATCH deleted_at). 현재 페이지 한정(대표 확정). css `.ci-sh-chk` 열 추가로 nth-child 시프트.
- **#1476→#1477** 상담상태에 **종결** 추가(`_SN_STAGES` 끝). 밴드 표시는 처음 청약완료 아래 검정 박스(#1476)로 했다가, **총괄이 축소 그림으로 컨펌받아 실제 큰 카드와 어긋난 실수** → 대표 확정 안 B로 교체(#1477: 헤더 '전체' 옆 무채색 '종결 N' 칩, 밴드는 8단계 원복). **총괄이 직접 실 CSS 렌더를 로컬 하니스+in-app 브라우저로 측정해 교정**(축소 그림 재사용 금지 교훈 재확인).
- **#1478** 표뷰 카드덮임 버그 수정 — 검색(`_ciSetNameQ`)·필터(`_ciListRefresh`)·카드선택(`_ciSelCard`)이 표뷰 상태에서도 무조건 `_ciListHtml`(카드)를 주입해 "전체폭인데 카드처럼" 깨지던 것을, `_ciRender`와 동일 규칙의 공통 헬퍼 `_ciListInject`로 통일. `_ciSheetRows`는 이미 `_ciViewData` 기반이라 표뷰 검색/필터 정상 동작 확인.
- **#1482** 표뷰 **열 너비 드래그 조절 + 저장** — colgroup<col data-colkey> 방식, 헤더 경계 드래그, localStorage(`ci_sheet_colw`) 저장·재렌더 유지, 핸들 더블클릭 auto-fit(canvas measureText), 툴바 "열 너비 초기화", 최소폭 50px. 체크박스/삭제열 제외. (작업자가 렌더 실측 중 핸들 hit-test 버그 직접 수정.)

### 상담관리 상단 대시보드 재설계
- **설계 연구(os-planner)**: 상담 플로우·데이터 현황·오늘의 할 일 3섹션이 **같은 원장(`_ciData`)을 읽지만** 숫자가 안 맞아 보이는 근원 = ①시간축 3개(오늘/재고 stock/기간 flow)를 라벨 없이 세로로 쌓음 ②'예약'이 날짜무관 '예약 단계 stock'이라 캘린더의 실제 예약과 안 맞음 ③calendar_events↔sales_customers **FK 없음**(예약↔고객 무링크).
- **#1479** 섹션 순서 = **데이터 현황을 맨 위로**(유입 flow → 오늘 액션 → 파이프라인 stock) + 데이터 현황 기본 펼침(`_ciStatsIsCollapsed` false) + hero "신규 고객"→**"신규 등록"**(status 레거시 '신규'와 어휘 충돌 제거). ⚠️`#ci-wrap`은 salesnote 공유라 고객관리에도 동일 적용.
- **#1480 P1** — 오늘의 할 일에 **케어 체크리스트**(놓친 케어 빨강 + 오늘 처리). 대상 3종 `appl91`/`appl181`/`bday`(예약 제외). 저장=`profile.care_done` jsonb(**무DDL**), 완료=서버 최신 profile 재조회 후 병합 PATCH(기존 키 보존, `_cfSave` 패턴 복제). baseline `CI_CARE_TRACK_SINCE='2026-07-28'`(도입 이전 due 제외=가짜 놓침 방지). 소급 완료·취소 지원. **보험생일은 놓침에 안 뜨고 그날만 표시**(매년 반복이라 next-occurrence 산식, 대표 A안=이대로 머지·별도 보완은 후속).
- **#1481 P2** — 오늘의 할 일에 **주간 회고 미니달력**(7일 스트립, 완료 초록/놓침 빨강/예정 회색/예약 파랑 점, `‹›` 지난주 회고, 날짜 클릭 선택일 상세). **예약=calendar_events 읽기 오버레이**(app.html:8904 쿼리 재사용, author_id 격리, **무DDL**). 완료체크는 `_ciCareToggle` 재사용, 전체 달력은 `_goMysCalendar()` 딥링크(중복 UI 0). P1 놓친 케어(전 기간)는 유지. 총괄 직접 렌더 실측(7칸 등폭·오버플로 0·점색 토큰 일치).

## 다음 세션 작업 (메모리 `project-sangdam-next-session-tasks` 저장 — 대표 지시로 이번엔 저장만)
- **(A)** 상담내용·비고 **카드↔표 양방향 저장** + 카드뷰에 비고란 신설. ⚠️착수 전 저장 구조 실측 조사(표뷰 상담내용=`_ciSaveMemo` 최근 sales_consultations 1건 / 카드=날짜별 이력 스레드 / 표뷰 비고=`profile.비고`) → 설계안 확인 → 구현 순서.
- **(B)** 표뷰 상담상태 **예약 선택 시 새 일정 팝업**(기존 `_ciResvOpen`/`_ciResvSave` 재사용, calendar_events source_type='personal').

## 로드맵·남은 것
- **P3(선택·C등급)**: `care_tasks` 테이블(customer_id FK·due_date·done_at·owner RLS)로 승격 → 예약↔고객 완전 연결·서버 회고쿼리. P2 실사용 검증 후 판단.
- 보험생일 놓침 보완(선택) · 예약 날짜 기반 전환(status stock→calendar_events 날짜) · '기타' 보장축 카드 성격 · 뇌심장/수술비 공개문서 · 실비 silson 5행 approved 승격.
- 표뷰 무관 기존 이슈: 좁은 뷰포트에서 표 헤더 높이 커지는 현상(작업자 발견, 별건 미착수).

## 교훈(박제)
- **축소 스케치로 UI 컨펌 금지** — 실제 컴포넌트 크기와 어긋난다(#1476 종결 박스 사건). 총괄이 로컬 하니스에 실 CSS(app-views.css/tokens) 얹어 in-app 브라우저 getBoundingClientRect/computed style로 실측 후 반영. `feedback-daepyo-screen-quality` 정합.
- **profile jsonb 저장은 반드시 서버 최신 재조회→병합→PATCH**(통째 덮으면 appl_date·care_cal_added 등 유실). P1/P2 완료토글에 강제.
- 표뷰 생기기 전 카드 전용 재렌더 경로들이 표뷰를 안 봐서 회귀(#1478) — 새 뷰 도입 시 모든 재렌더 진입점을 뷰 인식으로 통일할 것.

## 상태
- main 최신 `8f8032c`·워킹트리 clean. 관련 메모리: `project-sangdam-next-session-tasks`(신규)·`project-sangdam-sheet-view`·`project-onesecond-pending`.
