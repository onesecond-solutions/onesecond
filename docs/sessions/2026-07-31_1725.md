---
세션 종료 시각: 2026-07-31 17:25 KST
주요 마감 작업: 고객관리 상담관리化 ①표뷰(PR #1503)·②캘린더(PR #1504) 완주·라이브
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-31 (고객관리 상담관리化 ①표뷰·②캘린더 완주)

**main 최신 `8ddade2`. 이번 세션 = 어제(2026-07-30) 미완으로 보존한 "고객관리(salesnote) 상담관리化" 로드맵의 ①표뷰(PR #1503)·②캘린더(PR #1504)를 연속 완주·라이브.** 총괄=오케스트레이터(WIP 회수·실측검수·머지·라이브확인 종결), 구현=os-builder/조사=os-chore 위임.

## 라이브 완료 (전부 main 머지·GitHub Pages 배포·라이브 grep/curl 검증)
- **#1503 `94b4770` [PR-S1] 고객관리 표(시트) 뷰 도입** — 어제 산출물 보존한 WIP(`c690950`) 회수·검수·완성. 상담관리 `_ciSheet*` 엔진 재사용, care 하드게이트(`_ciMode==='care'`)→`_ciSheetEnabled()`(care∪salesnote) 치환 + salesnote 전용 데이터·컬럼은 `*_SALESNOTE` 분기함수 신설(행소스=`_ciApplData` 청약완료 전량, care 본문 문자 그대로 보존=회귀0). **컬럼 10열(대표 확정=상담내용·계약일 둘 다)**: 입력일자·인입유형·이름·생년월일/성별·전화·상담상태(청약완료 고정·편집잠금)·**상담내용(읽기전용, PR2 상담이력 입력차단 정합)**·계약일(profile.appl_date)·비고(가변)·첨부. ⚠️**실측 교훈**: 초기 WIP는 비고 가변열이 50px로 짜부(고정열 합이 가용폭 초과)→총괄이 로컬 실렌더 offsetWidth 실측으로 발견→기본 열폭 하향 재배분으로 해소(그림 아닌 실측). care 회귀 0 실측 확인(상담내용 편집 가능 유지·기본폭 그대로).
- **#1504 `8ddade2` [PR-S2] 고객관리 캘린더 탭 개방** — 상담관리 개인전용 캘린더(`cic` 엔진) 재사용. **핵심=캘린더는 calendar_events를 author_id+audience_target=personal로 조회→mode 무관, 상담관리·고객관리가 같은 개인 일정(상담 예약·케어 일정 포함)을 봄.** 데이터 분기 불필요, **하드게이트 2곳만 제거**하는 최소 변경(care-flow.js `.ci-flow-head{display:none}` 규칙 + app.html `_ciFlowSetTab`의 `t==='cal'&&salesnote→flow` 강제). `_ciFlowSetTab`은 mode 무관 #ci-band/#ci-cal-embed 토글, `_cfSalesnoteTop`은 #ci-band innerHTML만 채워 캘린더 탭 시 충돌 0(실측 확인: _ciRender 재호출에도 캘린더 유지). care 캘린더 회귀 0.

## 작업 프로세스 (표뷰/캘린더 공통, 성공 패턴)
WIP/조사 → **총괄 실렌더 실측 검수(로컬 http server + in-app 브라우저, offsetWidth/클릭/computed 실측)** → os-builder 위임 구현 → 총괄 재검수 → PR → 대표 승인(승인권자) → AI팀이 머지·배포·라이브 검증 종결. 병합 전 PR head 재확인 2회 이행. salesnote는 owner RLS 격리 게이트 화면이라 일반 사용자 영향 0.

## 대표 결정
- ①표뷰 컬럼 = **상담내용·계약일 둘 다(10열)** 확정. 상담내용은 **읽기전용**(고객관리=청약완료 격리, 상담이력 신규입력은 상담관리에서).
- ②캘린더 탭 라벨이 상담관리와 공유돼 고객관리에서도 "상담 플로우 | 캘린더"로 표시됨(대표에 알림·미조정). 원하면 "케어 플로우"로 변경 가능(별건).

## 미결/백로그 (다음 세션)
- **⭐다음 최우선 = ③인입유형 드롭다운+색.** 앞 두 트랙과 성격 다름 — **새 원장 필요**(화면 전 원장 원칙): 자유입력 → 드롭다운 + "+추가" + **유형별 색**. **선행 결정 = 개인 vs 팀 공유 범위(대표 미정)** → os-planner에 원장(유형 목록·색 저장 위치) 설계부터 위임. 인입유형=`profile.attr`(폴백 channel), 표뷰 행색은 이미 `ci-src-*` 하드코딩 규칙 존재.
- ④청약완료 전용 필드(맨 뒤): 보험사·상품명·월보험료·증권번호·납입주기·계약상태 中 대표 확정 후.
- 표뷰/캘린더 **대표 라이브 실사용 픽셀 확인**(게이트 로그인 필요·총괄 대행 불가).
- 캘린더 탭 라벨 "케어 플로우" 조정 여부(대표 판단).
- `consult_customer_form_v1.md` stale 갱신(카톡 이관분 2,583명 삭제 반영, 어제 이월).
- 카카오 대량발송 트랙(중계사·비용·템플릿 설계 대기).

관련 메모리: project-gogaek-care-track(신설), project-sangdam-sheet-view, project-onesecond-pending, feedback-pr-report-format, feedback-verify-pr-head-and-standard-floor, feedback-chonggwal-delegates-not-hands-on.
