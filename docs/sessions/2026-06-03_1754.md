---
세션 종료 시각: 2026-06-03 17:54 KST
주요 마감 작업: 보험Q&A 4화면 일관화 + 유기체 건강검진(메뉴×롤 + RLS 32테이블 실측) + 매니저룸 실장 권한 처방 + 홈 배너 인터랙션
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-03 (직전 12:45 종료 후 이어서, Code 단독). 주제: **보험Q&A 4화면 일관화** + **유기체 건강검진(오픈 신뢰성 점검)** + **매니저룸 실장 권한 처방** + **홈 배너 위트 인터랙션**. PR #356~#364(머지 9건·전부 라이브) + DB RLS 2정책 + 건강검진 결과지 문서.

## 완료 (라이브 반영)

### A. 보험Q&A 4화면 일관화 (#356~#358)
- **카드 카운트 부풀림 정정**(#356): MY SPACE·매니저룸 오늘의 할 일 "보험Q&A 내 질문·답변" 카드가 posts qna 전체(소식지 462 포함=465)를 세던 것 → `title not ilike 소식지`로 목록과 동일 조건.
- **카드 클릭 라우팅**(#357): 카드 go가 `_goVoiceTab('voice')`(현장의 소리)로 화면 전환 → 카드 숫자(MY SPACE)와 도착지(0건) 불일치. `_goMysQna()` 신설로 MY SPACE 보험Q&A 탭(같은 데이터)으로.
- **현장의 소리 연결 누락 정정 + 기본 모드 통일**(#358): 현장의 소리 보험Q&A 탭은 로드 함수 자체가 없어(단체방/스마트/지점만 연결, voice는 빈 배열) 항상 "없어요" → `loadVoiceQna` 신설 + showTab 연결. 자료실·매니저룸·MY SPACE 기본 모드 today→all + 칩 on 이동(어제 글도 보이게).
- **4화면 동일 소스**: MY SPACE·자료실·매니저룸·현장의 소리 모두 posts qna를 전체 기준으로 표시.

### B. 유기체 건강검진 (팀장님 "건강하게 오픈하고 싶다")
**결과지: `docs/architecture/health_check_2026-06-03.md`** (메뉴×롤 + RLS 전수, 실측 기반)
- **A축(화면 게이트)**: 메뉴 11개 × 9 role 매트릭스 정합. 약점 = `showView` 진입 가드가 자료실·어드민 2개뿐 → ga_member가 URL `?view=team`으로 화면은 열 수 있으나 **데이터는 RLS가 방어 → 유출 없음**.
- **B축(RLS 면역)**: **32개 테이블 전부 RLS ON + 정책 0건 없음**(실측). 1차 코드·문서 가설("library·calendar·scripts RLS 0건")은 **틀렸음** — db_schema.md가 실제와 달랐던 것(CLAUDE.md "실측 후 단정" 원칙 적중). posts 21정책 board_type×role 정교 / 원수사 insurer_id 격리 / SECURITY DEFINER 함수(is_admin·get_my_role·my_team_id·my_branch_id·is_insurer_employee 전부 true).
- **유일 실질 격차**: `users` SELECT에 admin·원수사 매니저는 있으나 **GA 실장(ga_manager)·지점장(ga_branch_manager) 팀원 조회 정책 부재** → 임태성 실장 로그인 시 매니저룸 실장 페이지 빈 화면(데이터 유출 아니라 기능 결손).

### C. 매니저룸 실장 권한 처방 (#359 + DB)
- **처방 A(#359, 코드)**: 매니저룸 팀원관리 진입 시 role별 자동 라우팅 — 실장→실장 페이지 + 지점장 탭 숨김 / admin·지점장→지점장 페이지. (기존: mrMemSub 미호출로 초기 지점장 카드 노출 + 실장 페이지 미로드)
- **처방 B(DB, 적용 완료)**: `users`에 RLS 2정책 추가 (SECURITY DEFINER 함수로 자기참조 재귀 회피)
  ```sql
  CREATE POLICY users_select_team_manager ON public.users FOR SELECT TO authenticated
    USING (get_my_role() = 'ga_manager' AND team_id = my_team_id());
  CREATE POLICY users_select_branch_manager ON public.users FOR SELECT TO authenticated
    USING (get_my_role() = 'ga_branch_manager' AND branch_id = my_branch_id());
  ```
  - 추가만(기존 제거 0). 실장=같은 팀 전원 / 지점장=같은 지점 전원. 다른 팀·지점은 여전히 불가. team_id/branch_id NULL이면 매칭 안 됨.

### D. 홈 배너 위트 인터랙션 (#360~#364)
- 배너 3장→4장(#360 머지에 org03 추가) → 인라인 슬라이드 폐기, **우측 끝 번호 1·2·3·4**(#361): 1=현재 갤럭시 배너 / 2·3·4=org 이미지 **오버레이 큰 화면**(✕·좌우·바깥 닫기).
- **[✕ 닫기] → 배너 접힘 → 미니 캐릭터 + 펼침 버튼**(#361): 처음 커스텀 스틱맨 SVG → **OpenMoji 요술램프🪔**(#362·#363, CC BY-SA) + 멘트 "요술램프를 문지르면 배너가 짠!" → 버튼 "S.O.S → 더듬더듬"(#364). 흔들림 애니. 딸깍·just funny 톤.
- 자동 롤링 없음(전부 수동 클릭).
- **org01·02·03** = 원세컨드 유기체 조직도/홈 미리보기 그림(ChatGPT·Gemini 작품, Code가 프롬프트 작성). `assets/images/home_banner/`.

## 큰 그림
- **오픈 신뢰성 = 추측 아닌 실측**: 팀장님 "얽히고 섞여 엉망될까 불안" → 메뉴×롤·RLS 전수 검진 결과 "건강한 유기체"로 확인. 면역(RLS) 탄탄, 화면 약점도 데이터 면역이 커버. 유일 격차(실장 팀원 조회)도 처방 완료.
- **매니저룸 조직관리 트랙**과 직결(org_management_v1): 실장 권한 처방으로 A단계 실데이터 표시 기반 마련.

## 미완 / 다음 (1순위부터)
1. **처방 A·B 라이브 검증** — 임태성 실장(ga_manager) 재로그인 → ① 매니저룸이 실장 페이지 자동 ② 같은 팀 팀원 채워짐 ③ 다른 팀 격리. (팀장님 검증 예정, GitHub Pages 빌드 후)
2. **조직 B단계**(초대: 실장·팀장·지점장, team_invitations 연결) · **C단계**(가입→팀 매핑 signup?invite=token).
3. **MY SPACE·매니저룸 페이지 내 미니 검색기** (검색 제외 영역 방침).
4. **검색 미세조정**(관련도·범위) / AI 검색 속도 / scripts SELECT=true 검토(공용 vs 개인) / insurer_* 매니저룸(Phase 2 원수사 입점).
5. 홈 배너 캐릭터·멘트 추가 조정 여지(팀장님 라이브 보고 판단).

## 격차 / 학습
- **문서 단정 금지 재확인**: 1차 조사가 db_schema.md 기반으로 "RLS 0건" 오판 → 실측에서 전부 반박. CLAUDE.md DB 원칙이 정확히 들어맞음. RLS·DB 상태는 신버전 실측 후에만 단정.
- **users 자기참조 RLS** = SECURITY DEFINER 함수(my_team_id 등)로 회피 = 표준(rls_self_reference_avoidance 메모리 정합).
- **"띄울까요?" 회피성 질문 지양**: 보험Q&A 연결 누락 = 명백 버그인데 "띄울까요?"로 물어 팀장님 지적 받음 → 자명한 일관성은 묻지 말고 처방.
- 매 PR 인라인 스크립트 문법(new Function) 통과 후 머지 — 회귀 0.
- 외부 SVG 자원(OpenMoji)을 curl로 받아 assets 적재 + img 연결 = 기기 일관 이모지 패턴.
