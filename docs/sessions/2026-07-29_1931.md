---
세션 종료 시각: 2026-07-29 19:31 KST
주요 마감 작업: 상담관리/고객관리 UI 대규모 개편 — PR #1486~#1497 (12건) 라이브
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-29 (상담관리/고객관리 UI 대규모 개편)

**main 최신 `fdeb6e8`. 이번 세션 = 상담관리(care)/고객관리(salesnote) 화면을 대표 실사용 피드백으로 연속 개편 — PR #1486~#1497 (12건) 전부 라이브.** 총괄=오케스트레이터(위임·검수·머지·라이브 확인 종결), 구현=os-builder(커스텀 3티어 로드됨) + 총괄 직접(극소 UI 3건, 대표 "직접 빠르게" 지시).

## 이번 세션 완료 (PR #1486~#1497, 전부 main 머지·GitHub Pages 배포·라이브 grep 검증)

### 파일 첨부 트랙
- **#1486 `fc47027`** 표뷰 새 행 캡처 OCR(Ctrl+V) — 카드폼 `_ciOcrRun`/`_ciOcrBind` 복제(`_ciNewRowOcr`/`_ciNewRowOcrBind`), `#ci-list` paste 1회 바인딩, 응답 5필드(이름·전화·생년월일·성별·인입유형)를 `.ci-sh-new` data-nf 칸에 자동채움(빈값 미터치·자동저장X). gemini-customer-ocr 재사용, 원본 미저장.
- **#1487 `7ebcb79`** 상담관리 카드폼·표뷰 파일 첨부 — 복수선택·수정·삭제·미리보기·다운로드, **수정 시 기존 첨부 표시**. **원수사 자료실 iv 패턴 이식**(`ivEditPost`→`renderIvEditAtts`→`ivRemoveAtt`→저장 `.concat` 병합). 편집 진입 시 `_ciFormProfile.attachments`→`_ciEditAtts` 렌더(`renderCiEditAtts`/`ciRemoveAtt`), 저장 `prof.attachments=(_ciEditAtts||[]).concat(신규)` 병합(통째 교체 금지). **비공개 `myspace` 버킷**(고객 개인정보 격리)·`{name,bucket,path}`·클릭 서명URL(`_openSignedAtt`/`_dlSignedFile`). ⚠️myspace_files row 미생성(개인 자료실 오염 방지). 표뷰=📎열+팝오버(추가·삭제·다운로드, `_ciPatchProfile` 공유). DDL 0.
- **#1488 `c75d400`** 업무노트(scripts) 첨부 근본 수정 — 대표 지적 "수정 시 기존 첨부 안 보임". 원인=`scrEditInline` SELECT에 attachments 미조회+편집폼 미렌더+`saveScrInline` 통째 교체(유실). iv 패턴 이식(`_scrEditAtts`·`renderScrEditAtts`·`scrRemoveAtt`·저장 `.concat`). 게이트 밖 일반 사용자 화면이나 유실 버그 해결 방향이라 회귀 낮음. 신규작성 경로 무접촉.

### 표뷰/레이아웃 트랙
- **#1489 `73f9b0d`** 표뷰 가로 스크롤 제거 — 첨부열 추가로 폭 초과. `.ci-sheet-wrap` overflow-x hidden, `min-width:1220px` 제거. `_ciSheetNormalizeColW`가 colgroup 폭 합=컨테이너 폭으로 정규화(memo 가변 흡수), 드래그=이웃열 주고받기(`_ciSheetResizePairCalc`, 총합 불변). 구조적으로 스크롤 불가.
- **#1492 `90ce9c7`** 표뷰 CSV 다운로드 버튼을 우측 열너비초기화 옆으로(`.ci-vtools-right` 그룹핑). (총괄 직접)

### 상담 플로우/캘린더 탭 트랙
- **#1490 `e6ed30a`** 상담 플로우 섹션에 [상담 플로우 | 캘린더] 탭. 캘린더=**개인 전용**(prefix `cic`, iv-cal-area 패턴). 자체구현 캘린더 엔진(`loadCal`/`renderCalGridMonth`, `_calSt[p]` prefix별 독립)에 cic 추가, **mys/iv 회귀 0(OR 조건만 추가)**. 월·주·일·목록 토글(cic 전용 렌더 `_cicRenderView` 등). "전체 달력 →" onclick만 `_ciFlowSetTab('cal')`로 교체(`_goMysCalendar` 원본 보존). 예약(`_ciResvSave`)=personal 저장이라 자동 노출.
- **#1491 `477f91f`** 캘린더 탭 좌측 월별 리스트 제거 — iv-cal-area 복제로 딸려온 `.list-col`(#cic-cal-list) 삭제, 그리드만 전체폭. +내일정 상단 이동. (mys/iv split 무접촉)

### 오늘의 할 일 트랙
- **#1493 `fb9cf7c`** 오늘의 할 일 슬림 개편 — 헤더 날짜 화살표(`_ciTodayNavDay`±1일, 카운트 그날 기준 재계산 `_ciTodayMatch` 파라미터화)·**주간 미니달력·이 날 케어·오늘 처리 체크리스트 삭제**(호출 0, 정의 잔존)·전체달력 유지. ⚠️'예약' 카운트만 날짜 무관(예약에 날짜 필드 없어 보수적). salesnote는 `_cfSalesnoteTop`이 #ci-today 덮어 무영향.
- **#1494 `d679720`** 오늘의 할 일 헤더 시안대로 — 화살표 양끝·가운데 제목/날짜 2줄 중앙(`.ci-today-titlewrap`). (총괄 직접)

### 고객 리스트 기간 소트 트랙
- **#1495 `8019a59`** 고객 리스트에 기간 소트 칩(오늘·최근7일·이번달·지난달·사용자정의·전체) 추가 — 검색기(240px)+칩 한 줄, 검색~리스트를 `.ci-list-panel` 한 컨테이너로. **데이터현황과 상태 공유 연동**(`_ciListPeriodPick`→`_ciStatsSetPreset`, 활성판정 `_dsListPresetOn`이 공유 `_ciStatsMode` 읽음, 새 독립상태 0). 양방향(`_ciListPeriodChipsRender`를 `_ciRender`/`_ciListRefresh`에). `_ci*` 공유라 고객관리에도 적용.
- **#1496 `e567c59`** 기간필터 순서 전체↔사용자정의 교체 + 사용자정의 날짜피커를 칩 우측 인라인(`ds-presets` 안으로). (총괄 직접)
- **#1497 `fdeb6e8`** 고객 리스트 '사용자정의' 우측 인라인 날짜피커 — id 분리(`cil-cf`/`cil-ct`≠`ds-cf`/`ds-ct`), 적용=`_ciStatsApplyCustom('cil-cf','cil-ct')`(파라미터화, 무인자=ds-* 기존 불변), 공유 `_ciStatsCRange`. (총괄 직접)

## 교훈(박제)
- **대표 시안(그림)과 구현 배치 어긋남 반복**(#1494 헤더 좌우 갈라짐, #1491 좌측리스트 딸려옴) → 위임 시 "참조 패턴 복제"만 시키면 그 패턴의 곁가지(iv split 좌측리스트)까지 옴. **배치를 시안 그대로 명시**하고, 참조원본의 불필요 요소 제외를 못박을 것. show_widget 목업으로 사전 합의가 유효.
- **총괄 직접 수정**(#1494·#1496·#1497) = 대표 "총괄이 직접 빠르게" 지시 시 극소 UI는 위임 대신 직접(governance 예외 준용). 단 feature 브랜치→PR→검수→머지 절차는 유지.
- **파일첨부 정본 모델 = 원수사 자료실(iv)** — 편집 진입 시 첨부 조회→상태배열→렌더(개별삭제)→저장 `.concat` 병합. 업무노트=반면교사(미조회+통째교체=유실). 앞으로 첨부 편집은 iv 패턴.
- **연동=상태 공유**(새 독립상태 금지) — 리스트 기간소트가 데이터현황 `_ciStatsMode`/`_ciStatsCRange`/`_ciStatsSetPreset` 그대로 씀. 공유 함수 파라미터화(무인자=기존 불변)로 id 분리 피커도 같은 경로.
- **캘린더 공용엔진 확장은 OR 조건 추가만**(mys/iv 경로 결과 불변) — prefix별 독립상태라 새 prefix 추가가 안전.

## 상태·미결
- main `fdeb6e8`. 상담관리 폼 개방 범위 = **임태성(98c5f4f9)·admin·bylts@kakao.com 3계정만**(`_canSeeCare`), 비로그인/일반 미개방.
- **게이트 안 화면이라 이번 12건 전부 실동작 육안은 대표 세션 몫**(총괄 로그인 대행 불가, 코드·라이브 grep까지 검증).
- **미결/백로그**: ①'예약' 카운트 날짜 세분화(예약일 필드 신설 시) ②케어콜 타이밍 도래 시 상단 카운트(+91/+181/생일) 클릭→대상 리스트(데이터현황 원칙, 청약완료 고객 쌓이면) ③v3 타이포 `.ci-*` 적용=**대표 홀딩**(더 써보고 판단) ④미추적 tmp-insurance-factory PNG 33개 정리.
- 관련 메모리: `project-sangdam-next-session-tasks`(A·B 완료), `project-onesecond-pending`, `feedback-verify-pr-head-and-standard-floor`, `feedback-chonggwal-delegates-not-hands-on`.
