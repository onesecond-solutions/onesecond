---
세션 종료 시각: 2026-06-25 19:00 KST
주요 마감 작업: 팀/지점 공유 스크립트 첨부파일 렌더 복원(PR #962) + 바텀시트 바닥=검색 전용 정리 트랙 저장(미착수)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-25 저녁 (공유 스크립트 첨부 복원 + 바닥 정리 트랙 저장)

Code 단독(=총괄팀장). main 직접 push 0(전부 PR). DB 변경 0. 신버전 pdnwgzneooyygfejrvbg. 오후(13:46) 세션이 전산 오류로 셧다운된 뒤 이어진 저녁 세션 — 직전 작업은 전부 머지·라이브로 정상 보존 확인.

## 오늘 마감 (증거)
- **팀/지점 공유 스크립트 첨부파일 렌더 복원** (PR #962 머지, main `6c406d6`): 공유 스페이스에서 스크립트 첨부(이미지·파일)가 안 보이던 버그. **데이터는 정상**(공유 사본 INSERT가 `attachments` 그대로 복사, app.html:13659) — #957 마스터-디테일 통일 때 공유 뷰어에서 **렌더 조각만 빠진** 것이 원인. 수정 2곳: ①`_snScopeScripts` 목록 SELECT에 `attachments` 추가 ②`_snScrPick`에서 마이 스페이스와 동일 헬퍼 `_voiceAttsHtml`로 첨부 렌더(이미지 썸네일·파일·딸깍 정합·`.vbody` 끝에 attHtml). DB 변경 0, 기존·신규 공유분 모두 화면만 복원. 7단계 흐름(브랜치→PR→Code 1차 검수→대표 결재 A안→머지→라이브) 정합. **라이브 검수 잔여**(임태성 계정: 공유 스크립트 첨부 노출 + 마이 스페이스 회귀 0).

## 저장만(미착수) — 다음 진입용 트랙
- **바텀시트 바닥=검색 전용 정리** (메모리 `project_bottomsheet_floor_search_only_pending` 신설): 바텀시트 뒤 바닥에 스크립트·옛 빠른 메뉴·변환기·**현장의 소리** 등 옛 페이지(`.wrap.view`)가 비쳐 보이는 문제. **근본 원인=바닥 단일뷰가 검색(`#v-home`)에 고정 안 됨** — 검색도 옛 페이지도 전부 같은 바닥 레이어에 형제로 존재, 한 곳(바닥 뷰 전환)만 막으면 전부 해결. **계획**: P1=시트 열림/닫힘 로직(`_osSheetOpen`/`_osSheetCollapse`)에 바닥을 `showView('home')`로 강제·리셋(회귀 최소) → P2=빠른 메뉴·변환기·녹취변환을 바텀시트 책상 안으로 실제 이전(스크립트는 이미 `_spSplitShow` 길 존재) → P3=빈 바닥 뷰 정리. 배경 IA=2026-06-21 "바닥=검색 / 바텀시트=책상". **대표 "저장만, 바로 진행 X"** — 다음에 "바닥 검색 전용"이라 하면 P1 작업지시서부터.

## 라이브 상태
- main `6c406d6`. PR #962 머지·GitHub Pages 라이브 전파 중(~1~2분). 작업 브랜치 삭제.

## 미완 / 다음 1순위
- **PR #962 라이브 검수**(임태성 계정: 팀/지점 공유 스크립트 첨부 노출 + 마이 스페이스 첨부 회귀 0). 특정 스크립트만 끝까지 안 보이면 그 행 `attachments` 값 직접 조회.
- **바텀시트 바닥=검색 전용 정리 P1**(위 트랙, 대표 진행 지시 시).
- **P2-b-3b 공유 알림 + send-push 팀필터 수정** = 6/23 푸시 broadcast 사고(`trg_notify_team_notice` 비활성 차단 중)와 묶어 동시 해소. ⚠️수정·검증 완료 전 트리거 재활성 금지.
- 이월: P3 전체 개방 후속(공유함 폐지·7명 team정합화·고아구독 10a859ec) / 검색·AI 다듬기 검수 / 자동 채굴 재가동 / SMS 운영 반영 / 영업노트 import.

## 교훈
- "첨부가 안 보임" 류 = 데이터 소실로 단정 말고, 사본 저장(INSERT 시 컬럼 복사) → 조회 SELECT 컬럼 → 뷰어 렌더 3단계로 끊어 추적. 이번 건은 데이터 정상·SELECT 누락+렌더 누락이 원인.
- 레이아웃 통일(#957) 같은 재작성에서 **부가 렌더(첨부) 한 조각이 빠지기 쉬움** — 통일 시 원본 뷰어의 렌더 항목 체크리스트화.
- 화면 비침 같은 구조 문제는 두더지잡기 말고 **공통 분기점(바닥 뷰 전환) 한 곳**을 찾아 막는다.
- 셧다운 후 재진입 = 직전 작업 머지·라이브 상태부터 git log로 확인(손실 0 확인).

## 메모리 갱신
- `project_bottomsheet_floor_search_only_pending` 신설(바닥=검색 전용 정리 트랙, 미착수).
