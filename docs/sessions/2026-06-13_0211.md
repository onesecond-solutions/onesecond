---
세션 종료 시각: 2026-06-13 02:11 KST
주요 마감 작업: 바텀시트 §7 SPACE 권한 트리 설계 확정 진행 (이미지 2장 기반, 코드 변경 0·설계 논의)
생성 도구: Claude Code /session-end (Code 단독 작성 — 본 세션 총괄팀장 직접 수행)
---

세션: 2026-06-13 01:39~02:11 (Code 단독, 총괄팀장). 직전 0135 세션 직후 재시작. **코드 변경 0 / DB 0 / 커밋 0 — 바텀시트 §7 SPACE 권한 트리 설계 논의 세션**. 대표님 이미지 2장(`upgrade_20260521/bottom.png`·`tree.png`) 기반으로 트리 구조 거의 확정.

## 이번 세션 본질 — 바텀시트 §7 1단계 설계 확정

직전 세션이 남긴 "설계 결재 2개"(트리 노드·시트 동작)를 대표님 이미지로 답받아 구체화. **코드 진입 전 설계 단계**.

### 시트 동작 (bottom.png)
- 현재 화면 맨 아래 바텀시트(파랑 상단선·"쓱" 핸들)를 **A영역(상단 검색바·헤더) 바로 아래까지 끌어올려 고정**.
- 시트가 화면을 거의 덮음 → 사용자는 시트를 "화면"으로 인지. **뒤(라이브러리)는 못 보게 잠금**(핸들로 못 내림). = §7.1 위장된 홈.

### 트리 구조 (tree.png) — 확정
```
🏢 에즈금융서비스 (회사 상호 = 간판처럼 표시, 펼침/진입 X)
   📍 더원지점 (지점)                    ← 트리 최상위 진입점
       ├ 📋 지점 공통 (지점원 누구나 R/W)
       │    ├ 지점 자료실   ← 현 "지점방"(지점 공지)
       │    ├ 보험사별 자료실 ← posts insurer
       │    └ 보험 Q&A      ← 현 #mys-qna 보드
       ├ 👥 4팀 (팀 소속만)
       │    ├ 팀 공지       ← team_notices(7유형)
       │    ├ 팀 자료       ← scope=team 공유(이미 일부 구현)
       │    └ 구성원        ← 현 "팀원관리"(display:none)
       └ 👤 내 스페이스 (본인만)
            ├ 자료 ← myspace 자료함 / 스크립트 / 메모 / 다이어리
```

### 🚨 핵심 본질 (대표님 명시)
- **트리 최상위 = 지점**. onesecond·회사 레벨은 트리에 **펼침 노드로 미노출**(회사 상호만 라벨/경로로 표시). 이유: 현실에서 원세컨드를 실제 쓰는 단위=지점, 회사는 결제 주체일 뿐(회사 레벨 노출=현실 괴리). 데이터(DB)는 companies>branches>teams>users 그대로(결제·관리용), **화면 트리만 지점부터**.
- **권한별 트리 가시성**(role 기반 동적 펼침): 설계사(ga_member)=본인 팀 1개 / 실장(ga_manager)=담당 팀들 / 지점장(ga_branch_manager)=지점 전체 팀(4팀·5팀·6팀…). DB role+team_id/branch_id + RLS 소속격리 + 어드민 9role 가시성 매트릭스(이전 세션)와 연결. 메모리 `visibility_is_core_permission` 정합.
- 트리는 **윈도우 폴더 멘탈 모델**(누구나 아는 구조) — 칩·탭 흉내보다 명확. 좌측 트리+우측 SPACE 콘텐츠(자료/공지/구성원 탭)+상단 breadcrumb.

### 정리 포인트 (총괄팀장 적발)
1. **라이브러리 4탭→트리 일원화**: 현 탭(다이어리/마이스페이스/공동스페이스/팀원관리)이 전부 트리 노드로 흡수 → **탭 구조 폐기**(중복 동선 방지).
2. 트리(소속 축) vs 사이드바(콘텐츠 축) 경계: 현장의소리·보험이슈·함께해요·전역스크립트는 SPACE 아니라 사이드바 유지(자료 중복 방지).

## ⏳ 다음 세션 결재 대기 (트리 완전 확정 전)
1. **레벨별 공지/자료 구성** — 지점·팀에 공지/자료를 나눌지 자료실 하나로 합칠지 일관 정리.
2. **보험사별 자료실·보험 Q&A 작성 권한** — 지점원 자유 작성 vs 열람만(보험사 공급 성격과 정합 확인. 총괄팀장 원칙 아닌 대표님 의도 확인).
3. **지점장/실장 타 팀 권한** — 다른 팀 폴더를 열람만 vs 작성도(관리 차원).
4. **잠금 해제 시점** — 검색 홈 완성 시 핸들 살려 내림(§7.3 커튼) vs 잠금 유지.

→ 위 4개 결재되면 트리 완전 확정 → 단계 커밋 구축 진입. 진실원천=`docs/specs/space_track_layout_principle_v1.md`.

## 운영
- 코드 0 / DB 0 / 커밋 0(설계 논의 세션) / main 유지 / 원장=진실.
- 직전 세션(0135) 이월: 라이브 검수(색·폰트 튄곳·악센트) 미진행, 코덱스 세팅(내일) 대기.
