---
세션 종료 시각: 2026-07-22 23:22 KST (근사)
주요 마감 작업: 지식페이지(변천사) iframe 걷어내기 3종 완주(#1382 간병 · #1383 실비·암) + V3 710px 읽기화면 3종 재연결·전체공개 전환(#1384) — 전부 라이브·실측 검증 완료
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-07-22 밤

**총괄팀장 Code · 대표 세션 · 데스크톱 앱 · PR #1382~#1384 라이브 · 대표 부재 중 "중간 승인 없이 끝까지" 논스톱 위임 완주**

## 이번 세션 한 줄

지식 페이지(변천사)를 app.html SPA 내 **iframe 임베드 → 독립 URL 페이지**로 전환하는 작업을 대표 지시로 단계별 진행(간병 파일럿 → 실비·암 확장), 이어서 de-iframe으로 꺼졌던 **V3 710px 읽기화면을 독립 URL에서 다시 켜지게 재연결**(임태성 게이트 조건 폐지 → 전체공개). 3단계 전부 라이브 반영·총괄 실측 검증 완료. 직전 세션(20:28) 미결이던 "②비로그인 전면개방(V3 게이트 해제)"을 이 세션이 실행.

## ✅ 완료·라이브 (증거)

### 1단계 — 간병 de-iframe (#1382)
- `showView` 최상단에 `key==='caregiver-history'` 리라우트 → 홈 타일·자료도구 카드·검색 바로가기·`?view=` 딥링크 **모든 진입점이 한 곳에서 `/pages/caregiver-history.html` 독립 URL로 이동.**
- `#v-caregiver-history` iframe 뷰 div·지연로드·labels 제거. 공유 CSS(`app-views.css`)에서 caregiver만 제외.
- 독립 페이지 상단 `‹ 원세컨드 홈` 백링크(`href="/"`) — 외부 유입 대응 위해 **history.back 아닌 홈 URL 직접 이동**(대표 지시).
- **관제탑 캐치**: 머지 직전 main이 동시 전진(직전 세션 V3 오버레이 #1381 계열)해 **de-iframe ↔ V3 상호작용 발견** → 대표 보고 → **대표 ①번 결정**(iframe만 걷어내고 V3 재연결은 다음 단계)대로 충돌 해소·머지. 대표 육안 확인("URL 전환·백링크·독립 렌더 정상") 통과.

### 2단계 — 실비·암 de-iframe (#1383)
- 1단계 동일 패턴으로 `silson`·`cancer-treatment` 리라우트 추가, 두 iframe 뷰·지연로드·labels 제거, **공유 CSS 규칙 완전 제거**(3종 다 de-iframe되어 불필요).
- 두 독립 페이지 상단 백링크 추가(각 페이지 팔레트: silson=`--indigo-d/--line`, cancer=`--ac-d/--hair`).
- **무접촉·회귀0**: `#v-cov-medical`(보장분석 의료실비 축·`silson_generations` 재사용)·`js/cov-medical.js` — silson iframe 뷰와 무관한 별도 화면. 손대지 않음.

### 3단계 — V3 3종 읽기화면 재연결 (#1384)
- 3종 페이지 동일 수정: **V3 활성 조건 `_v3 = true`** — 과거 `window.parent!==window && parent._canSeeXfile()`(iframe 안 + 임태성 게이트) 조건 폐지. **전체공개 페이지라 모든 방문자에게 V3 710px 타이포.** (`var _v3=false`·`if(!_v3)return` 골격 보존)
- 사이드바 '관련 자료' 링크: `window.parent.showView`(앱 의존·독립 페이지선 죽은 링크) → **독립 URL 직접 이동**(`_relurl` 맵)으로 교체. **← 이 세션 총괄 판단 1건**(타이포 복구에 딸린 필수 수정이라 스코프 안 처리).
- 헤더 주석 정정(임태성 게이트 전용 → 전체공개 V3). V3 CSS·본문 텍스트·DOM 무변경.

## 검증 (총괄 직접 — 신뢰 회복 계속 이행)

- **로컬 실렌더(localhost:5500)**: 데스크톱 1280px → 3종 다 `.v3` 적용·**읽기폭 710px**·사이드바(목차·PDF·링크복사·관련자료) 렌더·관련자료 클릭→독립 URL 이동·iframe 0·백링크 O·콘솔 에러 0. 모바일 375px → `.v3`·사이드바 숨김·**가로스크롤 없음**·16px 가독.
- **라이브 실측(curl)**: 3종 페이지 각 `_v3=true`·`_relurl`·`home-back`·own-iframe 0 / app.html 3종 리라우트 존재·iframe 뷰 3종 제거·cov-medical 유지 / **robots.txt 무접촉**(라이브 200·오늘 3개 PR 어디에도 robots 변경 0, git 확인).
- 라이브 브라우저 실렌더는 이 세션도 MCP Browser pane이 `onesecond.solutions` origin 정책차단(직전 세션과 동일). de-iframe·V3 목표는 라이브 curl + 로컬 실렌더로 확정, 도구 우회는 쌓지 않음.

## governance / 규칙 이행

- **대표 지시 준수**: robots.txt(공개) 무접촉 / 타이포 작아진 상태 → 710px 읽기화면 원래대로 복구 / V3 전체공개(임태성 게이트 조건 폐지) / "중간 승인 없이 끝까지" 논스톱 위임 완주(문제 無라 중단 없이 완주).
- **main 직접 push 금지 준수**: 제품 코드는 전부 feature 브랜치 → PR → 머지(#1382·#1383·#1384). 대표 명시 승인으로 머지 실행.
- 오늘 총괄 직접 구현(대표 "병렬 없이 총괄이 직접 빠르게" 지시). 3티어 위임 예외 = 대표 명시 지시.

## 미결 / 다음 세션

1. **대표 아침 육안 확인 대기** — `onesecond.solutions` → 홈 → 간병/실비/암 변천사 클릭 시 독립 URL 전환 + 710px 읽기화면 + 사이드바 정상 여부.
2. **로그인 전(일반) / 로그인 후(설계사 전용) 페이지 분리** — de-iframe 원래 방향의 다음 단계. 현재 V3는 전체공개로 켜 둠. 로그인 후 rich / 로그인 전 기본 분리는 별도 설계·승인.
3. (이월, 직전 세션) 사이드바 관련자료/KNOWLEDGE_COUNT 데이터구동식 · 고객관리 페이지네이션 · 입력일자 가드 · silson 5행 approved · 보장분석 실사용/고객 발송(C등급) · 검색 색인 DB편입 · 비로그인 검색개방(유출검토).
4. **워킹트리 정리** — `tmp-insurance-factory-*.png` 30+개 untracked(이월 정리 대상, 커밋 안 함).

## 상태
- main `59e9678`(#1384 머지). 오늘 PR #1382~#1384 병합·라이브(GitHub Pages built success).
- 워킹트리 = `tmp-insurance-factory-*.png` 다수 untracked(이월).
- 이 세션 총괄 검증을 실파일·라이브 curl로 끝까지 이행(직전 세션 "검증 떠넘김" 실패 교정 계속).
