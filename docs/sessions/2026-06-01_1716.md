---
세션 종료 시각: 2026-06-01 17:16 KST
주요 마감 작업: KG이니시스 정기결제(PortOne V2 빌링) 트랙 — 실연동 전환 + 구독 저장 + 월 청구 Edge Function + 심사 대응(금액 표시·offerPeriod)
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

# 2026-06-01 오후 세션 인계 노트 (집 PC, Code 단독)

> 본 세션은 Claude AI 채팅 노트 없이 Code가 전 작업 직접 수행. 주제 = **KG이니시스 정기결제 재연결 + 자동청구 시스템 구축**.
> 코드 쪽은 전부 완성·라이브. 남은 외부 관문은 KG이니시스 비인증 빌링 변경(심사완료 후 PortOne)뿐.

## 🎯 큰 그림 — 오늘 확립된 본질

원세컨드 유료 플랜(PLUS 9,900 / PRO 19,900)의 **월 정기결제(빌링)** 를 KG이니시스 실연동으로 연결하고, 발급된 빌링키로 매달 자동 청구하는 시스템을 구축. 결제 스택 = **PortOne(포트원) V2 SDK**. 프론트는 `storeId`+`channelKey`만 보유, MID/SignKey/API Secret은 PortOne 콘솔·Supabase 시크릿에만(코드/레포 평문 0).

## ✅ 오늘 마감 (전부 라이브/배포 완료)

- **KG이니시스 실연동 전환 (PR #244)** — 테스트 channelKey(`d1ae4232…`) → 실연동 `channel-key-ce197f9e-ade6-47b4-9b8e-ac721dd156c4` (MID `MOI7985785`). app.html+pricing.html. "테스트모드=실청구 안 됨" 문구 제거. storeId 동일(`store-41e93948…`).
- **구독 저장 — 조각 1 (PR #242)** — 빌링키 발급 성공 시 `/rest/v1/subscriptions` INSERT(`{plan_id, billing_key}`만 전송). user_id(=auth.uid())/next_billing_at(+1개월)/status('active')는 **DB 컬럼 기본값 = 서버 결정**(위변조 불가). 성공/실패/오류 3분기 alert.
- **subscriptions 테이블 + RLS — 조각 2** — 신버전(`pdnwgzneooyygfejrvbg`) 생성. 정책 4개(본인 select/insert, admin select/update via `public.is_admin()`). 컬럼 기본값 ALTER 적용. (Supabase SQL Editor 직접 실행)
- **월 자동청구 Edge Function — 조각 3 (PR #245)** — `charge-subscriptions`. PortOne V2 `POST /payments/{paymentId}/billing-key`로 청구일 도래분 청구. 보안=`x-cron-secret` 헤더, 멱등=paymentId(구독ID 24hex+청구월 YYYYMM), dedup=사용자별 최신 active만 청구·나머지 canceled, 금액=서버 고정 맵(plus 9900/pro 19900), 성공 시 next_billing_at +1개월. service_role로 RLS 우회.
  - **검수 통과(HTTP)**: 시크릿 없이 호출→`{"error":"권한이 없습니다."}` 401 / 시크릿 포함→`{"checked":0,"due":0,...}` 200(청구 0, 안전).
- **로그인 배경 구버전 정정 (PR #243)** — pricing.html 비로그인 CTA가 옛 브라운 index.html 위에 모달을 띄우던 것 → `/pages/landing.html?auth=X`(새 5컷 + 동일 모달). auth-modal.js가 `?auth`로 자동 오픈(기존 메커니즘 재사용).
- **KG이니시스 심사 대응 (PR #246, #247)** — requestIssueBillingKey에 `displayAmount: plan.price`+`currency:'KRW'`(발급창 우측 금액 표시, 청구X) + `offerPeriod:{interval:'1m'}`(모바일 빌링키 발급 필수).

## 🔧 인프라 메모 (다음 세션 필독)

- **Supabase secrets 등록 완료**: `CRON_SECRET`, `PORTONE_V2_API_SECRET` (값은 Supabase 콘솔/본 세션 transcript에).
- **Edge Function 슬러그 함정**: 대시보드 에디터로 처음 만들 때 슬러그가 `super-api`로 잘못 잡히고 예제코드가 올라가 `INVALID_CREDENTIALS` 발생 → **삭제 후 `charge-subscriptions`로 재배포(Claude in Chrome 의뢰)** 로 해결. 호출 URL = `https://pdnwgzneooyygfejrvbg.supabase.co/functions/v1/charge-subscriptions`. Verify JWT = OFF(cron 호출용, 보안은 x-cron-secret).
- supabase CLI는 이 PC에 미설치 → 함수 배포는 대시보드 또는 Chrome 의뢰로.

## ⛔ 현재 블로커 (외부, 대기) — 다음 세션 진입점

**KG이니시스 실연동 빌링키 발급이 "공동인증서 본인인증"만 노출** → 일반 사용자(설계사) 가입 불가(다른 인증수단 없음). **비인증 빌링키 발급**으로 변경 요청함. KG이니시스/PortOne 답변 = **"심사 완료 후 PortOne에서 수정·변경"**. 그 변경 전까지 실제 구독 1건도 못 만듦(charge 함수 검수 시 `checked:0`).

## ⏳ 변경 완료되면 남은 일 (순서)

1. 실제 PLUS 구독 1건으로 **결제→저장→청구 end-to-end 검수** (V023/공동인증서 해소 확인 + subscriptions 행 + PortOne 채널이 비인증에 맞는지)
2. **조각 4: 매일 cron** — Supabase Cron / pg_cron+pg_net로 charge-subscriptions를 매일 호출(`x-cron-secret` 헤더). 미구현.
3. **조각 5**(실패 재시도·카드만료 알림), **조각 6**(결제 성공 시 PLUS/PRO 기능 권한 부여) — (가) 최소 동작 범위 밖, 추후.

## 🚨 주의 / 학습

- **결제=본질 위험**: 추측으로 SDK 파라미터 넣지 말 것. 오늘 `displayAmount`/`offerPeriod`도 PortOne V2 공식 문서 확인 후 추가(추측 금지 준수).
- **DB 작업 첫 질문 준수**: subscriptions 생성 전 신버전(`pdnwgzneooyygfejrvbg`) 확인받고 진행.
- **팀장님 비개발자** — Supabase 대시보드 단계는 "하나씩" 아주 잘게. 시크릿 등록/함수 배포는 화면 보고 한 단계씩 안내가 정답.
- 메모리 `project_billing_recurring_track.md` 신설(트랙 상태 통째).

## 📦 PR 목록 (전부 머지)

#242(구독저장+B안) / #243(로그인배경 landing) / #244(실연동 channelKey) / #245(charge-subscriptions 함수) / #246(displayAmount 금액표시) / #247(offerPeriod 모바일).
