---
세션 종료 시각: 2026-07-03 02:39 KST
주요 마감 작업: D영역 홈 데스크(임태성 게이트) 라이브 반영 + 원수사 상품 라인업 설계·DDL·RLS·검증계획 확정(실행 전)
생성 도구: Claude Code /session-end
---

## 세션 인계 노트 — 2026-07-02 저녁~03 심야 (총괄팀장 Code 단독)

신버전 `pdnwgzneooyygfejrvbg` 이 세션 DB 실행 0(설계·문서만, 실작업 전 프로젝트 확인 게이트 대기). main 직접 push 0(전부 PR). 홈 데스크는 임태성 실장 user_id `98c5f4f9` 게이트 안 = 일반/어드민 무영향.

### 0. 세션 성격
①소식지 파이프라인 준비 확인 → ②D영역 홈 개편(바텀시트 콘텐츠를 홈 바닥으로) 라이브 → ③GitHub Pages 배포 큐 정체 진단·대응 → ④원수사 상품 라인업 설계 트랙 착수(설계·DDL·RLS·검증계획, 실행 직전 중단).

### 1. 소식지 데이터베이스화 파이프라인 = 준비 완료 확인
- 김실장·기획팀장 등록 통로(`newsletter-submit` Edge Function 3액션·검수게이트 DDL·기존 534 published 백필·스모크) + 7월 23건 Storage 업로드 + 연결표 = **전부 완료 상태 확정**(이전 세션분).
- 이번 세션엔 두 분 등록 미착수(대표 "다른 베이스작업 먼저"). 착수 시 필요 재료(엔드포인트·x-submit-key·JSON스키마·23건 해시·source_path표) 전달 완료. 등록키만 대표 별도 채널.

### 2. ⭐ D영역 홈 데스크 — 바텀시트 콘텐츠를 홈 바닥으로 (임태성 게이트, 라이브 완료)
대표+기획팀장 시안(`upgrade_20260521/new_home.png`·`new_home001.png`) 방향을 원세컨드 토큰·구조로 재작성. **바텀시트는 삭제 X**(콘텐츠만 바닥으로 내림, 시트·손잡이 유지).
- 게이트 `is-uat-homedesk`(=`_canSeeRoadmap` 동일 user_id 98c5f4f9). `#homeDesk` 컨테이너 + `_homeDeskShow`/`_homeCard`/`_homeCardClose`. **기존 엔진 `_spDiaShow`/`_spVaultShow`/`_spSplitShow` 재사용**, 홈 전용 슬롯 `#home-desk-slot`으로 격리(바텀시트 `#sn-slot` 노드 충돌 방지). `showView('scripts')`는 임태성 게이트에서 `#v-scripts`를 `.body`로 복귀시켜 바닥 전체 페이지로.
- 머지 라이브(6건): **#1034** 홈 데스크 기본(검색바+워크스페이스 4카드+자료·도구 3카드, 큰 배너 숨김) / **#1035** quick 3버튼 삭제+4카드 1열(오지시) / **#1036** 4카드 가로 1줄+인라인 토글·X닫기+첫진입 미열림+"완성된척~" 이동+"워크스페이스" 라벨 / **#1037** 상단 끌어올림(`.hs-wrap` 25vh→10px·홈 인사말 숨김)+문구 위치+카드 확대(세로 레이아웃) / **#1038** 스크립트 카드 바닥 전체 페이지.
- 확정 사양: 4카드=캘린더(diary)·업무노트(script)·자료실(lib)·메모(memo) 가로 1줄, 클릭=아래 인라인 펼침, 재클릭/X=닫힘(토글), 첫 진입 미열림. 자료·도구 3카드=스크립트(원세컨드 제공, 바닥 전체페이지)·상품라인업·소식지. "다이어리→캘린더"는 **홈 데스크 한정**(전역 미변경, 대표 지시). "무엇을 찾으세요?"=검색기 위, "완성된척~"=그 아래·검색기 위. 다른 계정=기존 홈 그대로.

### 3. 🚨 GitHub Pages 배포 큐 정체 진단·대응 (교훈)
- 오후 여러 PR 연속 머지→배포 큐 정체→`deploy` 잡이 `deployment_queued`에서 10분 타임아웃 실패 반복.
- 읽기전용 진단(대표 지시): env `github-pages` protection_rules에 wait_timer·reviewer 없음·branch=main 정상·pending 배포 0·커스텀 concurrency 0 → **저장소/환경 설정 문제 아님**. 원인=짧은 시간 연속·중복(rerun) 배포가 GitHub 기본 pages concurrency 직렬화와 겹쳐 큐 정체(GitHub 인프라 측).
- 해결: 큐 비운 뒤(진행중 run 0) **f76c57c 기준 1회만** `gh api .../pages/builds -X POST` → build 8s·deploy 8s·**총 29초 정상**(이전 10분 타임아웃과 정반대). `pages/builds` status API가 building으로 stuck 표시지연 있음=**라이브는 curl로 확인**(status API 신뢰 말 것).
- 신설 메모리 `github_pages_deploy_queue`. 원칙: 수정 모아 큐 빈 상태에서 1회 배포·실패해도 즉시 rerun 금지.

### 4. ⭐ 원수사 상품 라인업 설계 트랙 (설계·DDL·RLS·검증계획 확정 — 실행 전 중단)
대표 확정: 홈 "상품 라인업" 카드 → 독립 페이지(`?view=product-lineup`) · 시안 구조(좌 회사목록/상단 테마/조망·회사별/우 상세서랍) 유지 · **Supabase 단일 원천**(검색용 복사본 X) · **통합검색 재사용**(개별 상품 + 해당 월 전체 라인업 연관, 딥링크 `product`/`month`, 기준월 배지) · **월 단위 발행 게이트**(소식지와 동일 draft→published).
- **스키마**(7오브젝트+뷰+RPC): companies / company_snapshots(회사×월) / products / product_snapshots(상품×월) / themes / **product_snapshot_themes(테마=스냅샷 기준, 월별 변동 수용)** / **lineup_months(발행 게이트)**. 뷰 `insurer_products_current`=최신 published 전용 / RPC `insurer_products_for_month(month)`=월 분리(published만). 시안 `_p` 중복 폐기(상품 한 벌→화면 그룹화).
- **RLS**(대표 3차 보완): 스냅샷·회사스냅샷·테마 SELECT=published만(`insurer_is_published_month`/`_snapshot` SECURITY DEFINER). `lineup_months` SELECT=published만, **INSERT/UPDATE/DELETE 정책 0=authenticated(admin 포함) 직접 쓰기 전면 차단, service_role 서버 경로(Edge Function) 전용**. is_admin() 의존 완전 제거. 뷰 `security_invoker=on`·RPC `security invoker`+`search_path` 고정(호출자 RLS 우회 방지).
- 산출: `docs/work_orders/2026-07-03_product_lineup_schema_search_design.md`(설계 진실원천) · `db/migrations/2026-07-03_product_lineup_schema.sql`(DDL 초안, 실행 금지 헤더) · `design-preview/insurer-product-lineup-2026-07.html`(시안 UI 참조, 데이터는 DB 이관) · `docs/work_orders/2026-07-03_product_lineup_rls_verification_plan.md`(RLS 검증 10단계).
- 머지: **#1039**(설계+DDL+RLS 보완, 셀프검증 10항목) · **#1040**(RLS 검증계획). **머지=파일만, DDL 실행 0**.

### 5. ⏰ 다음 세션 1순위 = 상품 라인업 DB 실행·권한 검증
대표 지시(실행 대기): **①대상 프로젝트 확인**(`pdnwgzneooyygfejrvbg`) **②연결 자격/실행 방식**((a)`.env.local` `SUPABASE_DB_URL`로 Code 로컬 실행 / (b)검수팀·대표 SQL Editor) → **DDL 실행 → `__RLS_TEST_*__` 최소 시드 → 5검증**(published 조회 성공·draft 직접 SELECT 차단·draft RPC 차단·authenticated+admin UPDATE 차단·service_role UPDATE 성공) → **테스트 데이터 전량 삭제(published 유지 X)+잔존 0** → 종료 보고. service_role은 SQL Editor 관리자 성공 아닌 **실자격 스크립트/API**(자격 비노출). 통과 시 → **7월 상품 데이터 적재**(20사, `_p` 중복 제거)로. 검증 절차 진실원천=`..._rls_verification_plan.md`.
- 금지(적재 전까지): 데이터 적재·app.html 화면 연결·통합검색 수정.

### 6. 미완료·대기 (이월)
1. 소식지 7월 23건 김실장·기획팀장 create_draft 등록(대표 베이스작업 후) → Code source_path 연결·검수·published·검색노출.
2. 로컬 추가자료 인벤토리(범위 대표 지정 대기, PII/라이센스 폴더 제외 원칙).
3. (이월) 업무노트 발행 STEP6 화면검수(#1028), 검색 우측 미리보기 사양 충돌 정정, app.html 2.8MB 로딩성능 근본(코드/데이터 분리 트랙 — 배포 큐·로딩 공통 뿌리).

### 7. 교훈
- 시안=그대로 코딩 아님, 구현은 총괄팀장이 토큰·구조·게이트로 재작성. 지시 오해(1열 vs 가로) 시 즉시 정정.
- DOM 노드 단일성(_spXxxShow가 실노드 이동) → 홈 데스크는 전용 슬롯으로 격리.
- 배포 느림/실패 = 저장소 설정 파기 전 큐 상태부터(진행중 run·pending 배포·env 정책 읽기전용 점검). 큐 비우고 1회 배포. status API 아닌 curl로 라이브 확인.
- 데이터를 코드(HTML)에 인라인하면 로딩·배포·검색재사용 다 막힘 → DB 단일원천이 정답(라인업이 그 사례).
- RLS는 published 게이트를 DB 레벨에서 강제(뷰/RPC만 의존 X). 발행 변경은 service_role 서버 경로 단일화(admin 직접도 차단)로 프런트 전 경로 안전.
- main 직접 커밋 방지: 문서라도 PR. (이번 세션 검증계획 문서 main 커밋 push 전 발견→브랜치 전환)
