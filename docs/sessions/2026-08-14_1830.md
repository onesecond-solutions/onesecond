# SESSION END — 2026-08-14 18:30 KST

## 1. 프로젝트 상태

| 트랙 | 상태 | 진행률 | 우선순위 | 다음 작업 |
|---|---:|---:|---:|---|
| `/insu/` 개인 워크스페이스 | 임태성 게이트 라이브 | 85% | P1 | 로그인 실데이터 화면에서 파일 상세·폴더 조작 UX 검증 |
| 통합 데이터 모델 | 운영 DB 적용 완료 | 90% | P1 | 전체 사용자 공개 직전 증분 이관 실행 |
| Google Workspace 스타일 | 라이브 반영 완료 | 100% | P2 | 실제 사용자 피드백에 따른 미세 조정만 수행 |

## 2. 완료 증거

- PR #1618: 통합 스키마 및 최초 이관안.
- PR #1619: 기존 고아 owner UUID를 보존하는 교정 이관안.
- DB workflow `31783859065`: 적용 및 postverify 성공.
- 임태성 이관 결과: items 3,822 / customers 115 / consultations 23 / tasks 29.
- 자료·폴더·파일 및 고객은 전체 기존 원본 대비 누락 0 검증.
- PR #1620: `/insu/`를 `workspace_items`, `workspace_customers`, `workspace_consultations`, `workspace_tasks` 전용 읽기·쓰기로 전환.
- PR #1621: Google Drive 계열 폰트, 굵기, 색상, 밀도 적용.
- Pages workflow `31784772888`: 성공.
- 라이브: https://onesecond.solutions/insu/
- 현재 main: `32107f4d`.

## 3. 확정 구조

- 권한은 `private`(작성자만)와 `public`(로그인 사용자 전체) 두 단계만 사용.
- 팀·지점·회사 범위는 새 워크스페이스에서 사용하지 않음.
- `workspace_items`: 폴더, 파일, 업무노트, 메모, 링크, 문서.
- `workspace_customers`: 고객.
- `workspace_consultations`: 상담 기록.
- `workspace_tasks`: 일정과 할 일.
- 기존 원본은 삭제하지 않았으며 `legacy_source`, `legacy_id`, `legacy_payload`로 추적 가능.

## 4. 주의 사항

- 다른 사용자는 아직 기존 화면에서 기존 테이블에 새 데이터를 쓸 수 있다. 전체 사용자에게 새 화면을 공개하기 직전 증분 이관이 필요하다.
- 최초 migration `2026-08-14_workspace_unified_schema.sql`은 FK 불일치로 전체 롤백됐고 운영 DB 부분 반영은 없었다. 실제 적용본은 `_v2.sql`이다.
- `/insu/` 새 메뉴는 임태성 ID `98c5f4f9-10c1-4ee1-a656-5c2ca63239fd` 게이트만 유지한다.
- 기존 테이블과 화면으로 다시 연결하지 말고 새 통합 테이블을 기준으로 작업한다.

## 5. 다음 세션 권장 순서

1. 임태성 계정으로 `/insu/` 로그인 후 3,822개 자료의 분류별 개수와 파일함 표시를 화면에서 확인.
2. 파일 다운로드·미리보기·폴더 진입·이름 변경·이동·삭제를 새 레이아웃 안에서 완결.
3. 통합 검색에서 파일명, 노트 본문, 고객명, 상담내용, 일정 제목을 각각 회귀검증.
4. 전체 사용자 공개 전 증분 이관 migration과 postverify 추가.
5. 기존 테이블 쓰기 종료 시점과 읽기 전용 전환을 별도 승인 후 시행.

## 6. Git 상태

- 기준 브랜치: `main`
- 원격: `origin/main`
- 세션 종료 직전 코드 작업 트리 clean 확인 예정.
- 관련 PR: #1618, #1619, #1620, #1621.
