---
세션 종료 시각: 2026-06-20 00:03 KST
주요 마감 작업: 검색 미리보기 일관화(헤더 고정·좌우 독립 스크롤 근본수정·소식지 일관·돋보기 전체화면+딸깍·범위 안내·"원세컨드 제공" 라벨) + 구버전 Supabase 삭제 반영 + 검색·채굴 아키텍처 회신서
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-19 저녁~심야 (검색 미리보기 일관화 + 구버전 삭제 + 아키텍처 회신)

작성: 총괄팀장(Code) 단독 세션. main 직접 push 0(제품 PR·머지=총괄팀장 실행) / 전 작업 **3계정 미리보기 게이트** 유지(일반 사용자 영향 0) / DB 변경 0. 신버전 `pdnwgzneooyygfejrvbg`.

> ⚠️ 세션 교훈: 같은 "좌우 독립 스크롤"을 #840→#842→#843로 세 번 손댔다. 처음 두 번은 **목업(핵심 CSS만)으로 검증**해 통과했으나 라이브에선 실패 → **실제 app.html 전체 CSS(214KB)를 입힌 헤드리스 검증**으로 진짜 원인(특이성 충돌)을 잡음. **레이아웃 검증은 핵심만 추린 목업이 아니라 실제 전체 CSS를 입혀야 한다.** 또 대표가 "왜 작업마다 달라지냐"고 크게 지침 → UI/기능 요구는 채팅이 아니라 **docs/work_orders·메모리에 박제**해야 다음 세션이 안 까먹음.

## 오늘 마감 (전부 라이브, PR 머지)

### 검색 우측 미리보기 — 좌우 독립 스크롤 근본 수정
- **#840 → #841 원복**: `#v-home`을 flex 고정높이로 만든 첫 시도. 페이지 전체 스크롤을 막아버려(대표 요구 반대) + `:has` 규칙에 `.on` 게이트가 없어 다른 화면 이동 시 `.view{display:none}`을 못 이겨 **검색결과+스크립트 화면 겹침** 유발 → 즉시 원복.
- **#842**: hsResults 구조 재편 — 상단 헤더(AI 검색바·소스탭·분류칩·범위)를 `.sv-head`로 빼서 **2열 컨테이너 밖 고정**, AI 답변박스(`#aiAnsBox`)는 좌측 결과와 함께 스크롤. (기존엔 헤더가 `.sv-left` 안에 있어 결과와 함께 스크롤됐음)
- **#843 ★근본 원인**: `#app.home-search-preview #homeSearch{display:block}`(특이성 2,1,0)이 `#homeSearch.searching{display:flex}`(1,1,0)를 눌러 **#homeSearch가 한 번도 flex 컬럼이 안 됐음** → flex:1 무시 → 내부 높이 체인 붕괴. **어제 #839부터 줄곧 안 보이던 진짜 이유.** 데스크탑 한정 `#app.home-search-preview #homeSearch.searching{display:flex}`(2,2,0)로 복원. 실제 전체 CSS 헤드리스 측정으로 확정(PAGE_GROWS=false, sv-left/right 각자 내부 스크롤).
- 결과: 상단 헤더 고정 + 좌(목록)·우(미리보기) 각자 내부 스크롤 + 페이지 안 늘어남(데스크탑). 모바일=세로 stack 유지.

### 우측 미리보기 스크롤 (대표 지시→원복)
- **#844 → #845 원복**: 우측 패널 자체 스크롤 제거 시도 → 대표 "원복" 지시로 되돌림(우측도 독립 스크롤 유지).

### 소식지 미리보기 일관성 (#846)
- 소식지 저장 2종: 옛(시드)=`source_pdf_url`(공개 URL) / **2026-06 업로드 44건=`source_path`(newsletters private 버킷, source_pdf_url=null)**. `_searchOpen`이 source_pdf_url만 봐서 6월 소식지 미리보기가 안 떴음(옛 소식지만 보여 일관성 결여).
- 수정: source_pdf_url 없으면 `source_path`를 `_svSignUrl('newsletters', …)` 서명URL로 해석 → 6월 소식지도 미리보기 노출. selMap에 source_path 추가, `_pdfUrl` 도입(옛 소식지 회귀 0). **라이브에서 보임 확인** = newsletters 버킷 인증 사용자 서명 허용됨.

### 미리보기 돋보기→전체화면+딸깍 (#849, 대표 확정 사양)
- 자료실 이미지·소식지 PDF **둘 다** 동일: 미리보기 위 hover→돋보기("크게 보기") → 클릭→전체화면+딸깍.
- 공통 `.sv-d-zoomov` hover 오버레이(라인 돋보기 SVG, 기본 숨김·hover 시 표시). 소식지=`_nlOpenFull`(전체화면 모달, 딸깍 `_nlDdalggak`+새 탭+닫기), iframe `pointer-events:none`로 오버레이가 클릭 수신. 이미지=기존 `openLb`(라이트박스 4줄 딸깍). 헤드리스로 클릭 전파·오버레이 확인.

### 검색 범위 칩 안내 + 라벨 (#852·#853, 대표 제안)
- **#852**: 범위 칩 카운트 옆에 "누가 볼 수 있는지" 한 줄 — 내자료=나만 볼 수 있어요/팀/지점/제공. "내 자료 N"이 카운트가 아니라 **자물쇠 시그널**로 읽히게. (RLS는 대표 수차 확인 완료 = 표시만 추가, DB 0)
- **#853**: 라벨 "원세컨드 공동"→**"원세컨드 제공"**, 캡션 "가입자라면 누구나 볼 수 있어요"(유료 구독 오해 회피).

## 구버전 Supabase 삭제 (대표 실행 완료)
- 대표가 구버전 `onesecond-v1`(`qursjteiovcylqiepmlo`) **삭제 완료**(Unhealthy·데이터 0·트래픽 0). 월 $20~25 절감.
- 코드 검증(총괄팀장): **활성 코드(js/html/ts) 구버전 ref 0건** — 신버전 `pdnwgzneooyygfejrvbg`만. `.env` 없음, Edge Functions=SUPABASE_URL env 주입. 구버전 언급은 전부 docs "참조 금지" 주석.
- **#850**: CLAUDE.md 3곳(진실원천·주요경로·DB 첫질문)에 "2026-06-19 삭제 완료" 표기. 신버전 확인 습관은 유지.

## 검색·채굴·노출 아키텍처 회신서 (#851, 기획팀장 점검 의뢰)
- `docs/decisions/2026-06-19_search_mining_architecture_review.md`. 6레이어 정합성 점검 → **5/6 정합·구현, 신규는 시간모델(만료/아카이브 라벨) 하나, 외부 의존은 심의필 재가공권 법률 확인 하나.** Phase 3구간(지금=노출표시 / 다음=채굴 정규화 검수기준+B-2 게이트 / 나중=시간모델·대량적재(법률 선행)).
- **§6 현장 거르기 실전 예시**: 박영선님 카톡(고객 실명 송정화·박미선 + 상담콜 녹음 + 가족 얘기) → **채굴·검색·지식엔진 절대 금지**(보낸 분 판단이 정답). "동의 기반 PII 저장"은 본인 수집·동의 고객정보 얘기, 타인 상담녹음·가족·타고객명은 동의 밖.

## 미리보기 사양 박제 (#847·#848)
- `docs/work_orders/2026-06-19_search_preview_spec.md` — 헤더고정·독립스크롤·소식지 서명URL·미리보기 일관 딸깍허브(이미지·소식지 동일). 다음 세션 미리보기 작업의 진실원천.

## 대표 전략 결정 (이번 세션)
- "원세컨드 제공"의 **알맹이 = 채굴된 지식(지식엔진)** 이 맞다. 소식지는 **원천/근거**. 현재 제공 46 = 소식지 40 + 지식 3 + 스크립트 3 → 지식이 아직 빈약. **소식지를 지금 빼면 안 됨**(풀이 휑해짐) → 채굴 재가동으로 지식을 채운 뒤 소식지를 근거 레이어로 분리할지 결정.

## 다음 세션 1순위
1. **★채굴 재가동** (회신서 Phase 1) — "원세컨드 제공"의 지식 알맹이 채우기. 선결: ①정규화 검수 기준 1장 ②B-2 정제본 검수 게이트(대기 중). 총괄팀장이 초안 작성 예정. 채굴팀 🔴 정체 해소.
2. **외부(플랫폼 밖) 백업 설계** — 대표 "시간 여유 시 진행" + **"매일 물어봐"**. 매 세션 START 보고 [6.Backlog]에 노출 + 진행여부 한 줄 질문. (메모리 `backup_external_daily_reminder`)

## 미머지/대기 (이월)
- **PR #835** — B-2 OCR 정제 백필(미머지, 직전 세션부터). 머지→ocr-batch 재배포→밤샘 백필→정제본 품질 검수→B-3 검색 합집합.
- **PR #802** — AI 파일명 '정리 중' 시그널(6/17부터 미머지).
- 전략 3대 정정(중립=공정비교·PII=동의기반·요금제=미래) master_strategy 반영 = 대표 결재 대기.

## 학습/메모리
- 신설: `search_preview_consistency_spec`(미리보기 일관 사양·work_orders 박제 원칙), `backup_external_daily_reminder`(외부 백업 매 세션 물어보기).
- 레이아웃 검증 = 핵심만 추린 목업 금지, **실제 전체 CSS 입힌 헤드리스**로(특이성 충돌 놓침 사례). UI/기능 요구는 채팅 아닌 work_orders·메모리 박제. 시각 변경은 대표 라이브 눈 검수.
