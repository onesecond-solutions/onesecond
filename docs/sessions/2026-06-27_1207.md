---
세션 종료 시각: 2026-06-27 12:07 KST
주요 마감 작업: 상품 로드맵 씨드 검증(A~H) + app.html 임태성 전용 검수화면 편입·라이브
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-27 오전(이어짐)

> 총괄팀장(Code) 단독 작업. main 직접 push=세션노트만, 제품 변경=전부 PR. DB·통합검색 변경 0. 신버전(`pdnwgzneooyygfejrvbg`).

## 오늘 마감 (머지·라이브)
- **#979** 글랜스 + 색상 2단계 예약 박제(docs, 라이브 영향 0)
- **#980** 전 보험사 상품 로드맵 씨드 원장 보존 — 30사/260상품 + 인수기준 + 탐색 시안(검증 완료 데이터 아님·비공개 씨드 보존 목적)
- **#981** 상품 로드맵 시안을 **app.html `#v-roadmap`에 편입** — 임태성 실장 user_id 전용 검수화면
- **#982** 로드맵 하단 바텀시트 가림 수정(flex 높이 체인, v-voice 패턴)
- **#983** 로드맵 패널 ↔ 바텀시트 분리(하단 22px 간격, 시트 기존 상단 그림자 드러냄)

## 상품 로드맵 UAT 화면 (신규 라이브)
- **게이트** `_canSeeRoadmap()` = `os_user.id==='98c5f4f9-10c1-4ee1-a656-5c2ca63239fd'`(임태성 실장 user_id). 이름/표시명/role 아닌 확정 user_id로 판별(`_wavPlaybackEnabled` 동일 식별값). **어드민 포함 타 계정 전면 차단**.
- **3중 차단**: 메뉴(`.roadmap-only`+`body.is-uat-roadmap`)+`showView` 라우트가드(`?view=roadmap`→home)+로더 이중가드.
- **격리**: 통합검색 인덱스와 무관(별도 `docs/data` JSON fetch)·raw_seed 비공개·"가입 가능" 단정 금지(원문 사실만).
- 게이트 계정별 시뮬레이션 통과: 임태성만 노출/통과, 어드민·일반·"이름만 임태성(id다름)"·비로그인 전부 차단.

## 씨드 검증 결과 (A~H, raw_seed 초안·검수 전)
원문 진실원천=`db/newsletters/02_insert_67.sql`(소식지 67건 full_text).
- **B 중복**: 완전중복 0 / 손보 특약·담보가 상품으로 오인 추출 ~20건(메리츠·흥국생명)
- **C 비상품**: 순수 홍보·이벤트 0 / 특약·담보 단위 오인이 실질 이슈
- **D 분류근거 부재(핵심)**: coverType 자유텍스트(82종)뿐, 건강체/유병자 enum 없음 → 집계마다 미상 83~116 흔들림(재현 불가)
- **E 가입기준**: underwriting 공백 81 + 빈약 37 = 118건 미기재/부실
- **F 출처**: 260/260 회사 단위 연결(상품별 1:1 아님)
- **G 미상 원인**: 둘다공백 41·underwriting공백 25·coverType공백 12·분류어없음 30 (약 80%가 필드 공백=미수집)
- **H 0건 3사**: 미래에셋·푸본현대·DB손보 = **전부 추출 실패**(소식지·상품 모두 존재, 부재/언급없음/정규화실패 아님)

## 상태값 설계(확정)
raw_seed → reviewing → verified → published → hold / superseded / withdrawn. **현재 260건 전부 raw_seed, published 0**.

## 다음 세션 우선순위
1. **임태성 실장 계정 라이브 검수**(상품 로드맵 메뉴·화면·하단 분리·라우트 차단 교차검증) — Code는 라이브 캡처 불가, 대표님/임태성 확인
2. **씨드 보강 5종**: ①분류 카테고리 enum 신설 ②underwriting 118건 원문 재추출 ③손보 특약/담보 분리 ④0건 3사(미래에셋·푸본·DB손보) 26.04 소식지 재추출 ⑤상품별 출처·기준일 1:1
3. 이후: 상품 마스터 스키마 → DB 적재(임태성) → 통합검색 합류(검수 완료분만)
4. 외부 백업 — 토/일 재질문(오늘 토요일)

## 운영 주의
- 상품 로드맵 불변 원칙: "가입 가능" 단정 금지·중립성·기준일/출처 필수·흡수멘트 금지
- 현장의 소리 숨김(#973) 라이브 모니터 계속
- send-push 팀필터(`trg_notify_team_notice` 비활성 차단 중·⚠️수정 전 재활성 금지)

## 신설 메모리
- `project_product_roadmap_uat_track` — 상품 로드맵 임태성 전용 검수화면 + 씨드 검증·보강 대상

## 교훈
- 소식지 원문 repo(`db/newsletters/02_insert_67.sql`) 확보로 DB 직접접근 없이 추출 정확성·0건 원인(추출 실패 vs 부재) 판정 가능
- 신규 뷰는 뷰포트 고정값(vh) 대신 기존 자체스크롤 뷰(v-voice) **flex 높이 체인** 패턴 따라야 바텀시트 가림 0
- 바텀시트 분리는 시트 요소 수정 아닌 **본문 패널 하단 간격**만으로 해결(시트 기존 그림자가 드러남) = 최소 침습
- 권한 격리는 user_id 기반 + 메뉴·라우트가드·로더 3중(이름 판별 금지)
