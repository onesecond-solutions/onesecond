---
세션 종료 시각: 2026-06-16 17:14 KST
주요 마감 작업: 거미줄 조직 후속 + 실시간 presence + 매니저방 내보내기 + 자료실 scope 버그 해결 (PR #769~#784)
생성 도구: Claude Code /session-end
---

# 세션 인계 노트 — 2026-06-16 (오전~오후) 거미줄 조직 후속 + 실시간 presence + 매니저방 내보내기 + 자료실 scope 버그 해결

작성: 총괄팀장(Code) 단독. main 직접 push 0(전부 PR). 머지=총괄팀장 실행 / 대표=통과 결재. DB·Storage 실행=대표 직접(신버전 pdnwgzneooyygfejrvbg).

## 오늘 마감 (PR #769~#784 전부 머지·라이브)

- #769·#770 홈 포털(3열 메뉴+호버 안내) 제거, '원세컨드 시작하기' 버튼만 유지 + 위치 하향(margin 14→80px).
- #771 어드민 대시보드 '미배정 N명' 카드 → 운영>조직트리 '미배정' 노드(해당 인원만)로 연결. acGoUnassigned + _ctPendingPick.
- #772 (PR-2) handle_new_user 트리거 보강 — 직접 가입 시 new_branch_name/new_team_name이 오면 branches/teams 자동 생성·연결(동명 재사용, 회사 연결). 기존엔 신규명 무시→미배정으로 새던 구멍. branches.ga_org_name NOT NULL이라 그것도 채움. (DB 대표 실행)
- #776 touch_last_seen RPC(SECURITY DEFINER, id=auth.uid()) + 프론트 heartbeat(앱 진입+2분, visibility 가드). presence 기준 '오늘 자정'→'최근 10분'(_mrOnlineSince). 근본: users.last_seen_at이 전원 NULL(기록 로직 미구현)이라 매니저방 로그인 불이 항상 꺼져 있던 것.
- #777 어드민 관제센터 presence를 매니저방과 통일 — 소스 activity_logs→users.last_seen_at, 창 오늘자정→최근10분. ('오늘 접속' KPI는 activity_logs 유지)
- #773 매니저방 '+ 실/팀 추가'(실장 초대) + '+ 팀원 추가'(기존 팀 ga_member 초대) 두 버튼 분리(지점장·실장·어드민 빈상태). #774 어드민 빈상태에도 두 버튼. #775 1행 2카드 색상(실/팀=브랜드 --ac, 팀원=--color-success). #778 구성원 정렬 지점장>실장>구성원(가나다).
- #781 어드민 조직트리 구성원 정렬 = 직책 위계 → 로그인중 위 → 가나다.
- #779·#780 내보내기(조직 분리) 1·2·3단계: remove_member(회사·지점·팀 NULL+role ga_member, 권한 admin/지점장/실장 위계), keep_after_removal/withdraw_after_removal, users.removed_at·removed_by·removal_ack 컬럼. 본인 로그인 시 안내 모달(유지/탈퇴), 어드민 상세에 내보낸 기록 표시. (DB 대표 실행)
- shares 테이블 DDL(docs/migrations/2026-06-14_shares.sql) 실행으로 공유하기/공유받기 복구 — 코드는 6/14 완성됐으나 테이블 미생성으로 404였음. (DB 대표 실행)
- #782·#784 자료실 scope 버그 해결(핵심): 자동 폴더 생성 _mvCreateFolderRow가 _mvScopeRow() 누락 → 폴더가 DB 기본값 'personal'로 저장 → 지점/팀 폴더가 개인 자료실에 새고 지점 자료실엔 안 떠 그 안 파일도 0개. #782=scope를 #mys-vault DOM data-scope로 고정+개인필터(owner+or(null,personal)), #784=자동폴더 scope 적용. 진단 배지(#783, 제거됨)로 원인 확정.

## DB/Storage 대표 직접 실행분
handle_new_user PR-2, touch_last_seen, remove_member·keep·withdraw RPC, users.removed_at/removed_by/removal_ack 컬럼, shares 테이블+RLS, 임시팀 1~3팀 DELETE(소속자 0), 미배정 6명 4팀 연결+민인환 직할팀 생성, 어제~오늘 자료함 정리(파일 13건 등).

## 데이터 사고 정리(학습)
- 임태성이 어제 4팀 자료실(team scope)에 3450건(7.7GB) 정상 업로드했으나, 화면 표시 버그(개인 자료실에 owner 기준으로 섞여 보임)로 "잘못 올림"으로 오해→삭제 요청. 내가 hard delete(DELETE FROM)+Storage 폴더 삭제로 안내해 영구 삭제됨. 원본 보유로 재업로드 예정. → 교훈: 사용자 데이터 대량 삭제는 소프트삭제(휴지통/deleted_at)로 안내했어야. 저장은 정상이었고 표시 버그였다.

## 미완 / 다음 우선순위
1. 어제~오늘 자료 원본 재업로드(지점/팀 자료실) — 새 코드(#784)로 폴더·파일 정상 저장. 대량 전 1세트 검증 권장.
2. Storage orphan 정리(어제 3450 일부 + 오늘 13건 ~1.9MB) — 용량, 나중 일괄.
3. 내보내기 2·3단계 라이브 검수(시크릿 GA 계정으로 본인 안내 모달 확인).
4. 5b 운영탭 상황설정(거미줄 조직 트랙 미완, 5a 패턴 재사용).
5. GA 계정 라이브 검증(초대→가입→합류→트리 한바퀴, 어드민은 격리 검수 함정).
6. 채굴팀 🔴 정체(cycle4 25건 검수대기, knowledge 914).
7. (예약) 작업 흐름 트랙(WORK_TRACKS) 검수.

## 학습/메모리
- 자료함 scope 공용 테이블 구조 + _mvScopeRow 일관 적용(메모리 project_vault_scope_architecture 신설). scope 기본값 personal이라 안 보내면 개인으로 샘. owner_id=text.
- presence = last_seen_at heartbeat 필요(미기록 시 항상 꺼짐). 어드민/매니저방 기준 통일.
- 신버전 users엔 auth_user_id 컬럼 없음 → RPC는 id=auth.uid()만.
- 진단 배지(현재 scope+받은 파일 분포)로 추측 없이 원인 확정한 사례.
- 한 브라우저(프로필)=한 계정. 어드민+실장 동시 검수는 시크릿 창/프로필 분리.
- SQL 줄 때 한글 주석 긴 줄은 복사 중 줄바꿈 깨짐(42601) → 본질 위험 SQL은 주석 제거판 제공.
