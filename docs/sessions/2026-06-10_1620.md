---
세션 종료 시각: 2026-06-10 16:20 KST
주요 마감 작업: 자료함(library vault) 트랙 전구간 라이브(DB 골조~다형식 뷰어) + 한글키·서명URL 버그 근본수정 + radius 직각 봉합 + 사이클1 학습일지 백필 실행
생성 도구: Claude Code /session-end
---

세션: 2026-06-10 오전~오후 (Code 단독, 총괄팀장 직접). PR 머지 13건 (#517~#529, #526 다형식뷰어 포함) / main 직접 push 0 / DB는 대표님 직접 실행. 자료함(library vault) 트랙 전구간 라이브 + 사이클1 학습일지 백필 실행 + 사이클2 핸드오프.

## 🔝 다음 세션 1순위
1. **자료함 10종 형식별 미리보기 검수** (대표님 추후 별도 문의 예정) — jpg/png/pdf/txt/csv/docx/xlsx/pptx/mp4/mp3 정상/부분정상/실패 분류. 이미지·PDF·영상·음성·txt·csv = 기존 인프라(정상 기대) / docx·xlsx·pptx = lazy CDN(mammoth/SheetJS/JSZip) → 실패 시 콘솔 에러로 그것만 패치. 성공 항목은 건드리지 말 것.
2. **PR #522 사이클2 핸드오프 머지 결재** + 김실장(GPT) 산출물 대기. 채굴 주체=김실장, Code=PR 저장. dedup 베이스라인 SELECT를 대표님이 신버전 실행→김실장 전달.
3. **파일/폴더 이동(move) = P1.5** (별 PR, 다음 트랙).
4. (후순위) 자료함 통합 정책(평면 library '자료' ↔ 자료함)·티어·용량·AI 인덱싱.

## 완료 — 머지 PR (전부 라이브/전파, main 직접 push 0)

### A. 자료함 DB 골조 (#518)
- myspace_folders / myspace_files / myspace_usage 신설 + Storage private 버킷 myspace + RLS (auth.uid())::text=owner_id. owner_id uuid→text 정정(library/scripts 정합). id=uuid(파일 비열거성). 실측 SELECT로 미존재 확인 후 대표님 직접 실행. STEP4 RLS 즉시 적용(1번안). 스펙=docs/specs/myspace_library_v1.md.

### B. 자료함 프론트 P1~PR2 (#519·#520·#521·#523)
- #519 프론트 골조: 마이스페이스 '자료'칩→'자료함' 교체, 3분할(폴더트리+드롭존/파일그리드/미리보기), 파일 업로드(드래그+선택), signed URL 미리보기, 개인정보 고지 게이트.
- #520 CRUD: 폴더·파일 이름변경/삭제, ⋮ 플로팅 액션메뉴.
- #521 P1 후속(UX 정상화+가드3): 폴더 삭제=빈폴더 단순확인/내용 있으면 강한 모달(조용한 cascade 금지), 파일 삭제=Storage+DB 동시, search_text 갱신, 빈 CTA '첫 폴더 만들기', 고지 톤 완화. 레이아웃 풀폭 고지+3컬럼 20/30/50. '이월 라이브러리 삭제버그'=버그 아님 확인(library_delete_own RLS 존재).
- #523 PR2 폴더 재귀 업로드: '+폴더' 버튼(webkitdirectory)+드래그 재귀(webkitGetAsEntry), 계층 보존, 빈폴더 생성, 중복명 '이름 (2)' 자동, 대량 진행·부분실패(성공분 유지·전체롤백 금지).

### C. 자료함 버그 2건 — 라이브 진단으로 근본 확정 (추측 패치 0)
- **#524 한글 파일명 400 InvalidKey**: Supabase Storage 키에 비ASCII 거부. 콘솔 진단(ASCII200/한글400/INSERT201) → storage 키 ASCII화(uuid+ASCII슬러그), 한글 원본명은 original_name(DB)에 보존.
- **#529 미리보기 서명 URL /storage/v1 누락**: PNG/PDF '미리보기 미지원'+'requested path is invalid' 원인 = Supabase sign 응답 signedURL=/object/sign/...(/storage/v1 없음), window.db.url(raw)면 누락. 콘솔 진단(img 실패=CORS 아님→URL invalid 확정) → _mvSignedFull(/storage/v1 정확 prepend). 자료실 첨부 _openSignedAtt 동일 버그 합본 수정. **이 한 줄이 전 형식 미리보기를 막던 원인.**

### D. 자료함 UI (#525·#526·#527·#528)
- #525 깊이(작업7): 3컬럼 카드화(--s1+border+radius-lg+약한 그림자), 빈상태 원형아이콘+안내문.
- #526 다형식 뷰어(작업8): 이미지(onerror폴백)·PDF(loadPdfJs+페이지이동)·영상·음성·txt/md/json·CSV표·DOCX(mammoth)·XLSX(SheetJS)·PPTX(JSZip 슬라이드텍스트)·미지원(아이콘+다운로드). lazy CDN on-demand. 딸깍=이미지 클릭→openLb 4줄.
- #527 동일 높이(작업10): 3컬럼 height:clamp(440px,calc(100vh-300px),760px)+내부 스크롤, 바닥선 일치, 빈상태 중앙정렬.
- #528 radius 전역(작업9): **근본 발견** — app.html이 tokens.css/tokens-v2.css 미링크(pretendard만), 인라인 :root에 색상만 있고 --radius-* 미정의 → var(--radius-sm) 31곳·var(--radius-lg)가 fallback 없이 직각(0) 렌더되던 게 '직각·밍밍함'의 진짜 원인. 인라인 :root에 radius 토큰 정의(xs8/sm11/md13/lg18/xl18). 순수 가산이라 px하드코딩 영향0, 전역(모든 화면 버튼·카드 라운딩).

### E. 지식엔진
- **#517 사이클1 학습일지 백필 실행 완료**(대표님 직접): knowledge_logs 0→1행(id1, knowledge-cycle1, extracted_count 92, ai_draft). 0x0d CRLF 함정→jsonb_build_*로 정정 후 성공.
- **#522 사이클2 채굴 핸드오프**(결재 대기, 머지 안 됨): docs/knowledge/cycle2/00_handoff_and_guards.md. 채굴 주체=김실장(GPT), Code=PR 저장. dedup 베이스라인 235건 read-only SELECT + 가드(중립·비영리·검색가치우선·레이어분리·'스마트 게시판' 호칭·공식출처만) + 산출형식(review_summary+proposed_cycle2_mined.sql, ai_draft, 실행금지).

## DB 현황 (신버전 pdnwgzneooyygfejrvbg)
- myspace_folders/files/usage 생성됨 + 버킷 myspace(private) + RLS 정책 4종. 대표님 직접 실행·검증(정책 4건).
- knowledge_logs 1행(사이클1 백필). knowledge_entries 235 변동 없음.

## 미실행 / 대기
- 자료함 10종 형식별 검수 (대표님 추후).
- PR #522 머지 결재 + 사이클2 김실장 산출물.
- (전파 중) #527·#528 라이브 catch-up.

## 🚨 학습 (메모리 3건 신설)
- **feedback_jsonb_crlf_use_build_functions**: raw JSON ::jsonb는 0x0d(CR) 파스에러 → jsonb_build_object/array 조립.
- **feedback_supabase_storage_ascii_key**: Storage 키에 한글=400 InvalidKey → 키 ASCII, 원본명은 original_name.
- **feedback_supabase_signed_url_storage_v1**: sign 응답 signedURL은 /storage/v1 없이 반환 → SUPABASE_URL+'/storage/v1'+raw. img 실패=CORS 아님(img는 표시에 CORS 불필요)→URL invalid 의심.
- 공통 원칙: **버그는 추측 패치 0, 콘솔/응답 body로 원인 확정 후 정확 수정**(400·서명URL 둘 다 1샷 해결). 성공 항목 불가침.

## 운영
- main 직접 push 0 / DB=대표님 직접 실행 영역 준수 / 모든 함수 window.* 등록 / 모든 미리보기=클라 렌더(서버 저장·로깅 0, §4 정합).
