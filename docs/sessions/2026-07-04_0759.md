---
세션 종료 시각: 2026-07-04 07:59 KST
주요 마감 작업: 분리형 PG* 전환 완료(#1053) + 마이그레이션 배포 인증 실패 원인을 PGPASSWORD 값 불일치로 확정(파싱 원인 클래스 제거)
생성 도구: Claude Code /session-end (총괄팀장 Code 직접 작성)
---

# 세션 인계 노트 — 2026-07-04 새벽~오전 (총괄팀장 Code 단독)

> 신버전 `pdnwgzneooyygfejrvbg`. main 직접 push 0(전부 PR). 이 세션 성격 = **resid-fix RPC 배포 차단 원인 규명 + CI 연결 방식을 URI 통짜 → 분리형 PG* 로 전환**. DB 반영 0 유지 · newsletters 무변경.

## 🎯 이번 세션 흐름 (증거)

직전 세션(2026-07-03 21:21) 최대 차단 = resid-fix RPC 배포가 `SUPABASE_DB_URL` 인증 실패로 미완. 이 세션은 그 원인을 **운영 무접촉 진단 → 분리형 전환**으로 파고들었으나, **배포는 여전히 인증 실패(값 불일치)로 미완.**

### (1) 비운영 parse-only 진단 통로 구축 → 원인 1차 규명 (PR #1052, 이후 정리됨)
- 대표 방침: "추가 배포 승인 run 말고, DB 접속 없이 validate 단계에서 파싱 상태만 확인."
- 구조적 사실 확정: `SUPABASE_DB_URL`은 **production-db Environment 전용 secret**(repo 레벨 없음)·리뷰어=대표 → 승인 없는 잡은 구조상 못 읽음. pw 플래그는 #1051에서 추가돼 3회 실패 당시 **돈 적 없었음**.
- 대표가 db-validate 복제 방식 거부(보호 없는 환경에 운영 URL 복제 = 보안 우회) → **production-db 아래 "진단 전용 run 1회 승인" 방식**으로 재구성(psql 미설치·SQL·apply 경로 0·상태 플래그만).
- **진단 run 성공**: `pw_state=filled · pw_len=20 · pw_percent=none · pw_decode_changed=no`, host/user/ref/db 전부 정상. → **percent-decode 오손·플레이스홀더 배제** → 원인을 "비밀번호 값"으로 좁힘. (기존 `apply_and_verify.sh` DIAG의 percentlike 오탐 결함도 발견·교정: 디코드 후 검사 → 디코드 전 원본 검사.)

### (2) DB 비밀번호 재설정 후에도 배포 #6 실패 → 6항목 구분 확정
- 대표가 DB 비밀번호 재설정 + `SUPABASE_DB_URL` secret 갱신 → 배포 #6 **또 실패**.
- 로그 정밀 분석(scrub·비번 미노출): `DIAG ... pw_len=16 ... pw_state=filled` + `FATAL: password authentication failed for user "postgres"` + `STOP` + exit 1.
  1. `select 1` 가드에서 실패 **YES** / 2. 오류 = **password auth failed**(TCP는 pooler 정상 도달, 서버가 비번 능동 거부) / 3. migration SQL 도달 **NO**(auth 게이트에서 막힘) / 4. **DB 반영 0**(apply·begin/commit 미실행, 함수 0) / 5. **새 secret 읽음 확정**(pw_len 20→16) / 6. **pooler vs direct 문제 아님**(연결·username 라우팅 정상, auth만 거부).
- 결론: 새 secret은 반영·읽혔으나 **파싱·전송된 비밀번호가 여전히 거부**. 남은 원인 = (A) URI 특수문자/percent 파싱 오손 or (B) 값 불일치. 기존 DIAG `pw_percent`는 디코드 후 검사라 (A) 배제 불가.

### (3) 운영 연결 읽기 전용 조사 → DB 비밀번호 재설정 안전 확정
- 저장소·GitHub Actions·Edge Functions·외부 배포 설정·로컬 문서 전수(읽기 전용).
- **DB 비밀번호(직접 Postgres 연결)를 쓰는 곳 = CI 2세트뿐**(db-migrate + 당시 진단), 둘 다 on-demand·승인 게이트.
- 라이브 전부는 **anon/service_role REST**(Edge Function 15개 전부 `createClient`, 직접 연결 라이브러리 0)·pg_cron/트리거는 **CRON_SECRET + HTTP**·라이브=GitHub Pages 정적. **DB 비밀번호와 API 키(anon/service_role/JWT)는 독립** → 재설정해도 라이브 무중단. → 판정 **1번(운영 연결 없음)**.

### (4) ⭐ 분리형 PG* 컴포넌트 전환 (PR #1053 머지 — AI 팀 표준 플로우)
- 대표 결재: URI 통짜 secret이 파싱·percent-decode·@분리 오손 원인 클래스를 계속 남김 → **제거**.
- `apply_and_verify.sh`: `_dec`/scheme·@·query 분해/자동보정 **전부 삭제**. psql은 PG* 환경변수만(conninfo/URI 인자 0). **PGPASSWORD 존재만 강제·값/길이/해시/부분/상태 전부 미출력**(DIAG는 host/user/db/port/sslmode만). `select 1` 실패 시 STOP → 성공 시에만 apply 유지.
- `db-migrate.yml`: 공개값(PGHOST=aws-1-ap-south-1.pooler.supabase.com·PGPORT=5432·PGDATABASE=postgres·PGUSER=postgres.pdnwgzneooyygfejrvbg·PGSSLMODE=require·PROJECT_REF) workflow env 고정, 비밀은 **`secrets.PGPASSWORD` 하나만**.
- 진단 도구(`validate_db_url.sh`·`db-url-diagnose.yml`) 삭제(원인 확정 완료·URI 파싱 잔재 정리). db-validate 환경도 삭제.
- 로컬 `bash -n` 통과·독립검수 통과(PGPASSWORD 노출 0·psql URI 인자 0·select 1 STOP 유지). **PR #1053 머지**(대표 직접 머지 X, AI 팀 처리).

## 🚨 현재 차단 (다음 세션 1순위)

**분리형 PG*(URI 파싱 완전 제거)에서도 배포가 인증 실패 — 원인 = PGPASSWORD 값 불일치로 확정.**
- 대표가 production-db에 `PGPASSWORD` secret 등록(22:55) → 배포 run(28686848502) dispatch → 대표 승인 → deploy 실행.
- 로그: `DIAG host=...pooler... user=postgres.pdnwgzneooyygfejrvbg db=postgres port=5432 sslmode=require`(비번 미노출) → `FATAL: password authentication failed for user "postgres"` → `STOP` → exit 1. **DB 반영 0 확정**(apply 미도달).
- **파싱 오손 가능성 100% 제거**(URI 파싱 없음·PGPASSWORD 원문 전달)에도 실패 → 원인은 순수 **값 불일치**: 등록된 PGPASSWORD ≠ 실제 현재 DB 비밀번호. 후보: 붙여넣기 오타/부분복사·**앞뒤 공백/개행**(GitHub secret 붙여넣기 시)·재설정 값과 입력 값 상이·(가능성 낮음) Supavisor 자격 전파 지연.

## 다음 세션 순서 (합의 대기)
1. **PGPASSWORD 재확인/재입력**(대표 계정 전용): Supabase Settings→Database 비밀번호를 다시 정확히 확인(또는 재설정) → production-db `PGPASSWORD`에 **원문 그대로·공백/개행 없이** 재저장. (연결 문자열 전체·URL인코딩·따옴표 금지 — 이전과 동일.)
2. 재저장 후 **배포 run 1회만** 요청(가드 재확인 후). 성공 시 resid-fix RPC 설치 → postverify → 감사센터 라이브 미리보기 → 문서별 실 ID·fix/hold 확정 → 8항목 보고 → 대표 데이터 버튼 1 → 사후검증.
3. **여전히 실패 시** = Supavisor 세션 pooler 자격 문제 의심 → direct connection(`db.<ref>.supabase.co`) 검토. 단 GitHub 러너는 IPv4·Supabase direct는 IPv6 → 러너에서 direct 미도달 가능(Supabase가 CI에 pooler 권장하는 이유). 이 경우 pooler 자격 전파/재설정을 Supabase 쪽에서 재점검.
4. 배포 성공 후: `SUPABASE_DB_URL` secret 삭제(분리형 미사용), `_COMPANY_ALIASES` 보강(MG손해보험 등).

## 머지/브랜치
- 머지: PR #1052(진단 통로·이후 #1053에서 도구 제거)·**#1053(분리형 PG* 전환)**. main 직접 push 0.
- 미머지 브랜치: 없음(작업 브랜치 전부 반영·삭제). 나머지 origin 브랜치는 이력용.
- production-db 상태: secret `PGPASSWORD`(22:55 등록·값 불일치 추정)·`SUPABASE_DB_URL`(미사용·배포 성공 후 삭제 예정). db-validate 환경 삭제됨.

## 교훈
- **파싱 원인 클래스를 통째 제거**하면(URI→분리형 PG*) 실패 시 원인이 순수 값으로 좁혀져 디버깅이 결정적이 됨. 통짜 URI secret은 percent-decode·@분리 오손을 계속 숨김.
- Environment secret은 그 환경을 쓰는 잡만 읽고 = 리뷰어 승인 유발 → "승인 없는 진단"은 secret을 보호 없는 곳에 복제해야 가능(=보안 우회). 대표 판단대로 **복제 금지, production-db 아래 진단 전용 run**이 정답.
- DB 비밀번호와 API 키(anon/service_role/JWT)는 독립 → 비밀번호 재설정은 라이브(REST·pg_cron HTTP·GitHub Pages) 무영향. "운영 연결" 조사는 직접 Postgres 연결만 대상.
- CI secret은 값·길이·해시·부분·argv·로그 전부 미출력이 원칙(DIAG는 접속 대상 메타만). GitHub secret 붙여넣기 시 **앞뒤 공백/개행**이 조용한 인증 실패를 만든다.
- `select 1` 연결 가드 = 인증 실패 시 apply 전 STOP → 어떤 값 오류에도 **DB 반영 0** 안전 정지(이 세션 배포 2회 모두 DB 무변경으로 입증).
