---
세션 종료 시각: 2026-06-20 17:14 KST
주요 마감 작업: 채굴 mine-batch Phase A~F 완성(회귀 전부 해소) + 영업노트 세 층 DB 토대
생성 도구: Claude Code /session-end (Code 진행 세션 — Code가 인계 노트 작성)
---

# 세션 인계 노트 — 2026-06-20 (채굴 Phase A~F 완성 + 영업노트 DB 토대)

작성: 총괄팀장(Code) 단독 진행. main 직접 push 0(제품 PR·머지=총괄팀장). 신버전 `pdnwgzneooyygfejrvbg`. ⚠️토큰 89% 도달로 새 큰 작업 직전 매듭(영업노트 import/화면·채굴 실적재는 충전 후).

## 오늘 마감
- **채굴 mine-batch Phase A~F 완성** — 회귀 전부 잡고 게이트 완성. 첫 제한 실적재 직전까지.
- **영업노트** 세 층 DB 생성 + 엑셀 파싱 + owner 확정 (토대 완성, import/화면은 다음).

## 채굴 (mine-batch) — 생산라인 첫 가동 직전까지
- **검수기준 v1 박제 (#854)**: 소스적격성 게이트(eligible_public/internal_only/manual_review/source_rejected)·개인정보 hold/discard 분기·자동 diff·시의성 4상태(current/archive/expired/unknown)·최종 4분기(approved/hold/archive/discard)·표본20건. 진실원천 `docs/decisions/2026-06-20_mining_review_criteria_v1.md`.
- **B-2 OCR 정제(clean_text) #835** 머지·검수팀 배포·품질 게이트 통과.
- **mine-batch Edge Function** (`supabase/functions/mine-batch/index.ts`): Phase A(#855 기반+dry-run+가드) → C(#856 diff·PII 정밀화+표본지정) → D(#857 시의성/uncertainty 축분리) → E(#858 숫자 안전정규화+비정상추정금지) → F(#859 비율/금액/통화 타입분리+토큰경계 lookaround+줄바꿈제외).
- **회귀 전부 해소(20건 검증)**: 교보·iM(120.2%·3.0% 형식오탐)→ai_draft 복원 / IBK(79 환각) hard_fail 유지 / NH·AIA(OCR 비정상 숫자) hold / 농협손보(49)=**D확정**(Gemini 실행변동, 코드 정상 — numberTokens는 149/6490서 49 부분추출 0을 단위검증). 기존 ai_draft 9건 불변, 코드 회귀 0.
- 단위테스트 `scripts/mine_batch_number_diff.test.cjs` (ALL PASS, PR 포함).
- **DDL(대표 실행)**: `knowledge_mining_state` 테이블 + `knowledge_entries` 컬럼4(lifecycle_status·diff_flag·pipeline_version·source_hash)+CHECK2. status는 기존 chk_ke_status(ai_draft/approved/hold/discarded) 재사용.
- ⏳ **다음**: 검수팀 **첫 제한 실적재**(공식소식지 5건=교보·iM·ABL생명·AIG손보·DB생명, dry_run=false, ai_draft까지만, AIA·NH·IBK·농협손보 제외, 자동approved 금지) 의뢰서 대기. **★셀결합 잠재버그**("147 148 149"→"147148149" 천단위 오결합) 충전 후 점검. cron·대량·자동approved 계속 금지.

## 영업노트 (CRM 주춧돌) — DB 토대 완성
- **세 층 DB 생성(대표 Dashboard 실행)**: `sales_customers`(고객원장) / `sales_consultations`(상담 1:N·타임라인) / `sales_status_history`(상태이력). RLS=owner 본인만(개인 CRM, 어드민도 내용 미열람). 미래자리 `profile`/`health` jsonb(Phase 2 AI/수동). 소프트삭제 `deleted_at`.
- **엑셀(`upgrade_20260521/상담리스트_정리.xlsx`) 파싱**: 정리시트 2,621 상담행 / 고유고객 ≈2,353(전화기준) / 전화없음 230. 칼럼=번호·상담일자·이름·생년월일·성별·나이·연락처·상담채널·상담내용·추가메모·원본행·원문. ★기획팀장 "117"은 미리보기 일부 오인(실제 20배+).
- **owner = 임태성 실장** (bylts@naver.com, `98c5f4f9-10c1-4ee1-a656-5c2ca63239fd`). 영업노트=임태성 계정 전용(대표 본인 영업), 어드민(창조주)엔 미생성.
- **방침**: 전체 2,558명 통째 이식 + 본인 검수·삭제. ★목록 맨앞 체크박스+선택 일괄삭제 / ★AI정리=버튼(일괄자동 X)·UI는 Phase1·동작은 외부전송게이트 Phase2 / 자동차단 폐기(상담에 주민·계좌 필요)·원문보존(주민번호 평문 그대로)·채굴 절대제외·삭제(자동파기)는 추후.
- `_sales_import_*.sql` gitignore 보호(개인정보 커밋 방지).
- ⏳ **다음**: import SQL 생성(고객 dedup+상담 분리+원문보존, owner 박음, 분할) → 대표 Dashboard 실행 → **화면**(목록 체크·일괄삭제·검색·정렬 + 카드 폼·빠른메모·타임라인·AI버튼 자리 + 수동삭제). Phase1=AI 없이(외부전송0). 큰 작업이라 충전 후 한 번에.

## 디자인 (Claude Design 연동 — 맛보기)
- **DesignSync 도구**로 claude.ai/design 양방향 연동 확인(임태성 "Design System" 프로젝트). 스페이스 설명서 시각화 시안 `design-preview/space-guide.html` 업로드.
- 검수팀이 Claude Design AI로 시안 생성(품질 양호, `upgrade_20260521/mannual01.png`). 실제 app.html 반영은 나중(SPACE IA 트랙).

## 신설 메모리
- `project_onesecond_ux_core_problem` — 위계없는 과밀=글자많다 근본. 해결3원칙(덜보여주기·핵심행동 시각압도·모션은 안내). 화면결정 상위기준.
- `project_sales_note_crm_foundation` — 영업노트=CRM 주춧돌. UI단순/데이터 큰그림. 세층·병력민감정보·임태성 베타.
- `project_product_versioning_supersede_track` — 상품 버전관리·대체관계(판매종료·개정 자동반영 전제).

## 다음 세션 1순위 (충전 후)
1. **영업노트 import SQL → 화면** (오늘 사무실 목적, 충전 후 한 번에 — DB·파싱·owner·보호 다 됨, import부터 바로).
2. **채굴 첫 제한 실적재** 결과 확인 + **셀결합 점검**.

## 미머지/대기
- PR #802 (AI 한글 파일명 '정리 중' 시그널, 6/17부터 미머지) — 이월.
- mine-batch 첫 실적재(dry_run=false) = 검수팀 손, 결재 후.
