---
세션 종료 시각: 2026-06-28 13:21 KST
주요 마감 작업: 회사 허브 BMI 본문 누락 수정(#999) + 결제·본인인증 계약 현황 박제(Gmail 증거) + 배포 게이트 전용 정책(Netlify 자동빌드 중단)
생성 도구: Claude Code /session-end
---

## 세션 인계 노트 — 2026-06-28 오후 (총괄팀장 Code 단독)

별도 AI 창 없이 총괄팀장(Code)이 직접 진행. 신버전 `pdnwgzneooyygfejrvbg`. main 직접 push=docs/세션노트만. 제품 코드 머지=PR(#999).

### 오늘 마감

1. **결제·본인인증 계약 현황 박제 (Gmail 실제 메일 증거 조사)** — 대표 기억 의존 해소 요청. MCP Gmail로 실제 메일 전수 확인.
   - **가입·결제 = KG이니시스**(㈜케이지이니시스, **포트원 경유**·직접계약 아님). 정기결제(빌링)만·일반결제 철회. 전자서명 완료(5/29, 계약번호 8003172596)·실운영모드·**카드사 심사 통과**. **보증보험료 15,000원**(SGI서울보증·케이스마트인슈 대리점) **납부 완료**(대표 확인). NHN KCP=계약지연·90일 자동철회(안 쓰면 방치 무방).
   - **문자 본인인증 = 다날(Danal)**(포트원 경유). 신청만 접수(5/04)·이후 비용/계약 메일 0건·운영 채널키 미설정(placeholder)이라 저장소에 회사명 없었음.
   - **현재 결제 홀딩 진짜 사유 = V023**("신용카드 본인인증은 계약되지 않은 인증방법")=KG이니시스 빌링키 발급용 카드 본인인증 옵션 미계약. **다날 비용 아님**. → 연동 작업 시 콘솔에서 확인(대표 판단·총괄팀장 동의). 비용 단정 금지=연동 시점 콘솔 실측.
   - **판정: 당장 결제할 항목 0건.** 진실원천 `docs/work_orders/2026-06-28_payment_identity_contract_status.md`(커밋 `1c80c41`). 신설 메모리 `payment_identity_contract_status`.

2. **배포 정책 전환 = 당분간 임태성 게이트 전용 반영 (대표 결재)** — 사이트 수정 중이라 전체 일괄배포 의미 없음.
   - **실측(오해 정정):** 일반 사용자 라이브 = **GitHub Pages**(`cname: onesecond.solutions`·source main·status built). **Netlify는 프로덕션 아님** = `splendorous-bavarois-818e3a` 미러 + PR Deploy Preview(저장소에 netlify.toml 등 설정 0건·콘솔에서만 관리). 심야 일괄배포 없음(workflows=issues-daily RSS뿐). → "Netlify 끊기"는 일반 사용자 반영과 무관.
   - **정책:** ①신규 작업 100% 임태성 user_id `98c5f4f9` 게이트 안에서만 main 반영 ②게이트 밖 일반노출 머지=대표 확인·승인 후만 ③GitHub Pages 자동배포 유지(게이트로 일반 사용자 격리) ④Netlify 콘솔 미러/프리뷰 일시중단.
   - **Netlify 자동빌드 중단 완료**(검수팀 Chrome 실행): Build settings→Configure→**Build status "Stopped builds"**→Save. 화면 `Build status: ⚠️ Stopped`. **복구=Active builds 라디오**(삭제·설정변경 0). onesecond.solutions(GitHub Pages) 무영향.
   - 진실원천 `docs/ops/2026-06-28_deploy_gate_only_policy.md`(커밋 `48f0f8c`·`5f9a3e1`) + CLAUDE.md 배포 절대규칙에 1줄(`2bc69ae`) + 신설 메모리 `deploy_gate_only_policy`.

3. **회사 허브 BMI 등 본문 박스 누락 해소 (#999 머지·라이브)** — "삼성화재 29 초과 방진"이 안 나오던 건.
   - **원인 확정(quick_contents content_html 구조 SELECT로):** 카드 구조 = `카드 > [헤더(flex)=회사명+우측라벨, 본문박스(방진)]`. `_hubPick`이 회사명 요소의 **부모 한 단계(헤더)** innerHTML만 반환 → 헤더 형제인 **본문 박스("방진" 등) 통째 누락**. 삼성화재뿐 아니라 DB손보·KB·농협 등 **전 회사 본문이 잘리고 있었음**.
   - **수정:** `_hubPick`이 회사명에서 위로 올라가 **grid 컨테이너 직전(=회사 카드 전체)** 반환. grid 없으면 기존 동작 폴백 → 연락처/결제/원전산 탭 회귀 0. node --check 통과. 게이트 안이라 일반 사용자 무영향. PR #999(`e02c152`)·머지 `d978aa1`.

4. **채굴 검수 대기 = 0건 확정** — DB 실측: `knowledge_entries` 914건 **전건 `approved`**(다른 상태 0). 6/13 PR #619 이후 변동 없음. 채굴 트랙 깨끗하게 마감(재검진 불필요). 살아있는 "검수 대기"는 채굴이 아니라 **상품 로드맵 씨드 트랙**(raw_seed)뿐.

### 보류/중단

5. **★삼성화재 상품 로드맵 트랙 = 대표 "이상하게 흘러간다" → 여기까지 중단** (구현·코드·DB·브랜치 **0건**·잔재 없음).
   - 진행한 것: PDF 3건 확인(GA소식지/1매형/순통치 판매비법서). **3건 모두 한글 텍스트 추출 불가**(커스텀 폰트 인코딩·ToUnicode 없음, pdftotext가 숫자·영문만 추출). 로컬에 PDF→이미지/OCR 도구 없음(gs·pdftoppm·ImageMagick 부재·GEMINI_API_KEY 없음). GA소식지 16~17p에 "6월 Line-up"·납입면제 표 **존재 확인**(상품명 한글만 못 읽음).
   - 작업지시서1(데이터·DB 1~7단계·자동화·전사 표준·중립·공급/수요 원장 분리·역할분담: 기획팀장 PDF 판독→정형데이터, **Code는 OCR 안 함·구조화 데이터 받아 적재+화면**) + 작업지시서2(화면 표준·셸 매핑·재사용 컴포넌트·라인업↔납면 독립·중립 불변식 구조로 박기·`[준비 중]` 자리·거버넌스: `docs/specs/v2_insurer_hub_layout_v1.md`·브랜치 `feat/insurer-hub-samsung-v1`) **이해 보고 2회** 완료.
   - 목업 3장(`upgrade_20260521/search_result10·11·12.png`) 확인: 회사 허브(라인업·납면 [준비 중] 줄 추가)/상품 라인업 표/상품별 납입면제 표.
   - **미해결 질문 게이트 1건**(착수 시): 허브 상단 표현 = (A) 현 아코디언+라인업/납면 줄만 추가(목업) vs (B) §3-1 glance-row+행 목록 재구성. 대표 결정 대기.
   - PDF 3건 = `upgrade_20260521/삼성화재 202606/`. 자료 전부 로컬/대화 보존.
   - **재개 여부 미정**(대표 방향 재정비 대기). 다음 세션이 임의로 재개·확대하지 말 것.

### 다음 세션 우선순위 (대표 지시 대기)

1. **상품 로드맵 트랙 = 대표 방향 재정비 후에만 재개.** 임의 진행 금지(대표 "이상하게 흘러간다" 중단 존중).
2. **외부 백업** — 메모리 지시(토/일 재질문). 6/28 토요일이었으나 결제·배포·BMI 작업에 묻힘 → 다음 세션 START [6.Backlog]에서 한 줄 질문.
3. 회사 허브 잔여(상품 라인업 고해상도·나머지 회사 URL 확대·로고 33사 위키미디어 SVG·콜센터 accent·동결영역) — 단 상품 로드맵과 얽혀 보류 가능.
4. 배포: **게이트 전용 정책 가동 중** — 신규 작업 임태성 게이트 안에서만, 게이트 밖 노출은 대표 승인 후.

### 교훈

- **"결제 마감/회사명"류 = 저장소 추정 말고 대표 Gmail 실제 메일 증거로**(MCP Gmail 검색 가능). 콘솔 실측값은 대표 회신(Code 콘솔 접근 불가).
- **"Netlify 끊기" 전 실측** = 라이브는 GitHub Pages·Netlify는 미러. 콘솔 접근 못 하면 "끊었다" 거짓 보고 금지 → 운영원칙 박제+대표 콘솔 토글로 분리.
- `_hubPick` 같은 **추출 버그 = 데이터 구조(SELECT) 확인 후 수정**(추측 패치 회피). 부모 1단계만 잡으면 형제 본문 누락.
- **큰 작업지시서 = 착수 전 이해 보고로 확인. 대표 "이상하게 흘러간다" 신호 시 즉시 중단**(부풀리기·추측확대 회피, 메모리 `feedback_small_request_minimal_change`·작업지시서 §17 정합).

### 신설 메모리

- `payment_identity_contract_status` — 결제(KG이니시스)·본인인증(다날) 현황·당장 결제 0건·V023
- `deploy_gate_only_policy` — 배포 게이트 전용·Netlify 중단·라이브=GitHub Pages
