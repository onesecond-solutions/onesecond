---
세션 종료 시각: 2026-06-03 00:16 KST
주요 마감 작업: 글쓰기 저장 RLS 연결 + 창 이탈 버그 전수 + 보험Q&A 필수입력 + 캘린더 트랙 풀 구축(3영역·역할별 공유·시간순·오늘의할일) + 검색 stub 해제 + 데이터 현황 파악 (PR #303~325)
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

세션: 2026-06-02 저녁 ~ 06-03 심야 (집 PC, Code 단독, 장시간 마라톤). 직전 1844 세션의 1순위였던 "글쓰기 저장 연결"로 진입 → 라이브 검수 연쇄 + **캘린더 트랙 전면 구축** + 검색 트랙 준비까지. PR #303~325 머지(전부 라이브) + DB RLS/ALTER 다수.

## 완료 (라이브 반영)

### 1. 글쓰기 단체방·보험Q&A 실저장 + RLS (#303·#306 + DB 3정책)
- saveMyspaceWrite group/qna → 실제 INSERT(team_notices / posts qna). 추측 INSERT 금지 준수(posts.created_by 없음 라이브 확정, 검증패턴 컬럼만, 전용컬럼 없는 폼값은 content 조합).
- **ga_member 스크립트 저장 RLS 위반 발견** → pg_policies 실측: scripts INSERT정책 admin만/team_notices 매니저만/posts qna admin·seed만. **팀장님 결정=설계사 작성 허용** → 신버전 RLS 3정책 실행(additive·본인명의): `scripts_insert_own` / `team_notices_insert_member`(team_internal+본인팀) / `posts_insert_qna_member`(qna+auth.uid=author+branch_id=my_branch). qna는 branch_id 세팅(#306).
- **os_user.id=auth.uid() 확인**(db.js mergeUserProfile은 role/name만 덧붙임 → owner_id 값 정상=정책 누락이 원인이었음).

### 2. 창 이탈(드래그→모달 닫힘) 버그 전수 (#304·#305)
- 입력칸 mousedown→배경 mouseup이 배경 div에 click 오발동. `_ovDown`/`_ovClick` 가드(mousedown·click 모두 배경=진짜 배경클릭만 닫음).
- #304 인라인 7개 + #305 다른방식 3곳(mc-overlay·quickOverlay·스크립트카드 outside-click).

### 3. 보험Q&A 필수 입력 + 기타 옵션 (#307·#308)
- 빈 공란 저장 차단: **3폼(인수/상품/기타) 전 칸 필수**(인수 '기타 적고 싶은 내용'만 선택), 인수 제목 자동생성 폐기, 키워드 필수.
- 기타 폼 '확인 범위' = 팀 내부/지점/전체 공유 3개만(특정보험사·운영진만 삭제), '기본값=전체공유' 문구 삭제.

### 4. 홈 인사말 (#309)
- "안녕하세요~ {이름}님!" → **"{이름} {직함}님, 안녕하세요~"**(roleLabel). 이름=직함 중복 시 직함 생략.

### 5. 캘린더 트랙 전면 구축 (#310~#324 + DB)
- **DB**: `calendar_events` 신설(신버전) + RLS 4정책(select/insert/update/delete) + `event_time` 컬럼 ALTER. **⚠️ 초기 CREATE를 구버전에서 실행한 사고→to_regclass=null로 발견→신버전 재실행으로 해결**(CLAUDE.md 교훈 재확인).
- **풀뷰**(#310~312): MY SPACE 오피스 칩 줄에 '캘린더' 추가(한 줄). 게시판 split = 좌 월별 누적(최신월 상단·sticky 헤더) / 우 캘린더 그리드. 출처 4종(insurer/manager/branch/personal) 색+SVG아이콘+라벨(이모지X·접근성).
- **3영역 복사**(#315): prefix 일반화 한 벌(mys/mr/iv) — MY SPACE·매니저룸·보험사 자료실 오피스 동시. `CAL_HOST`/`_calSt[p]`. (복제 대신 일반화=자체완결 위젯이라 안전·간결)
- **일요일·휴무 빨강**(#316): 일요일+CAL_HOLIDAYS(2026 한국 공휴일) var(--err).
- **칸에 일정 제목**(#317): 점→제목 칩. **높이 채움**(#320, cal-cells grid-auto-rows 1fr) + **폰트 확대**(#321).
- **본인 일정 수정·삭제**(#319): 출처 무관 author=본인이면 [수정][삭제]. openCalEdit/updateCalEvent.
- **역할별 공개 범위**(#318): 일정 추가 모달에 공개범위 선택 — 실장=팀공유/지점장=지점/보험사=담당지점(insurer_employee_branches fetch)/설계사=개인만. saveCalEvent가 scope→source_type/audience_target/team_id/branch_id 매핑. RLS select로 같은 팀·지점 팀장 캘린더 자동 반영.
- **시간순**(#322): event_time 입력칸 + (날짜,시간) 정렬(좌목록·우그리드·3일요약). 시간 없는 일정은 그날 맨 아래. 시간 HH:MM 표시.
- **오늘의 할 일 = 가로 4카드 스크립트카드 스타일**(#313·#314 정정): 첫 카드=캘린더 전일·당일·내일 3일 요약. MY SPACE + **매니저룸(#323)** + **보험사 자료실 신설 탭(#324, showView 진입=ivTab(null,'todo'))** 3영역 적용. _goMys/Mr/VaultCalendar 딥링크.

### 6. 검색 트랙 준비 (PR #325 미머지 — 결정 대기)
- A영역 검색창이 alert stub('다음 자리에서 정합 예정')로 막혀 있던 것 확인(2026-05-28 v1→v2 전환 stub). **PR #325 = openSearch stub 해제**(잠든 모달=출처별 그룹 결과 활성). 단 데이터가 mock 8건이라 머지 보류 중.
- **데이터 현황 실측**: posts insurer=4 / posts qna=465 / team_notices=24 / scripts=61 / library=4 / calendar_events=10. **검색용 충분.**
- **🚨 핵심 발견**: qna 465건 중 **462건이 소식지**(board_type='qna' AND title ILIKE '%소식지%'). 즉 진짜 보험Q&A 질문은 ~3건. newsletters 전용 테이블=0(미구축). 소식지는 posts qna에 제목 '소식지'로 섞여 저장됨(그래서 Q&A 목록이 NOT ILIKE 소식지로 462건 제외).

## 미완 / 다음 (1순위부터)
1. **검색 실데이터 연결 (검색 트랙 본진)** — 데이터 충분 확인됨. mock 건너뛰고 실데이터 검색 권장. 검색 대상=posts(qna+insurer)·scripts·team_notices·library·calendar_events. 출처 그룹=현재 영역(보험Q&A/자료실/현장의소리/스크립트/MY SPACE자료/캘린더/**소식지**). db.fetch=RLS 자동 필터(본인 볼 것만 검색). **소식지는 title ILIKE '%소식지%'로 별 그룹 분리**(462건). PR #325 처리: (A)닫고 실데이터 한방에(추천) (B)지금 머지(mock 노출).
2. **소식지 분류 정리 (선택·장기)** — qna에 섞인 462건을 전용 board_type 또는 newsletters 테이블로(Phase 2). 검색은 지금 title ILIKE로도 가능.
3. **캘린더 라이브 검수** — 실장→팀 공유가 같은 팀 팀장 캘린더 반영(전제: 팀장 계정 team_id/branch_id 매핑) / 보험사 실계정 노출 범위 / 시간순.
4. **이월**: 매니저룸 실장/지점장 role별 구조 조정.

## 격차 / 학습
- **구버전 사고 재발**: calendar_events CREATE를 구버전에서 실행("Success"인데 신버전 to_regclass=null) → CLAUDE.md "테이블 있어야 하는데 없으면 90% 구버전" 교훈 재확인. DB 첫 질문(프로젝트 확인)으로 복구.
- **추측 진입 1회 정정**: 검색창이 "mock 동작한다"고 보고했으나 실제는 stub(alert) → 코드 재확인 후 정정. 라이브 동작 단정 전 실제 코드/화면 확인 필요.
- **prefix 일반화 vs 복제**: 캘린더는 자체완결 위젯이라 일반화(한 벌, 3영역) 선택 — 큰 view 로더의 복제 원칙과 구분.
- **RLS=검증 우선**: 권한 격차를 pg_policies 실측으로 진단, 권한 확대는 팀장님 결정(설계사 작성 허용) 후 additive 정책만.
- 매 PR 인라인 스크립트 문법 체크(node vm.Script) 통과 후 머지 — 회귀 0.
