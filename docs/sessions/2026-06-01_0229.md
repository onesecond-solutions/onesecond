---
세션 종료 시각: 2026-06-01 02:29 KST
주요 마감 작업: 보험사 자료실 이미지 카드 만들기(Gemini 핵심 추출) 완성·라이브 + 보험Q&A 흐름 MY SPACE 중심 재정의·문서화
생성 도구: Claude Code /session-end (Code 단독 세션 — Code가 인계 노트 작성)
---

# 2026-06-01 세션 인계 노트 (집 PC, Code 단독, 장시간)

> 본 세션은 Claude AI 채팅 노트 없이 **Code가 전 작업을 직접 수행**. Code가 전체 컨텍스트로 본 노트 작성.
> 카드 만들기 트랙은 **라이브 반영·검증 완료**. 보험Q&A는 **방향 확정 + 문서화**(구현은 다음).

## 🎯 큰 그림 — 오늘 확립된 본질

1. **보험사 자료실 이미지 "카드 만들기" = Gemini 핵심 추출** (라이브 완성). 자료실 첨부 이미지 → 딸깍 → 카드 만들기 → Gemini가 핵심만 뽑아 표 카드. 새 글·이미지면 카테고리 무관 **자동** 작동(이미지 한정).
2. **보험Q&A 흐름 = MY SPACE 중심 동시-노출 모델** (문서 확정). 설계사는 게시판을 찾아가지 않고 **MY SPACE("내 책상")에서 질문하고 답만 받는다.** 질문/답변은 `posts`에 1건 저장 → 보험Q&A·보험사 자료실·MY SPACE가 **같은 데이터를 각자 필터로 동시에** 봄(복사·이동 X). → `docs/product/qna_myspace_flow_v1.md`.
3. **카드 만들기 2종 구분**: 자료실 딸깍(이미지→Gemini, 이미지 전용) / Quick 메뉴(이미지·TXT·PDF 직접 제작, Gemini 안 씀). → `.claude/skills/ddalggak/SKILL.md` 현행 정리.

## ✅ 오늘 마감 (라이브)

- **PR #233** 자료실 라이트박스 딸깍 "카드 만들기" → `gemini-card` Edge Function 연결. 하드코딩 데모 카드(mock) + index 초과 무반응 에러 폐기.
- **Edge Function `gemini-card` 신설·배포**(Supabase). 입력 이미지 URL → Gemini → `{brand,name,rows[]}` JSON. 키 = secret `GEMINI_API_KEY`(팀장님이 Google AI Studio에서 신규 발급, 앱 코드/SQL에 평문 0). 모델 = ListModels 자동선택(고정 금지, 404 회피).
- **PR #234** 모델명 고정(gemini-2.0-flash 404) → ListModels 동적 선택.
- **PR #235** JWT 만료 401 자동 갱신 — 홈 `library`/`script_usage` + 카드 호출이 raw fetch라 자동갱신 미적용이던 것 → `db.fetch`로 통일(크롬 검증자 진단). + 카드 하단 **작성자 이름·전화**(os_user) + 면책 "※ 상담 참고용"(원세컨드 노출 제거) + brand 빈값 시 줄 생략.
- **PR #236** 딸깍 메뉴 **4줄**(다운로드 저장/MY SPACE 저장/복사/카드 만들기) + 부연설명 제거 + 카드 헤더 파일명(KakaoTalk…) 제거 + 변환 중 **물결 애니메이션**.
- **PR #237** "다운로드 저장" = **다른 이름으로 저장**(showSaveFilePicker 위치 선택 창, 미지원 브라우저 fallback). 라벨은 "다운로드 저장" 유지(팀장님 지시).
- **PR #238** `ddalggak` 스킬 문서 현행 정리(카드 2종 / 라이트박스 4줄 / Gemini / 적용 범위 / 회귀 신호).
- **PR #241** 보험Q&A 동시-노출 흐름 설계 문서(`docs/product/qna_myspace_flow_v1.md`).

## ⛔ revert (라이브 무사)

- **PR #239** 보험Q&A 질문 등록 실저장(`submitAsk` posts INSERT) → 머지 후 **라이브 먹통 보고** → **즉시 revert(PR #240)**로 복원.
  - **먹통 원인은 코드 아님** — `submitAsk` 문법 정상(node --check 통과). **팀장님 본인 실수로 확인됨.** (revert는 안전 조치, 손해 0.)
  - 더 본질적으로: 질문 입구를 **MY SPACE로 하기로** 했고 **현장의 소리 "질문하기" 폼은 사용 안 함** → #239(현장의 소리 질문하기 실데이터화)는 그 원칙과 충돌해 **revert가 맞는 방향**.

## ⏳ 미완 / 다음 단계

- **🔜 다음 1순위 = MY SPACE 질문 입구** — 인수/상품 폼(기존 디테일 그대로, 위치만 MY SPACE) → `posts` INSERT + **내 질문/답변 목록**(질문자 뷰). 이게 선행돼야 아래가 얹힘.
- **보험사 자료실 "오늘의 할 일" 카운트** — 인수/상품 × 신규/미처리/답변완료/오늘 누적. (질문 데이터가 쌓여야 진짜 숫자.)
- **우리 회사 필터** — `insurer_target IN (전체, 우리보종(손보/생보)전체, [회사지정 & 대상=우리])`.
- **답변 흐름** — 보험사 직원이 `parent_post_id`로 답변 INSERT → 첫 질문자 MY SPACE에 도착.
- **🧹 초기화 트랙 (팀장님 결정, 미착수)** — 스마트게시판 PDF 소식지 노출 제거→검색 자료로만 / 각 메뉴 mock 제거→빈 상태 + "사용방법" 안내 1개 / 4팀 전용 mock 정리→신규 사용자와 동등(clean start). **빼는 작업이라 유한.** 머리 맑을 때 "지울 것/남길 것" 목록 먼저 만들고 진입 권장.
- MY SPACE 이미지 미리보기+딸깍 (보류 — `library.image_url` 필드 존재, 현재 `doSave`는 mock).

## 🗂️ 데이터/인프라 실측 (재확인용)

- `posts` 컬럼 신버전(`pdnwgzneooyygfejrvbg`) REST 검증 완료(HTTP 200): `board_type/question_type/insurer_target/patient_age/patient_gender/disease_name/diagnosis_timing/current_status/product_category/parent_post_id/status/branch_id/title/content/attachments/created_at`. → **보험Q&A 실데이터화에 DB 신규 거의 0.**
- 답변 = `parent_post_id`(posts 컬럼). 보험사 범위 = `insurer_target`. 인수/상품 = `question_type`.
- 현장의 소리 mock 현황: **보험Q&A 탭만 정적 mock 10건**(`showPost(0)`~`showPost(9)`). 단체방/지점게시판/스마트게시판은 이미 실데이터.

## 🚨 주의사항 / 격차 학습

- **추측 진입 격차(반복)**: #239를 "현장의 소리 질문하기 폐기" 원칙 확인 없이 진입 → revert. START PROTOCOL(전략/decisions 통째 점검) 정신 미준수. **다음엔 큰 그림(결정·전략) 먼저 확인 후 진입.**
- **DB 실측 선행은 잘 지킴**: posts 컬럼 REST 검증 후 진입.
- **빌드 직후 검수 주의**: 머지 직후 먹통 보고 → revert(코드 무죄, 팀장님 실수). 빌드 타이밍/캐시 가능성도 있음. 강력 새로고침 + 빌드 완료 확인 표준.
- **AI 동조(아첨) 경향 자각**: 팀장님이 "AI들이 다 이런 반응이지?" 지적 → 칭찬이 아니라 **근거**로 판단하도록 안내. 결정은 팀장님 감각 + 실사용으로.
- **팀장님 번아웃 신호**: "일이 해도해도 끝이 없네". 무리 금지. 초기화는 "비우는 작업"이라 오히려 일을 줄이는 방향임을 공유함.
- **Gemini 키** = Google AI Studio 신규 발급(과거 5/22 Vision 키 사고와 **별개 키**). 같은 프로젝트에 다른 Google API(Vision 등) 켜면 같은 키로 쓸 수 있으나, 카드엔 Gemini만으로 충분 + 보안상 불필요한 API는 끄는 게 안전.

## 🤝 협업 메모

- 크롬 검증자(Claude in Chrome)가 JWT 만료(PGRST303) 진단 → Code가 raw fetch→db.fetch 정정(#235). 3-AI 협업 정상 작동.
