---
세션 종료 시각: 2026-07-17 14:20 KST
주요 마감 작업: X-FILE 신설 트랙 완주(붉은 카드→빈 페이지→"내 보험 어때?" 자가검진표→카드 허브→카드 8종, PR #1305~#1310) + 보장분석 560px 폭·세로 강제 해제(#1311) + AGENTS.md 코덱스 일반 구현 재편(ba46f53) + 코상무(Codex) 위임 파일럿 첫 가동.
생성 도구: 총괄팀장 Code (대표 세션, 데스크톱 앱). 신버전 pdnwgzneooyygfejrvbg.
---

> **세션 종료 시각:** 2026-07-17 오후 (~14:20 KST) — 같은 날 02:42 노트에서 이어짐(대표가 세션 계속)
> **주요 마감:** ① **X-FILE 트랙 신설·완주**(홈 붉은 카드 → 빈 페이지 → 자가검진표 → 카드 허브 → 카드 8종, 라이브) ② **레이아웃 강제 해제**(X-FILE·보장분석 560px 폭·세로 1열) ③ **AGENTS.md 재편 — 코덱스=일반 구현** ④ **코상무 위임 확정 + 첫 파일럿 가동**
> **생성 도구:** 총괄팀장 Code (대표 세션, 데스크톱 앱).

# 세션 인계 노트 — 2026-07-17 오후 (1420)

## ⭐ 성격
02:42 종료 후 대표가 같은 창에서 계속 진행. **X-FILE 트랙을 0에서 라이브까지 완주**(PR 6건) → 대표 지적으로 **레이아웃 강제 해제**(2건) → **AI팀 체제 개편**(코덱스=일반 구현) → **코상무 파일럿 첫 가동**. 총괄 실수 3건 박제(아래 교훈).

## ✅ 완료 — X-FILE 트랙 (전부 라이브, 임태성 게이트)
| PR | main | 내용 |
|---|---|---|
| #1305 | `8dfac12` | 홈 **붉은 X-FILE 카드**(신설 토큰 `--t-xfile` #DC2626/다크 #EF4444, `--err` 미참조) + 빈 페이지 `#v-xfile` + 게이트 `_canSeeXfile` 신설(`_canSeeCoverage`와 동일 조건·독립 함수) |
| #1306 | `981f18a` | **"내 보험 어때요?" 자가 검진표** — `js/xfile.js` 신설. 4항목(의료실비·암·뇌/심장·수술비)×3문항 → 4축 판정 → 상담 CTA(전화·카톡·문자). 문구는 **bojang.js 자산에서 가져옴(창작 0)**. DB/fetch 0, 상태=메모리만 |
| #1307 | `7ba1ef2` | **X-FILE = 카드 허브로** — `CARDS` 배열 신설, 진입=허브 / 카드 클릭=검진표. 허브=`‹ 홈으로` / 서브=`‹ 목록으로` |
| #1308 | `340b1fa` | 네이밍 **"내 보험 어때요?" → "내 보험 어때?"**(표시 3·주석 4, 잔여 0) |
| #1309 | `fbda1b0` | **카드 7종 추가**(의료실비·암·뇌/심장·수술비·종합보험·보장분석·간병인) + 각 빈 페이지. `renderBlank()` 공용 1개+팩토리(중복 0). **보장분석 step key=`analysis`**(앱 `#v-bojang`과 별개 — 혼동 방지) |
| #1310 | `76bab44` | 허브 카드 **긴 세로 막대 → 컴팩트 타일 그리드**(`repeat(auto-fit,minmax(224px,1fr))`, 열 수 미고정) + **560px 폭 해제 → 860px**(홈 정합) |

## ✅ 완료 — 보장분석 레이아웃 해제
- **#1311 `67d3873`** — `.bj-card`/`.bj-topbar` **560px→860px**, 세로 강제 6곳 중 **4곳 품**(`.bj-list`·`.bj-blocks`·`.bj-cmp`·`.bj-axes` = 순서 없는 병렬) / **2곳 둠**(`.bj-acc` 아코디언=펼치면 높이 변동·흐름 깨짐 → **대표 "아코디언은 유지" 확정** / `.bj-btns`=CTA 전체폭 장치). **열 수 하드코딩 0**(`minmax(min(Npx,100%),1fr)`), `@media` 추가 0. **모바일 회귀 0**(origin/main A/B 픽셀 대조).

## ✅ 완료 — AI팀 체제 개편 (`ba46f53`)
- **대표 확정: 일반 구현 = 코덱스(→ 호칭 "코상무") / 복합 조사·운영 장애 대응 = Claude Code.**
- **`AGENTS.md` 재작성** — 코덱스 경계를 **"데이터 마이닝·PDF OCR·DB 적재 전담"에서 "일반 구현"으로 이동**(제품 코드 수정 허용). **데이터 마이닝 트랙 전부 폐기**(대표: "당시 필요해서 해보려던 일, 폐기하라" — 실제 가동된 적 없음·코덱스 PR 0건). '하루 2회 결재' 리듬도 폐기(대량 채굴 전제).
- **유지(대표 지시):** 저장소·브랜치 규칙(main 직접 push 금지·feature→PR)·배포 절차(총괄 검수→대표 승인→머지)·임태성 게이트·DB는 CI 채널.
- **신규 박음:** v2 토큰(하드코딩·직각 금지)·CSS 스코프 격리·**폭/열 수 임의 고정 금지**(오늘 사고를 규칙화)·라이브 용어·증거 있는 보고·상상해서 채우지 말 것.
- `docs/ops/codex_setup_guide.md` **일반 구현용 재작성**(Secrets 미투입=DB 무접촉이라 키 없는 게 사고 원천 차단 / Setup script 불필요=빌드 없는 정적 사이트 / 파일럿 판정 기준 5개). `codex_data_map.md` → `docs/archive/_deprecated/` + 폐기 배너.

## 🆕 코상무(Codex) 파일럿 첫 가동 — 대표 직접 지시
- 대표가 코덱스에 **직접 지시**해 `pages/insurance-self-check.html`(786줄, 독립 페이지) + `assets/insurance-self-check-hero.webp` 제작 → **X-FILE 허브에 "내 보험 어때? v2.0" 카드로 추가**(iframe `?embed=1` 임베드, `renderCheckV2`).
- **main 커밋 5건**(`1cd4eaa`·`52e3f21`·`59c2e44`·`774c1c7`·`0a22c29`), 파일 = `pages/insurance-self-check.html`·`js/xfile.js`·`app.html`·`assets/`.
- **총괄 검수 결과:** ✅ **회귀 0** — 총괄의 X-FILE 작업(허브 그리드·카드 8종·560px 해제) 전부 보존. **기존 `check` 카드를 부수지 않고 `check-v2`를 나란히 추가**(기존 자산 존중). 게이트 있음.
- **main 직접 push 5건 = 대표 지시.** 대표 원문: *"내가 지시한 거야."* → **사고 아님. 절차 위반 아님.** `AGENTS.md`의 "main 직접 push 금지·산출=PR"은 총괄이 쓴 규칙이고, **대표 지시가 그 상위**다. 다음 세션은 이 커밋들을 사고로 오독하지 말 것.
- 대표 원문: *"코덱스를 활용해서 몇 가지 테스트 작업을 했고, 코상무로 위임하기로 했다."*
- ⚠️ **미확정:** 코상무 본가동 시 PR 흐름을 쓸지 main 직접으로 갈지는 **대표가 정한다**(총괄이 판정할 사안 아님). AGENTS.md 문구와 실제 운영이 다르면 대표 지시에 맞춰 문서를 고친다.

## ⏰ 다음
1. **코상무 위임 본가동** — 대표가 코덱스 화면에서 GitHub 연결·환경 설정(Repository=`onesecond-solutions/onesecond`/main, **Setup script 비움**, **Secrets 넣지 않음**). 총괄이 작업지시서 제공. → `docs/ops/codex_setup_guide.md`
   - **PR 흐름 vs main 직접 = 대표가 정한다.** 첫 파일럿은 **대표 지시로 main 직접 push**로 진행했다(사고 아님). 총괄이 이를 위반으로 판정하지 말 것. 대표가 정한 방식에 맞춰 AGENTS.md·setup_guide 문구를 고치면 된다.
   - 판정 기준(범위 준수·게이트 준수·증거 있는 보고·회귀율)은 참고용. **절차 항목은 대표 결정에 종속.**
2. **X-FILE 카드 7종 = 빈 페이지 상태** — 대표가 채울 자리. 내용 정해지면 카드별로 착수(원장 없는 화면=하드코딩 금지 → 데이터 필요 시 원장 먼저).
3. **`insurance-self-check.html` 미완 2건** — ⓐ **상담 신청이 `fake-submit`**(버튼 클래스 그대로, 입력 3란이 아무 데도 안 감) → 대표 핵심 요구("고객 결과 확인 → 나한테 상담 신청")는 **미충족** ⓑ **`noindex` 없음** → 공개 시 구글 색인(보험 광고 규제 영역). **공개 시점 전 필수.**
4. **"내 보험 어때?" 네이밍·구글 플레이 출시 검토 = 미완**(대표가 웹 검색 중단). 재개 시 총괄 사전 경고 = 보험업법 모집·광고 규제 + 소속 GA 승인이 선행.
5. **v2 토큰 타이포 = 대표 방향 회신 대기**(ⓐ글자 크기 ⓑv2 2단계 적용 ⓒ구조 정리). → `docs/sessions/2026-07-17_0242.md`
6. **회사별 보험료 비교(B안) = 대표 월초 샘플 실물 1건 대기**(2026-07-17_0023 이월).
7. **worktree 9개 적체**(6~7월 것) — 디스크만 점유. 정리는 별도 지시 시.

## 교훈 (총괄 실수 박제)
- ★**세로형 강제 사고 = 총괄이 만든 것.** 총괄이 빌더에게 "보장분석 발송센터(`.bj-row`) 패턴 따라라"고 지시 → 발송센터가 **고객 폰 발송용(560px·세로)**이라 그대로 복제 → X-FILE까지 전염. 대표 지적: **"내가 강제시킨 적 없다고, 니가 맘대로 한 거지."** → **참조 대상을 맥락 없이 고르지 말 것.** 기준은 "대표가 데스크톱에서 쓰는 도구인가 / 고객이 폰으로 볼 자료인가".
- ★**"3열"은 규칙이 아니다.** 대표: *"가로 3카드는 정해진 게 아니야, 사이즈에 따라 배치되는 거라구. 내가 필요할 때만 몇 개로 하자 이런 식으로 하는 거라고."* → **열 수·폭은 화면이 정한다.** 하드코딩 금지. (참고: **홈은 오히려 `repeat(3,1fr)` 하드코딩** 상태 — 대표는 모르던 사실, 필요 시 별도 정리)
- ★**대표 "터미널에서는 사고방식이 유연했는데, 이 창으로 바꾸고 뭐가 달라진 거야."** 원인 = 총괄이 위임서마다 **"이 패턴 그대로 따라해라·새로 발명 금지"**를 반복해 박아 **빌더도 총괄도 생각을 멈춤.** 회귀를 막으려다 경직을 만듦. → **패턴 참조 ≠ 맥락 없는 복제.** 위임 시 "판단해서 맞게 하고 근거를 보고하라"로.
- ★**과잉 분석 2회 지적** — 대표: *"총괄은 너무 복잡하고 심오하게 사고하고 있어. 내 의도는 간단해."* → 메모리 `feedback-keep-it-simple-not-overthink` 신설. 가장 단순한 해석 먼저 채택.
- ★★**코상무 산출물을 대표 지시도 모르면서 "위반"으로 판정 — 한 세션에 2회 반복(가장 심각).**
  - **1회차:** 총괄이 자기가 쓴 `AGENTS.md`를 근거로 "승인하지 마십시오"까지 감. 대표: *"내가 지시한 걸 그대로 다 따른 거야."*
  - **2회차:** 교훈으로 박은 직후, 세션 종료 확인에서 **main 직접 push 5건을 "⚠️ 절차 어긋남"으로 기록**하고 "PR 흐름 지킬지 대표 판단 필요"까지 씀. 대표: *"내가 지시한 거야."* → 노트·INDEX 즉시 정정.
  - **원칙: 대표 지시 > 총괄이 쓴 문서(AGENTS.md·CLAUDE.md 포함).** 총괄이 만든 규칙을 대표에게 들이대지 말 것.
  - **행동 규칙:** 지시 내용을 모르는 산출물은 **사실만 보고하고 판정하지 않는다.** "위반·사고·어긋남" 같은 단어를 쓰기 전에 **"이게 대표 지시였을 가능성"을 먼저 확인**한다. 실제 위험(예: noindex·미작동 버튼)은 판정 없이 사실로만 알린다.
- ★**배경 라이트 전환의 함정**(오전 건) = 다크용 near-white 텍스트는 배경만 밝히면 사라짐 → 하드코딩색 전수 var화가 정답.

## 정합
CLAUDE.md START PROTOCOL(복명복창→승인·총괄 위임·증거 뒤 완료·게이트 전용 반영) · `AGENTS.md`(2026-07-17 재편) · `docs/ops/codex_setup_guide.md` · 메모리 `feedback-keep-it-simple-not-overthink`(신설)·`feedback-daepyo-only-direction-and-verdict`·`project-bojang-analysis-product-spec`. 직전 세션: `docs/sessions/2026-07-17_0242.md`.
