# 원세컨드 유기체 지도 (DRAFT) — 2026-05-31

> **상태:** DRAFT. 팀장님 눈 검수로 확정 전. (팀장님 = 비개발자, 화면 보며 조율 선호 → 이 문서는 "받아적기"이고, 화면 조율 결과로 갱신됨. [[user_work_style]])
> **목적:** 머릿속에서만 맴돌던 전체 구조를 밖으로 꺼내 박아둠 (세션 넘어가도 안 흩어지게).

## 한 줄 본질
**현장에서 떠든 것 → 정제돼 검색자료가 되고 → 1초에 찾아 꺼내 쓰고 → 다시 현장에서 쓰이며 새 데이터를 낳는다.** (순환 = 살아있음)

## 유기체 흐름

```
   매니저룸(실장 공지)┐   매니저룸(지점장 공지)┐   보험사 자료실(보험사)┐
                     ▼                        ▼                      ▼
          현장의 소리 = 글이 모이는 곳 (원천, 읽기)
            · 단체방   ← team_notices(scope=team_internal)   [실장이 씀]
            · 지점게시판 ← team_notices(scope=branch_internal) [지점장이 씀]
            · 보험 Q&A  ← qna board (묻고답하기)
            · 스마트게시판 ← 보험사 소식지/자료 (= 보험사 자료실 흐름)
                     │  옮겨와 정제 (어드민 허브게시판)
                     ▼
          ★ 원세컨화 (구조화 정규화) — auto_hub_v1 §5 structured_meta
                     │
                     ▼
          🔎 검색 자료 = 허브 (보험판 구글) ── Ctrl+K = 혈류
                     │  1초에 찾아서
            ┌────────┼────────┐
            ▼        ▼         ▼
        스크립트   MY SPACE   딸깍(복사/저장/카드)
                     │
                     ▼
                   홈 = 요약·심장박동 → 현장에서 쓰임 → 새 글 → 다시 현장의 소리로
```

## 쓰는 곳 / 모이는 곳 분리 (핵심 원칙)
- **쓰는 곳**: 매니저룸(실장·지점장) · MY SPACE(개인) · 스크립트(개인+α) · 보험사 자료실(보험사)
- **모이는 곳(읽기)**: 현장의 소리 — 직접 쓰는 공간 아님. 위에서 쓴 글이 모여 보임.

## 매니저룸 (= 팀원관리 재편)
- **실장(ga_manager)** = 단체방 공지 작성 / **지점장(ga_branch_manager)** = 지점 게시판 작성 / admin = 미리보기 전환
- nav 노출 = GA限(실장·지점장·admin)
- 공지 작성 폼 = **카테고리(7유형) + 제목 + 본문 + 첨부** (보험사 자료실 등록 폼 패턴 재사용)

## 카테고리 = 공지 7유형 (team_notices.notice_type, 확정 2026-05-31)
operation(운영) · urgent(긴급) · product(상품) · underwriting(인수) · education(교육) · event(일정) · etc(기타)
→ 작성 폼·단체방·지점게시판 칩 전부 이 7유형으로 통일. (옛 화면 칩 공지/전달사항/팀질문/자료공유 폐기)

## ★ 역할별 "오늘의 할 일" 엔진 (2026-05-31 팀장님 핵심 통찰 — 막힌 매듭)

> **본질:** 여러 영역에 필요한 "이 사람이 지금 챙길 것"은 **N개 기능이 아니라 하나의 패턴.** 롤마다 "할 일" 정의만 다를 뿐. → **단일 엔진 + 롤별 정의 교체.** (= 원세컨드의 개인화 진입 = 심장)
>
> **왜 못 만들었나:** 화면이 어려운 게 아니라, 각 롤의 "할 일 정의"가 머릿속에만 있어 매번 따로 그림. → 정의를 표로 꺼내면 화면(엔진)은 하나로 끝.

**이미 절반 구현됨 (같은 것의 세 얼굴):** 홈 `home-today`(설계사) · 어드민 Task Queue(admin) · 매니저룸 cockpit(실장).

### 재사용 단위 = "오늘의 할 일" 카드
**[채널] + 내 것만 필터 + 카운트 + 전체보기 딥링크** (전체보기 = 현장의 소리 해당 탭으로, showView+탭 이미 가능 / 카드=내부원본 딥링크 [[home_no_mock_realdata_principle]])

### 롤 → '오늘의 할 일' 정의 (채워가는 표)
| 롤 | 카드(채널 · 필터) |
|---|---|
| **설계사(MY SPACE)** | 실장 공지(오늘만, 열람/미열람/오늘 N) → 단체방 · 보험Q&A(내 질문 답변만) → 보험Q&A · 원수사 공지(내게 온 것만) |
| **실장** | 미답변 팀원 질문 · 새 보험Q&A · 내 공지 반응 |
| **지점장** | 지점 전체 미처리 · 스텝 공지 |
| **보험사** | 우리 회사로 온 질문 · 등록할 자료 |
| **admin** | 가입 승인 대기 · 위험 신호 (구현됨) |

### DB 필요 (정직)
- **"열람/미열람"** = 읽음 상태 추적 필요(새 테이블/컬럼). 나머지(오늘 공지·내 질문 답변)는 기존 데이터로 가능.

### 다음 액션
**롤별 '오늘의 할 일' 정의 표를 같이 채우면 → 엔진 하나 구현 → 홈·매니저룸·어드민·보험사에 정의만 교체해 끼움.**

---

## 지점 게시판 — 더원지점 특수성 (2026-05-31 팀장님 사전 고지)
- **작성자 = 지점장(ga_branch_manager)뿐 아니라 스텝(ga_staff) 공지도 포함 가능.**
- 이유: **더원지점은 허브게시판이 없어서**, 지점 게시판이 그 역할 일부까지 떠안는 설계. 팀장님이 지점 게시판까지 염두에 둔 계획.
- ⚠️ **이건 더원지점 기준. 타 지점은 상황 다를 수 있음 → 가정 금지** (중립 독립 SaaS 정합 [[ai_collaboration_priority]]).

### 연결된 함의 (안 꼬이게 — 작업 진입 전 점검)
1. **RLS 격차**: 현재 `team_notices` INSERT 정책 = `is_admin() OR role IN(ga_manager, ga_branch_manager)` → **스텝(ga_staff) 제외.** 스텝이 지점 게시판에 쓰려면 **RLS INSERT 정책에 ga_staff 추가** 필요 (DB 변경 = 승인 흐름).
2. **진입점 격차**: 매니저룸 nav = GA限(실장·지점장·admin) → **스텝 진입 불가.** 스텝 작성 진입점은 매니저룸이 아닐 수 있음(지점 게시판 직접 / 별도 진입) → 정의 필요.
3. 허브게시판(auto_hub) 구축 시 지점 게시판 역할 재조정 가능 (지금은 전환기 = 현실 반영).

## DB 파이프라인 = 이미 결재된 스펙 (재정의 금지)
- **[[auto_hub_v1]]** (`docs/product/auto_hub_v1.md`) — 현장의 소리 → 허브 승급 → 원세컨화 → FTS+pgvector 검색. 2026-05-16 7건 결재.
- **[[pdf_newsletter_v1]]** (`docs/product/pdf_newsletter_v1.md`) — 462 PDF 소식지 OCR → 구조화. 2026-05-16 결재.
- 둘 다 "사이트 완성"이 선행조건이라 보류됨 → 현재 전환기. **지금은 현실 반영 우선.**

## 팀장님 비전 (전환기 인식)
"실장이 공지를 너무 많이 함 + 원수사 공지와 중복" → 유기체로 돌아가면 같은 공지 반복 불필요. **지금은 그 전환기 = 상상을 현실로 만드는 단계. 일단 현실(7유형 공지 작성)부터 반영.**

## 오늘(2026-05-31) 라이브 반영
- 매니저룸 재편 첫 조각 (PR #213) — role별 D영역 + 실데이터 멤버
- 공지 작성 폼 + team_notices 저장 (PR #214) — 7유형, RLS INSERT 정합
- 단체방 = team_notices 실데이터 (PR #215)
- 지점 게시판 = branch_feed 실데이터 (PR #216)

## 매니저룸 첨부 요구사항 (2026-05-31 팀장님 명시)
- **모든 파일 형식 허용** — PDF·이미지·오피스(docx/xlsx/pptx)·한글(hwp/hwpx)·기타 등등. (파일 선택 UI = accept 제거 + multiple, 라이브 반영 완료)
- **용량 제한 없음.**
  - ⚠️ 정직 고지: 실제 업로드는 Supabase 스토리지 배선 필요(다음 조각). "무제한"은 플랫폼상 완전 무제한은 불가(버킷 max file size 설정값 존재) → **플랜 최대치로 상향 설정** = 팀장님 대시보드 권한(또는 SQL/관리 API). 업로드 배선 시 같이 처리.

## 남은 것 (눈 검수 후 방향 확정)
- **첨부 실제 업로드(스토리지 버킷 + 용량 상향)** / 보험 Q&A·스마트게시판 실데이터 정합 / 지점 게시판 스텝(ga_staff) RLS+진입점 / 어드민 허브게시판(정제·승급) / 검색 실연결 / 어드민 엔티티 카드 상세([[backlog]])
